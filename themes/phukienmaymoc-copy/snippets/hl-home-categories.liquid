<style>
@media (max-width: 991px) {
  section.category-list .category-list__content {
    display: flex !important;
    flex-wrap: nowrap !important;
    overflow-x: auto !important;
    -webkit-overflow-scrolling: touch !important;
    gap: 10px !important;
    padding: 8px 12px !important;
  }
  section.category-list .category-list__item {
    flex: 0 0 148px !important;
    width: 148px !important;
    min-width: 148px !important;
    box-sizing: border-box !important;
  }
  section.category-list .category-list__item .thumbnail {
    width: 100% !important;
    height: auto !important;
    border-radius: 8px !important;
    overflow: hidden !important;
    background: #f8fafc !important;
    padding: 8px !important;
  }
  section.category-list .category-list__item .thumbnail img {
    width: 100% !important;
    height: 90px !important;
    object-fit: contain !important;
  }
}
</style>
<section class="category-list">
            <div class="container container-fuild">
            <div class="category-list__content flex w-100">
                                    {%- assign cat1 = collections['phu-kien-may-laser'] -%}
                                    {%- if cat1 -%}
                                    <div class="category-list__item">
                                        <div class="thumbnail">
                                            <a href="{{ cat1.url }}" title="{{ cat1.title | escape }}" aria-label="{{ cat1.title | escape }}">
                                                <div class="thumbnail-item">
                                                    <img loading="eager" src="{{ cat1.products.first.featured_image.src | product_img_url: 'medium' }}" alt="{{ cat1.title | escape }}" width="136" height="95" onerror="this.onerror=null; this.src='{{ 'default_image.png.webp' | asset_url }}';">
                                                </div>
                                            </a>
                                        </div>
                                    </div>
                                    {%- endif -%}
                                    {%- assign cat2 = collections['dau-cat-laser'] -%}
                                    {%- if cat2 -%}
                                    <div class="category-list__item">
                                        <div class="thumbnail">
                                            <a href="{{ cat2.url }}" title="{{ cat2.title | escape }}" aria-label="{{ cat2.title | escape }}">
                                                <div class="thumbnail-item">
                                                    <img loading="eager" src="{{ cat2.products.first.featured_image.src | product_img_url: 'medium' }}" alt="{{ cat2.title | escape }}" width="136" height="95" onerror="this.onerror=null; this.src='{{ 'default_image.png.webp' | asset_url }}';">
                                                </div>
                                            </a>
                                        </div>
                                    </div>
                                    {%- endif -%}
                                    {%- assign cat3 = collections['nguon-laser-soi-quang'] -%}
                                    {%- if cat3 -%}
                                    <div class="category-list__item">
                                        <div class="thumbnail">
                                            <a href="{{ cat3.url }}" title="{{ cat3.title | escape }}" aria-label="{{ cat3.title | escape }}">
                                                <div class="thumbnail-item">
                                                    <img loading="eager" src="{{ cat3.products.first.featured_image.src | product_img_url: 'medium' }}" alt="{{ cat3.title | escape }}" width="136" height="95" onerror="this.onerror=null; this.src='{{ 'default_image.png.webp' | asset_url }}';">
                                                </div>
                                            </a>
                                        </div>
                                    </div>
                                    {%- endif -%}
                                    {%- assign cat4 = collections['vat-tu-tieu-hao-cho-laser'] -%}
                                    {%- if cat4 -%}
                                    <div class="category-list__item">
                                        <div class="thumbnail">
                                            <a href="{{ cat4.url }}" title="{{ cat4.title | escape }}" aria-label="{{ cat4.title | escape }}">
                                                <div class="thumbnail-item">
                                                    <img loading="eager" src="{{ cat4.products.first.featured_image.src | product_img_url: 'medium' }}" alt="{{ cat4.title | escape }}" width="136" height="95" onerror="this.onerror=null; this.src='{{ 'default_image.png.webp' | asset_url }}';">
                                                </div>
                                            </a>
                                        </div>
                                    </div>
                                    {%- endif -%}
                                    {%- assign cat5 = collections['contactor'] -%}
                                    {%- if cat5 -%}
                                    <div class="category-list__item">
                                        <div class="thumbnail">
                                            <a href="{{ cat5.url }}" title="{{ cat5.title | escape }}" aria-label="{{ cat5.title | escape }}">
                                                <div class="thumbnail-item">
                                                    <img loading="eager" src="{{ cat5.products.first.featured_image.src | product_img_url: 'medium' }}" alt="{{ cat5.title | escape }}" width="136" height="95" onerror="this.onerror=null; this.src='{{ 'default_image.png.webp' | asset_url }}';">
                                                </div>
                                            </a>
                                        </div>
                                    </div>
                                    {%- endif -%}
                            </div>
        </div>
    </section>