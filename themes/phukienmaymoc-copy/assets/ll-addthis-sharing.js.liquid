Loomline.Addthis = {
		init: function() {
			var that = this;
      that.Loomline_addthisIconShare();
      that.Loomline_clickIconsToolbar();
		},
  Loomline_addthisIconShare: function(){
		if ($('.loomline_addthis_listSharing').length > 0){
			$(window).scroll(function(){
				if($(window).scrollTop() > 250 ) {
					$('.loomline_addthis_listSharing_list').addClass('is-show');
				} else {
					$('.loomline_addthis_listSharing_list').removeClass('is-show');
				}
			});
			$('.loomline_addthis_contact__icons .loomline-box-contact,.loomline_addthis_contact__lists .loomline_addthis_close').on('click', function(e){
				if($('.loomline_addthis_listSharing').hasClass('active')){
					$('body').removeClass('loomline-locked-scroll');
					$('.loomline_addthis_listSharing').removeClass('active');
					$('.loomline_addthis_listSharing').fadeOut(150);				
				}
				else{		
					$('.loomline_addthis_listSharing').fadeIn(100);
					$('.loomline_addthis_listSharing').addClass('active');
				}
			});
			$("body").on('click', function(event) {
				if ($(event.target).is('.loomline_addthis_contact__dialog') || $(event.target).is('.addThiclose')) {
					event.preventDefault();
					$('body').removeClass('loomline-locked-scroll');
					$('.loomline_addthis_listSharing').removeClass('active');
					$('.loomline_addthis_listSharing').fadeOut(150);			
				}
			});
			$('.loomline-body-popupform form.contact-form').submit(function(e){
				var self = $(this);
				if($(this)[0].checkValidity() == true){
					e.preventDefault();
					grecaptcha.ready(function() {
						grecaptcha.execute('6LchSLkqAAAAABVHBpeFgg8N-WgkYsr5fO6GUF_s', {action: 'submit'}).then(function(token) {
							self.find('input[name="g-recaptcha-response"]').val(token);
							$.ajax({
								type: 'POST',
								url:'/contact',
								data: $('.loomline-body-popupform form.contact-form').serialize(),			 
								success:function(data){		
									$('.loomline-modal-addthis.fade.show').modal('hide');
									swal({
										icon: "success",
										className: "loomline-form-success",
										title: "{%if settings.loomline_addthis_icon3_title2 != ''%}{{settings.loomline_addthis_icon3_title2}}{%else%}Đăng kí thông tin thành công{%endif%}",
										text: "{{settings.loomline_addthis_icon3_content}}",
										button: false,
										timer: 5000,
									}).then((result) => {
										//location.reload(); 
									});
								},	
							})
						}); 
					});
				}
			});
		}
		/*if ($('.layoutProduct_scroll').length > 0 ) {
			if	(jQuery(window).width() < 768 ){
				var curScrollTop = 0;
				$(window).scroll(function(){	
					var scrollTop = $(window).scrollTop();
					if(scrollTop > curScrollTop  && scrollTop > 200 ) {
						$('.layoutProduct_scroll').removeClass('scroll-down').addClass('scroll-up');
					}
					else {
						if (scrollTop > 200 && scrollTop + $(window).height() + 150 < $(document).height()) {
							$('.layoutProduct_scroll').removeClass('scroll-up').addClass('scroll-down');	
						}
					}
					if(scrollTop < curScrollTop  && scrollTop < 200 ) {
						$('.layoutProduct_scroll').removeClass('scroll-up').removeClass('scroll-down');
					}
					curScrollTop = scrollTop;
				});
			}
		}*/
	},
  Loomline_clickIconsToolbar: function(){
		$('.loomline-toolbarCartClick').click(function(){
			if($('.loomline_addthis_listSharing').hasClass('active')){
				$('.loomline_addthis_listSharing').removeClass('active');
				$('.loomline_addthis_listSharing').fadeOut(150);	
			}
			$('body').addClass('loomline-locked-scroll');
      $('.loomline-header-action-item').removeClass('loomline-js-action-show');
			$('.loomline-sidebar-main').addClass('loomline-is-show-right');
			$('.loomline-sidebar-main .loomline-sitenav-cart').addClass('loomline-show');
		});
		$('.loomline-toolbarContactClick').click(function(){
			if($('.loomline_addthis_listSharing').hasClass('active')){
				$('body').removeClass('loomline-locked-scroll');
				$('.loomline_addthis_listSharing').removeClass('active');
				$('.loomline_addthis_listSharing').fadeOut(150);				
			}
			else{		
				$('body').addClass('loomline-locked-scroll');
				$('.loomline_addthis_listSharing').fadeIn(100);
				$('.loomline_addthis_listSharing').addClass('active');
			}
		});
		$('.loomline-toolbarMenuClick').click(function(){
			$('.loomline-activeMenuChecked').trigger('click');
		});
	},
};
(function() {
  Loomline.Addthis.init();
})();


