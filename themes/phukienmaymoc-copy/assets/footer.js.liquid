Loomline.Footer = {
		init: function() {
			var that = this;
      that.Loomline_toggleFooter();
      that.Loomline_newsletterForm();
		},
  Loomline_toggleFooter: function(){
		$('.loomline-title-footer').on('click', function(){
			if ($(window).width() < 992) {
				jQuery(this).toggleClass('opened').parent().find('.loomline-block-collapse').stop().slideToggle('medium');
			}
		});
	},
  Loomline_newsletterForm: function(){
		if($('.loomline-newsletter-form').length > 0){
			$('.loomline-newsletter-form form.contact-form').submit(function(e) { 
				var self = $(this);
				if($(this)[0].checkValidity() == true){
					e.preventDefault();
					grecaptcha.ready(function() {
						grecaptcha.execute('6LchSLkqAAAAABVHBpeFgg8N-WgkYsr5fO6GUF_s', {action: 'submit'}).then(function(token) {
							self.find('input[name="g-recaptcha-response"]').val(token);
							$.ajax({
								type: 'POST',
								url:'/account/contact',
								data: $('.loomline-newsletter-form form.contact-form').serialize(),
								success:function(data){		
									if($(data).find('#loomline-newsletter-success').length > 0){
										$('.loomline-newsletter-form .loomline-newsletter-error').html('');
										swal({
											icon: "success",
											className: "loomline-newsletter-form-success",
											title: "Đăng kí thông tin thành công",
											text: "Thông báo sẽ tự động tắt sau 5 giây...",
											button: false,
											timer: 5000,
										});
										e.target.reset();
									} else {
										$('.loomline-newsletter-form .loomline-newsletter-error').html('Địa chỉ email không hợp lệ');
									}
								},	
							})
						}); 
					});
				}
			});
		}
	},
};
(function() {
  Loomline.Footer.init();
})();
