# Kickoff — đích mới theme `1001514194` (org 200001207485, store phukienmaymoc.com)

Ghi lại bằng chứng đã đo read-only ngày 2026-09-13, để session mới bắt đầu từ dữ kiện chứ không phải phỏng đoán.

## 1. Vai trò theme (đo bằng API, không phải suy luận)

```
GET https://apis.haravan.com/web/themes.json   (Bearer token của org 200001207485)
→ 200, 3 theme:
   1001514194 | role: unpublished | Phụ kiện máy móc - 15/09/2026   <-- ĐÍCH MỚI
   1001512581 | role: unpublished | Bản sao chép của clothing       (đích cũ)
   1001510509 | role: main        | Phụ Kiện Máy Móc New Version    (BẢO VỆ - không đụng)
```

Token đọc từ `HARAVAN_CLI_CONFIG` (mặc định `~/.haravan-cli.json`), khoá `["200001207485"].access_token`. Không in token ra log.

## 2. Bản pull ở `15092026/` (gốc repo)

- Bind: `{"org_id":"200001207485","theme_id":"1001514194","theme_name":"Phụ kiện máy móc - 15/09/2026"}`.
- 43 template / 65 snippet / 1 layout / 129 asset; `.haravan-cli_remote.json` 241 mục.
- **Độ trung thực của bản pull (đo lại 2026-09-13, sau khi làm mới toàn bộ)**: 241/241 khoá khớp; 0 thiếu, 0 thừa, 0 tải lỗi; mỗi tệp có sha256 + `bytes` trong `.haravan-cli_pull-manifest.json`. **143 tệp văn bản**: độ dài byte bằng `size` API. **Ảnh nhị phân — câu trả lời của CDN cho cùng một URL không ổn định**: đo được `hera_index_hero_1_mobile.jpg` trả 45.507 B rồi 55.681 B ở hai thời điểm; `Vary: accept, accept-encoding` với `Accept: image/webp` → 76.840 B (webp) còn không gửi `Accept` → 98.904 B (jpeg) cho `hera_index_hero_2_mobile.jpg`; `size` API luôn là **kích thước bản gốc lưu trữ**. ⇒ **Không lấy khớp byte với CDN làm tiêu chí kiểm chứng**, và `size` **không** phải tiêu chí đủ/thiếu cho ảnh. Trạng thái mirror hiện tại (241/241 khoá, 0 tải lỗi): **239 tệp khớp `size`**; còn **2 tệp mà client tải của script luôn nhận bản biến thể** (`hera_index_hero_1_mobile.jpg` 45.507 vs 55.681; `shop_coupon_item_image_2.png` 1.653 vs 2.140) trong khi `size`/`public_url` mô tả bản gốc. Cùng một URL, một GET trần từ client khác lại nhận bản gốc ⇒ **câu trả lời phụ thuộc client, byte parity không phải tiêu chí**. Hai khoá này đã được ghi `variantRepresentation: true` trong manifest nên tín hiệu độ dài chỉ bắn **một lần** (đo: lần đầu `refreshed 2`, hai lần sau `refreshed 0, skipped 241`) — không có vòng tải lại mỗi lần chạy. Ngoài ra `layout/theme.liquid` **đổi thật trên remote** (stamp lẻ `17:44:45.982Z` giữa 241 khoá; nay 2.851 byte = `size`) và đã được tải lại. Mọi ảnh đều có marker kết thúc hợp lệ (JPEG `ffd9`, PNG `IEND`) ⇒ không tệp nào bị cắt.
- **Stamp `updated_at` đổi giữa phiên (đo 2026-09-13)**: `hera_index_hero_2_mobile.jpg` `17:02:03Z → 17:35:29Z`, `shop_social_sidebar_item_image_1.png` → `17:35:42Z`, `shop_social_sidebar_item_image_5.png` → `17:35:47Z`; mọi asset khác vẫn `17:02:03Z`. Ba khoá đó nay **khớp đúng bản gốc lưu trữ** (98.904 / 1.542 / 5.091 = `size` API = `fresh GET`) ⇒ stamp đổi đi kèm việc bản lưu trữ được ghi lại. **Không suy ra nguyên nhân từ stamp**: `hera_index_hero_1_mobile.jpg` có byte CDN phục vụ đổi (45.507 → 55.681) mà stamp **không** đổi, nên stamp có thể nhích vì lý do phía CDN chứ không chỉ vì người sửa asset.
- **Rác CLI**: đã xoá `15092026/.hrv_tmp_pull-<pid>-<ts>/assets/` (6 tệp: 4 bản gốc lưu trữ 55.681 / 120.262 / 2.140 / 1.542, 1 tệp 0 byte, 1 tệp trùng bản trong `assets/`).
- `config/settings_schema.json` = `[{}]`, còn `config/settings.html` = 138 KB ⇒ theme dùng cơ chế settings kiểu F1GENZ (legacy HTML), linter phải chạy `--settings-mode f1genz`/`auto` mới đọc đúng.
- **Trạng thái git**: `15092026/` là untracked và **đã được thêm vào `.gitignore`** (dòng `/15092026/`), nên `git add -A` sẽ bỏ qua cây này (12 MB, gồm cả ảnh nhị phân). Muốn commit thì `git add` theo đường dẫn cụ thể.

## 3. Việc phải sửa khi đổi đích — đã thực thi 2026-09-13 (xem §7)

- `scripts/run-haravan-verification-loop.mjs` — các hằng `TARGET_STAGING_THEME_ID=1001512581`, `PROTECTED_LIVE_THEME_ID=1001510509`, `EXPECTED_THEME_ROLE='unpublished'` và `assertStagingContainment()` chỉ cho phép **một** id staging. Với đích mới, guard phải nhận id từ manifest/config và **luôn** từ chối `role === "main"` (đọc vai trò qua `/web/themes.json`, không hardcode id).
- ~~`reports/haravan-project-route-manifest.json` (9 route) + `reports/15-page-data-mapping.json`~~ — **đã xoá**. Danh sách 15 route dùng lại được vì route là cấp store, không cấp theme: `scripts/verify-storefront-routes.mjs` chạy trên theme mới cho **15/15 HTTP 200** (tiêu đề trang vẫn là "… – Hera Jewelry" của theme mới).
- `reports/haravan-store-inventory.json` **giữ lại** (được `specs/base-theme-contract.json:42` cite).
- ~~`themes/phukienmaymoc-copy/` (53 MB)~~ — **đã xoá** cùng `plans/260912-1731-…` và các report gắn đích cũ (617 tệp tracked; khôi phục bằng `git log`).
- `scripts/fetch-haravan-theme-safe.mjs` **đã viết lại phần an toàn** (2026-09-13), và **bổ sung phát hiện drift theo `updated_at`** (2026-09-13): quyết định lấy/không lấy nằm ở `scripts/lib/pull-decision.mjs` (có test `test/unit/pull-decision.test.mjs`, 8 ca) — tệp bị sửa sau pull **không bao giờ** bị ghi đè (kể cả `HARAVAN_FORCE_REFRESH=1`), tệp văn bản so độ dài byte, **ảnh nhị phân so `updated_at`** vì độ dài không phản ánh thay đổi remote, và danh sách khoá được tải lại in ra khi kết thúc. Tham số môi trường: `HARAVAN_ORG_ID`, `HARAVAN_THEME_ID` (mặc định `1001514194`), `HARAVAN_THEME_DIR`, `HARAVAN_CLI_CONFIG` (mặc định `~/.haravan-cli.json`), `HARAVAN_FORCE_REFRESH=1`, `HARAVAN_ALLOW_DUPLICATE_MIRROR=1`. Hành vi đã kiểm bằng chạy thật: (1) **không tạo bản sao thứ hai** — nếu đã có `.haravan-cli_local.json` bind cùng `theme_id`, script **nhận** thư mục đó (`[INFO] Adopting the existing mirror … 15092026`) và chỉ **abort** khi `HARAVAN_THEME_DIR` chỉ định thư mục khác (exit 1, không tạo thư mục); (2) **không bao giờ ghi đè việc đang sửa dở** — có manifest `.haravan-cli_pull-manifest.json` (bytes + sha256 mỗi tệp), tệp lệch hash bị đếm là `locally edited` và để nguyên (đã thử: sửa 1 tệp → `locally edited 1`, hash không đổi); chỉ `HARAVAN_FORCE_REFRESH=1` mới lấy bản remote; (3) **nhắc lại được** — chạy lần hai `skipped 241, failed 0`; (4) lỗi từng asset ghi vào `.haravan-cli_fetch-failures.json` và thoát mã 1, kể cả asset văn bản không có `value` và lỗi mạng. Lệch `size` API của ảnh chỉ in `[INFO]`, không phải lỗi.
- **Pin cứng còn lại (cố ý hay chưa xử lý — quyết ở phiên sau)**: `.canary/tools/theme-fidelity.mjs:85` `DEFAULT_ALLOWED_THEMES = ['1001512581']` và `.canary/tools/theme-fidelity-run.mjs:96-98` `COPY_THEME_ID = '1001512581'` / `ALLOWED_THEME_IDS = ['-1','1001512581']`; preflight của nó **từ chối exit 4 `THEME_ID_NOT_ALLOWED`** mọi id khác, nên tái dùng harness cho `1001514194` sẽ đứng ngay ở stage `preflight`. Đây là *default của công cụ canary*, không phải mã sản phẩm; hoặc thêm `--allow-theme 1001514194`/sửa hằng, hoặc giữ nguyên và coi việc từ chối là thiết kế.
- - **Pin cứng còn lại — harness canary `1001512581`, KHÔNG có cờ/env để đổi**: `.canary/tools/theme-fidelity.mjs:85` `DEFAULT_ALLOWED_THEMES = ['1001512581']`; `.canary/tools/theme-fidelity-run.mjs:96` `COPY_THEME_ID = '1001512581'` — hằng này được so ở `readThemeSettings` (`:698-703`, lệch ⇒ `refuse('THEME_ID_NOT_ALLOWED', EXIT.REFUSAL)` = exit 4), ở kiểm tra id lạ của inventory (`:916-922`), và được truyền xuống harness qua `--allow-theme` (`:1086`, `:1355`); mặc định ở `deriveInventories` (`:380`) và `probePreview` (`:849`). Env chỉ chỉnh timeout (`:222-231`), `parseArgs` (`:584-612`) không có cờ theme, và chỉ **harness** (`theme-fidelity.mjs:363`) mới nhận `--allow-theme`. ⇒ Muốn chạy campaign fidelity cho `1001514194` thì **bắt buộc sửa module** (hoặc cho driver đọc id từ settings của workspace kèm đúng kiểu từ chối theo role); thêm cờ cho harness là không đủ.
- **Nhóm script hardcode đích cũ `1001512581`** — **đã xử lý**: `audit-theme-templates.mjs`, `sanitize-theme-snippets.mjs`, `deploy-copy-theme.mjs` (cả ba gắn mirror/đường ghi cũ) **xoá**; `verify-storefront-chromium.mjs`, `verify-storefront-chromium-direct.mjs`, `verify-storefront-routes.mjs`, `test-single-visual-compare.mjs` **nhận id qua `scripts/lib/theme-target.mjs`** (`resolveThemeId()`: mặc định `1001514194`, từ chối `1001510509` và mọi giá trị không phải số) — có test đơn vị `test/unit/theme-target.test.mjs`; đã chạy thật: `HARAVAN_THEME_ID=1001510509` → exit 1 kèm `[SAFETY] refusing to target the protected live theme`, mặc định → 15/15 route HTTP 200. `probe-theme-identity.mjs` chỉ còn ví dụ trong docstring (script vốn nhận tham số) — cập nhật ví dụ sang id mới. Trong `test/unit/`: `route-identity-gate`, `theme-fidelity`, `theme-fidelity-run` dùng id cũ làm **fixture** — bản ghi hợp lệ, giữ nguyên.
- **Cảnh báo đã xử lý**: `docs/haravan/routes-and-handles.md:49` và `docs/haravan/data-lifecycle-and-storefront-behavior.md:163` từng **cite `themes/phukienmaymoc-copy/...` làm nguồn kiểm chứng** cho claim đánh `VERIFIED`. Xoá mirror thì các claim đó mất nguồn đối chiếu, và **không thể repoint sang `15092026/`** vì đây là theme khác (nội dung/asset khác) ⇒ phải hạ cấp thành "không kiểm chứng lại được".
- ~~`scripts/deploy-copy-theme.mjs`~~ — **đã xoá** cùng mirror (script này hardcode `TARGET_THEME_ID = 1001512581`; không tái dùng cho đích mới).

## 4. Kết luận đã có bằng chứng (dùng lại được cho theme mới)

- 5 vi phạm `HARAVAN_INCLUDE_TARGET_MISSING` ở `themes/phukienmaymoc-copy` **không phải** do pull thiếu: `GET /web/themes/1001512581/assets.json` (617 khoá) xác nhận `snippets/product-loop.liquid`, `snippets/product-loop-no.liquid`, `snippets/product-loop-loadding.liquid` **không tồn tại trên remote**, trong khi `home-collection-tabs.liquid` và `homepage-collection.liquid` có. Hai snippet mồ côi này không được include ở đâu và trang chủ staging không render chúng ⇒ cách xử lý chỉ có: xoá 2 file mồ côi, hoặc ghi nhận ngoại lệ. **Không tạo snippet giả để linter xanh.**
- Lỗi đã sửa và có test đột biến: cổng "chứng thực đồng bộ" từng mặc định `VERIFIED` (fail-open) khi thiếu barrier; và `isUniformImage` chỉ phát hiện khung trắng **xám**, nên cặp khung trắng **có màu** bị chấm `PASS 0.00%`. Bản dùng chung `scripts/lib/png-raster.mjs` **hiện không có trên cây** (nằm trong `stash@{0}`, file untracked); `scripts/verify-all-visual-compare.mjs` ở HEAD vẫn giữ decoder nội bộ của nó.

## 5. Nếu khôi phục `stash@{0}` để tái dùng bộ kiểm chứng — các lỗ hổng fail-open phải sửa trước

Công việc của phiên trước (vòng kiểm chứng, chứng thực danh tính, `png-raster`, 2 test unit kèm theo, `execution-status.md`) đã được cất vào `stash@{0}` ("haravan-az-old-target-1001512581"); test PTY vẫn nằm trên cây vì nó canh một lỗi đã commit ở `e4511ce`, không gắn với đích cũ. Nếu pop lại, **không dùng nguyên trạng** — còn 4 chỗ cho phép chứng nhận mà không có bằng chứng:

1. `evaluateVerificationCase`: `hasHeader/hasMain/hasFooter ?? true`, `hoplongLeaks/liquidErrors ?? 0`, và `layout` mặc định bằng chính bề rộng viewport. Hệ quả: evidence tối thiểu `{ mismatchPercentage: 0 }` đạt `PASS` cả bốn cổng; 27 mục như vậy ⇒ `FINAL_PASS`. Sửa: thiếu `layout`/`landmarks`/`functional` ⇒ `INCONCLUSIVE` (nêu tên trường thiếu), không suy diễn mặc định.
2. `performApprovedTeardown` khi không truyền `entityDeleter`: mọi id được ghi như đã xoá và trả `clean: true` ⇒ biên bản `CLEAN` cho một lần dọn chưa từng xảy ra. Sửa: danh sách id không rỗng mà thiếu deleter ⇒ `UNVERIFIED`/không sạch.
3. Dây nối danh tính trong `runVerificationLoop` **tự sinh** `assetCdnUrls` từ chính manifest (`https://cdn.hstatic.net/themes/<orgId>/<themeId>/assets/theme.css`) — URL chưa từng được tải, khớp do được dựng ra ⇒ yếu tố CDN tautology. Sửa: trích `cdn.hstatic.net/themes/<orgId>/<themeId>/` từ **bytes thật** của tài liệu đã tải (probe đang chỉ giữ `sha256`/`bytes`, cần giữ thêm bằng chứng trích ra), rồi so với kỳ vọng.
4. `adjudicateVerificationCase` trong module chứng thực dùng `if (x && !x.verified)` nên khi tham số là `null` thì cổng bị **bỏ qua** và vẫn có thể ra `VERIFIED_PASS`. Sửa: thiếu bằng chứng ⇒ `INCONCLUSIVE`, chỉ được bỏ cổng khi có tín hiệu "không áp dụng" tường minh (module đã có `applicable: false` cho URL không phải storefront).

Hai bản ghi output của lần chạy cũ (`reports/storefront-verification-verdict.json`, `reports/fixture-teardown-receipt.json`) đã bị xoá **trước** khi stash và **không** nằm trong `stash@{0}`; chúng là output sinh lại được bằng cách chạy lại vòng kiểm chứng, không phải mã nguồn.

Hai điểm còn lại cần quyết khi tái dùng:

- Vai trò theme **đo được** qua `GET /web/themes.json` (`role`: `main`/`unpublished`) ⇒ nên truyền vào như bằng chứng quan sát, thay vì để người gọi tự khai (`manifest.store.themeRole`) hoặc hardcode. Guard containment phải từ chối mọi `role === "main"` và nhận id đích từ manifest, không hardcode `1001512581`.
- ~~`scripts/fetch-haravan-theme-safe.mjs` hardcode đường dẫn cấu hình CLI cá nhân~~ **đã sửa 2026-09-13** (xem §3): đọc `HARAVAN_CLI_CONFIG`, ghi `.haravan-cli_fetch-failures.json` và thoát mã 1 khi có asset lỗi. Phần còn lại: chưa có test tự động cho script này (đã kiểm bằng chạy thật read-only).
- `.canary/tools/lib-png.mjs` vẫn giữ decoder PNG riêng (có hỗ trợ palette, ném lỗi thay vì trả `null`) — hợp nhất vào `scripts/lib/png-raster.mjs` chỉ khi chấp nhận đổi hành vi đó.

## 6. Chặn hạ tầng đang có

- Cầu MCP của AntiFan Desktop trả `REVISION_STALE` (authority revision không còn hiệu lực) và autoheal thất bại ⇒ mọi lệnh chụp/đo DOM trên storefront đều không chạy được cho tới khi app được khởi động lại để cấp revision mới. `scripts/verify-storefront-chromium*.mjs` và vòng kiểm chứng phụ thuộc cầu này.
- Chưa có quyền ghi/push theme (kế hoạch cũ ghi rõ ở `plan.md:11`); không có pha ghi thì không có chứng thực đồng bộ ⇒ vòng kiểm chứng dừng ở `INCONCLUSIVE` theo đúng luật fail-closed.

## 7. Bàn giao việc xoá/repoint (đã thi hành 2026-09-13, sau khi người dùng "chốt")

- **Đã xoá 617 tệp tracked**: `themes/phukienmaymoc-copy/` (bản sao 53 MB của theme 1001512581), `plans/260912-1731-haravan-az-implementation-plan/` (11 tệp), các report gắn đích cũ trong `reports/` (`15-page-*`, `HARAVAN-STOREFRONT-FINAL-CAMPAIGN-REPORT.md`, `run-identity-manifest.json`, `runs/`, `fidelity-campaign-matrix.json`, `VISUAL-COMPARE-15-PAGES-VERIFICATION.md`, `visual-compare-45-cases.json`, `chromium-verification/`, 2 ảnh CSS chụp, `haravan-home-media-*`, `haravan-home-product-binding-probe.json`, `haravan-live-*`, `haravan-project-route-manifest.json`), 3 script gắn mirror (`audit-theme-templates.mjs`, `sanitize-theme-snippets.mjs`, `deploy-copy-theme.mjs`). Khôi phục: `git log --diff-filter=D --name-only` rồi `git checkout <sha>^ -- <path>`.
- **Đã giữ**: `docs/haravan/**`, `specs/**`, `themes/universal-haravan-base/`, `themes/roahtrip-haravan/` (chưa quyết), lint tooling, test PTY, `reports/haravan-store-inventory.json` (được specs cite), `plans/reports/**` (bản ghi lịch sử — không viết lại). `.canary/**` vẫn chứa id cũ (corpus có sẵn của clone engine, ngoài phạm vi).
- **Đã repoint 4 script kiểm chứng + `probe-theme-identity.mjs`** sang `scripts/lib/theme-target.mjs`; đã hạ cấp 2 claim mất nguồn trong `docs/haravan` và repoint 1 quan sát corpus sang `customizes/*` (nguồn còn sống).
- **Rác `.hrv_tmp_pull-*` đã xoá**; `15092026/` giờ chỉ còn `.gitignore`, `.haravan-cli_local.json`, `.haravan-cli_pull-manifest.json`, `.haravan-cli_remote.json`, `assets/`, `config/`, `layout/`, `snippets/`, `templates/`.
- **Không đụng remote (đo lại 2026-09-13 sau sự việc §8)**: `1001510509` = `main` (518 asset), `1001512581` (617), `1001514194` (241) — không theme nào mất asset.
- **`specs/base-theme-contract.json`**: 2 `investigationPlan` (dòng 1099, 1139) còn ghi id staging cũ → nay ghi "unpublished staging theme under work (1001514194 as of 2026-09-13)". `reports/haravan-store-inventory.json:101` vẫn được cite ở dòng 42 và **đã giữ lại**.

## 8. Sự việc: watcher `hrv theme dev` còn sống trên thư mục mirror (2026-09-13)

- Khi xoá `themes/phukienmaymoc-copy/`, thư mục bị khoá (Windows `os error 32`). Nguyên nhân: **3 tiến trình `hrv theme dev` còn sống** (node pids `13568` sinh 2026-09-12 13:11, `25780` 2026-09-13 00:15, `23088` 00:25, kèm `cmd /c hrv theme dev --log` pid `1476`), một tiến trình đang ghi `themes/phukienmaymoc-copy/.hrv-sync-state.json`.
- Nguy hiểm thật: watcher này **lan truyền việc xoá tệp local sang remote** — `.hrv-sync-state.json` ghi 2 lượt xoá `assets/check_out.css`, `assets/color_font.scss.liquid` với `"reason": "processingDeleteFile returned false"` (17:32Z). **Cả hai đều thất bại**, và đo lại sau đó cho thấy theme `1001512581` vẫn đủ **617 asset** ⇒ không có tệp remote nào bị xoá.
- Đã dừng: `taskkill` nhẹ bị từ chối ("can only be terminated forcefully") → dừng cưỡng bức `/F` cho 4 pid trên; còn lại không có tiến trình `theme dev` nào. Sau đó thư mục mirror xoá được.
- **Nguồn của watcher**: `.canary/tools/theme-fidelity-run.mjs:115` khai `DEV_COMMAND = { command: 'hrv', args: ['theme','dev'] }` và spawn nó với `cwd: themeDir` ở dòng 1213-1214 — comment trong file gọi đây là "the one remote-writing command this pipeline may spawn". Hai tiến trình `hrv theme dev` trần (pid 25780, 23088) khớp đúng lệnh này ⇒ nhiều khả năng là **giám sát còn sót của lần chạy canary cũ** (cwd = mirror cũ), và chính nó lan truyền việc xoá. Kết luận: khi xoá một mirror, phải dừng watcher của harness trước.
- **Rủi ro còn treo**: bất kỳ ai chạy lại `hrv theme dev` trong repo khi `15092026/` đang là mirror **sẽ tự động đẩy nội dung qua remote** (đúng tính năng của `theme dev`, nhưng trái luật "không ghi remote" của phiên này). Chỉ chạy khi chủ đích muốn push.
