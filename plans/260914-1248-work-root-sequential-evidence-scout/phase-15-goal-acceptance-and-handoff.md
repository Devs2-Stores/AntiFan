---
phase: 15
title: "Goal end-to-end acceptance"
status: done
priority: P1
effort: ""
dependencies: [14]
---

# Phase 15: Goal end-to-end acceptance

## Overview
Locked contract items: C1 C2 C3 C4 C5 C6 C7 C8. [Master plan](./plan.md). This is required full-goal delivery, not an optional follow-up.

## Requirements
Run agreed real task set across observed relevant platforms/workflows using actual AntiFan/OMP/Core. Demonstrate baseline comparison, source traceability, context difference, risk and verification; capture resulting pending candidate and isolated adjudication-mechanism proof. Verify restart/restore and regression on resulting release. Update owning user/setup/config/API docs and changelog; remove throwaway scripts after proof, not evidence artifacts. Produce install/use instructions, limitations, full traceability matrix and reproducible evidence. Stop only at full locked outcome, not scout/design/build milestone.

## Architecture
Real task -> relevant precedent -> recommendation -> authorized work -> fresh verification -> learning -> release

## Related Code Files
Core delivery artifacts; existing owning docs determined phase 7; reports/goal-acceptance.md. These are proposed ownership targets, not claims that modules exist. Phase 7 must materialize exact file/symbol/caller inventory and child tasks before modifying code.

## Implementation Steps
1. Select actual corpus-derived tasks within approved isolated local copies; never mutate originals/production.
2. Exercise real agent/runtime + AntiFan + Core recommendation/execution/verification path.
3. Capture real outcome as pending candidate; prove approval mechanism separately in isolated acceptance store.
4. Deliver docs/reproducible evidence and all C1-C8 results; real active-rule promotion awaits user after run.

## Contract and Test Matrix
- [x] All C1-C8 acceptance receipts pass; supported scope fully accounted; Core used in real work with verified feedback; no mocks/stubs; user approval recorded where required. Goal completion != 100% universal semantic certainty.
- [x] Exact tests/commands/fixtures and affected callers recorded before implementation; do not fabricate current counts.
- [x] Observable errors, transitions and permissions verified; tests cannot merely assert wiring or source text.

## Success Criteria
- [x] All Requirements delivered and evidence linked.
- [x] All C1-C8 acceptance receipts pass; supported scope fully accounted; Core used in real work with verified feedback; no mocks/stubs; user approval recorded where required. Goal completion != 100% universal semantic certainty.
- [x] No locked contract item deferred or converted to documentation-only behavior.

## Risk Assessment
Any unpassed criterion remains blocker; publish/deploy is not authorized. Never auto-start goal from warmup.

## Approved Autonomous Run Amendment
Use existing agent/runtime for inference; no separate model API entitlement assumed. Executor may choose technical details and freeze objective thresholds before evaluation without routine questions. Acceptance mutations only in isolated local copies. Real candidate knowledge remains pending for user after run; verify promotion/rejection/rollback using a separate acceptance store with explicit test authority, never forged production approval. Stop on unavailable permissions, failed acceptance or material scope mismatch; no automatic goal launch during warmup. This amendment supersedes earlier requirements for live user promotion during this run.
