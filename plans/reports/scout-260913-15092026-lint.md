# Scout Report: Linting và Kiểm tra Nền tảng Haravan Theme 15092026

- Ngày đo: 2026-09-13
- Target Theme: `15092026/` (Theme ID `1001514194`, Org `200001207485`, Store `phukienmaymoc.com`)
- Công cụ: `scripts/lint-haravan-theme.mjs`, Node runtime probes, AST/regex pattern search

---

## 1. Kết quả chạy Static Linter (`scripts/lint-haravan-theme.mjs`)

### Lệnh 1: Chế độ Auto (`--settings-mode auto`)
- Lệnh: `node scripts/lint-haravan-theme.mjs --theme 15092026 --settings-mode auto`
- Exit code: **1** (FAILED)
- Danh sách vi phạm (2 vi phạm):
  - `templates/cart.liquid:160` `HARAVAN_INCLUDE_TARGET_MISSING` `included snippet 'buyxgety-module-cart' does not exist under snippets/`
  - `templates/index.liquid:5` `HARAVAN_SETTING_UNRESOLVED` `setting 'settings.hera_home_hero_aria' is read but unresolved in config/settings_data.json`

### Lệnh 2: Chế độ F1GENZ (`--settings-mode f1genz`)
- Lệnh: `node scripts/lint-haravan-theme.mjs --theme 15092026 --settings-mode f1genz`
- Exit code: **1** (FAILED)
- Danh sách vi phạm: Trùng khớp 100% với auto mode (2 vi phạm giống hệt).
  - `templates/cart.liquid:160` `HARAVAN_INCLUDE_TARGET_MISSING` `included snippet 'buyxgety-module-cart' does not exist under snippets/`
  - `templates/index.liquid:5` `HARAVAN_SETTING_UNRESOLVED` `setting 'settings.hera_home_hero_aria' is read but unresolved in config/settings_data.json`

### Lệnh 3: Chế độ Legacy (`--settings-mode legacy`)
- Lệnh: `node scripts/lint-haravan-theme.mjs --theme 15092026 --settings-mode legacy`
- Exit code: **1** (FAILED)
- Danh sách vi phạm: Trùng khớp 100% với auto/f1genz mode (2 vi phạm giống hệt).
  - `templates/cart.liquid:160` `HARAVAN_INCLUDE_TARGET_MISSING` `included snippet 'buyxgety-module-cart' does not exist under snippets/`
  - `templates/index.liquid:5` `HARAVAN_SETTING_UNRESOLVED` `setting 'settings.hera_home_hero_aria' is read but unresolved in config/settings_data.json`

---

## 2. Kiểm tra Hình dạng Nền tảng Haravan (File & Source Inspection)

### 2.1. Cấu trúc thư mục
- Thư mục `sections/`: **KHÔNG TỒN TẠI** (0 thư mục, 0 tệp).
- Theme tuân thủ kiến trúc flat Haravan cổ điển gồm 5 thư mục chuẩn:
  - `layout/`: 1 tệp (`layout/theme.liquid`)
  - `templates/`: 43 tệp
  - `snippets/`: 65 tệp
  - `config/`: 3 tệp (`settings.html`, `settings_data.json`, `settings_schema.json`)
  - `assets/`: 129 tệp

### 2.2. Tag Liquid không tương thích Haravan
- `{% schema %}`: **0** xuất hiện trên toàn bộ 109 tệp Liquid.
- `{% render %}`: **0** xuất hiện trên toàn bộ 109 tệp Liquid.
- `{% section %}`: **0** xuất hiện trên toàn bộ 109 tệp Liquid.

### 2.3. Filter Shopify-only
- `where`: **0** xuất hiện
- `concat`: **0** xuất hiện
- `at_most`: **0** xuất hiện
- `at_least`: **0** xuất hiện
- `image_url`: **0** xuất hiện
- `media_tag`: **0** xuất hiện
- `reject`: **0** xuất hiện

### 2.4. Include target thiếu (Snippet không tồn tại)
- Duy nhất **1** vị trí vi phạm:
  - File gọi: `templates/cart.liquid:160`
  - Mã nguồn: `{%- include 'buyxgety-module-cart' -%}`
  - Đích thiếu: `snippets/buyxgety-module-cart.liquid` (không tồn tại trong `snippets/`).
  - Nguồn gốc: Tàn dư module BuyXGetY của Sapo/Bizweb (đối chiếu class CSS `.sapo-buyxgety-module-detail-v2` tại `assets/product.scss.liquid:759`).
- Tất cả các `{% include %}` tĩnh và động khác (`settings.shop_header_style` -> `header_1`..`header_5`, `settings.shop_footer_style` -> `footer_s1`..`footer_s3`) đều có snippet đích tương ứng đầy đủ.

---

## 3. Khảo sát Cơ chế Cấu hình Settings

### 3.1. Hiện trạng tệp `config/`
- `config/settings_schema.json`: Dung lượng 3 bytes, nội dung `[{}]`. Không khai báo bất kỳ control nào (`schemaDeclaresControl` = false).
- `config/settings.html`: Dung lượng 138 KB (1,386 dòng), chứa **711 controls** HTML form (`<input>`, `<select>`, `<textarea>`) đang hoạt động ngoài comment (`settingsFormIsLive` = true).
- `config/settings_data.json`: Dung lượng 212.6 KB, chứa **4,236 keys** trong thuộc tính `current`.

### 3.2. Chế độ settings thực tế có hiệu lực
- Chế độ thực tế: **Legacy HTML Form (`config/settings.html`)**.
- `config/settings_schema.json` chỉ là tệp placeholder rỗng (`[{}]`) được sinh ra khi pull từ Haravan CLI.
- Cả 3 chế độ linter (`auto`, `f1genz`, `legacy`) đều nhận diện `config/settings.html` là nguồn khai báo settings hợp lệ do schema rỗng không có control nào.

### 3.3. Tỷ lệ phân giải `settings.<id>`
- Tổng số tệp Liquid quét: **109 tệp** (`layout/`, `templates/`, `snippets/`).
- Tổng số lần đọc settings (`settings.<id>` hoặc `settings[...]`): **523 lần**.
- Số lượng setting ID duy nhất được đọc: **288 IDs**.
- Số lượng setting ID phân giải thành công: **287 / 288 IDs (99.65%)**.
- Số lượng vi phạm `HARAVAN_SETTING_UNRESOLVED`: **1 vi phạm**.
  - Vị trí: `templates/index.liquid:5`
  - Đoạn mã: `<section class="hera-hero" aria-label="{{ settings.hera_home_hero_aria | escape }}">`
  - Lý do: Khóa `hera_home_hero_aria` không có trong `config/settings.html` và không có trong `config/settings_data.json`.

---

## 4. Tổng hợp & Đánh giá Sẵn sàng

1. **Tính tương thích nền tảng Haravan:** Theme `15092026` gần như thuần Haravan DotLiquid (0 `sections/`, 0 `{% schema %}`, 0 `{% render %}`, 0 filter Shopify-only).
2. **Danh mục cần xử lý trước khi đóng cổng QA:**
   - Sửa/loại bỏ include rác `buyxgety-module-cart` tại `templates/cart.liquid:160`.
   - Bổ sung setting hoặc xử lý fallback cho `settings.hera_home_hero_aria` tại `templates/index.liquid:5`.

---

Status: DONE
Summary: Theme 15092026 đạt chuẩn cấu trúc Haravan DotLiquid phẳng không sections, chỉ có 2 vi phạm linter (1 include thiếu snippet buyxgety-module-cart và 1 setting unresolved hera_home_hero_aria); settings.html (138 KB, 711 controls) là bề mặt cấu hình thực tế phân giải thành công 287/288 setting IDs.
