# AntiFan Browser Desktop — Changelog

Tất cả các thay đổi, tính năng mới và bản vá lỗi quan trọng của AntiFan Browser Desktop.

---

## [v1.3.6] - Unreleased

### Thêm — Mute cố định theo website
- Nút loa luôn hiện cạnh Reload, cho phép tắt/bật âm thanh cả khi website chưa phát tiếng; icon gạch chéo và `aria-pressed` phản ánh trạng thái.
- Lưu theo hostname HTTP/HTTPS trong `saved-tabs.json`; đồng bộ các tab cùng hostname và hai khung Desktop/Mobile. Giữ lựa chọn qua reload, điều hướng và khởi động lại; hostname khác không bị ảnh hưởng. Trang nội bộ như `about:blank` không hỗ trợ nút này.
- Kiểm chứng: `npm run compile`; 41/41 test host liên quan; `node scripts/run-electron.cjs test/e2e/site-mute-smoke.cjs` chạy trên Electron thật với profile tạm, kiểm tra nút ở 960px, nhiều tab/split, điều hướng, tiến trình mới và xóa lựa chọn khi bật tiếng lại.



### Sửa bảo mật — Terminal: scope mọi lệnh terminal gắn attachment về đúng tab sở hữu (commit `0cf4e68f`)
- **Vấn đề**: trước bản sửa, một attachment MCP đã pair có thể điều khiển terminal thuộc tab/agent khác — không có kiểm tra sở hữu giữa attachment và tab.
- **Sửa**: `resolveTerminalCallerScope` trong `src/main/tools/terminal-capabilities.ts` phân giải `ownerTabId(attachmentId)` qua `TerminalOwnershipPort` (`src/main/control-plane/control-plane-runtime.ts`); attachment không gắn tab nào bị từ chối với `TERMINAL_FORBIDDEN`. `operate` (write/resize/close/split) owner-strict; `observe` (wait) cho phép user plane nhưng chặn terminal riêng của attachment khác.
- **Bằng chứng**: `test/main/` suite xanh; regression mới phát hiện 2026-09-24 (attachment pair qua `/api/pairing/exchange` chưa ghi `tabId` vào record ⇒ `TERMINAL_FORBIDDEN`) được vá trong cùng loạt sửa này — proxy giờ dispatch `browser.rebind-target` để ghi `tabId` vào `AttachmentRegistry` (`capability-transport.ts` → `updateAttachmentTab`).


### Tối ưu — Switch tab: bỏ vòng recycle vô ích và chèn pane đúng chỗ mà z-order muốn (2 bản sửa, có test)
- **Bậc thang A/B của plan bị chính dữ liệu vô hiệu (đo, không suy luận)**: `r2` chạy **đúng bundle** `a1e5f358…` mà chỉ ở volume của leg 1 vẫn cho **49.02 ms** (warmup, n=530) / **30.95 ms** (leg 1, n=29) — tức bước switch tái hiện **trong cùng một bundle** khi đổi giai đoạn. Cùng bundle `f2e0b41f…` cho 13.48 (leg 4 `burst-off`) / 23.89 (leg 5), và cùng bundle `0b56f9f6…` cho 20.16 (leg 5) / 20.98 (leg 6). Vậy "thang A/B định vị được bước" là **sai**; đại lượng quyết định là **giai đoạn/khối lượng switch**, không phải driver burst — và mọi so sánh phải đọc trong cùng band elapsed.
- **Cơ chế (đọc từ chính đường switch)**: `attachTabView()` chèn view vào `contentView.children` tại **index của frameBackdrop + 1**, tức *dưới* các tab view mà cú switch này đang thay thế. `enforceZOrder()` sau đó phải **tháo và gắn lại** view, sidebar và toolbar — ba remove, ba add, ba invalidate — để dựng đúng thứ tự mà chính phép chèn kia có thể dựng trực tiếp. Chỉ số chèn phụ thuộc thứ tự `_children` **hiện tại**, nên cùng một bản compile cho 30.95 ms hay 49.02 ms tùy lịch sử attach trước đó: đây là biến ẩn đứng sau khác biệt liên-run.
- **Sửa 1 — `reassertPresentedView()` thôi recycle thứ vừa gắn**: thêm `options.recyclePresentedLayer` / `recycleMobileLayer`. Đường switch ghi nhận `isTabViewAttached()` **trước** khi nó tự `attachTabView()`, rồi truyền kết quả đó xuống, nên view mà cú switch vừa gắn đã có visual của nó và **không** phải remove+add lại. View đã gắn từ trước vẫn recycle như cũ (occlusion / capture vẫn cần dựng lại visual), nên đường chống "canvas trắng" không đổi hành vi.
- **Sửa 2 — `attachTabView()` chèn ở nơi `enforceZOrder` muốn**: `insertIndex = children.length`, tụt xuống ngay **trên** shell chrome đầu tiên tìm thấy trong `[sidebarView, toolbarView]` (thay vì index của backdrop). Cùng một thứ tự, dựng bằng một phép chèn thay vì một chuỗi remove/add.
- **Bằng chứng**: `npm run compile` exit 0; `node --test --test-force-exit test/unit/native-tab-host-presented-view.test.ts` **2/2 pass** (test mới: view được gắn trong cùng cú switch ⇒ không recycle; view đã gắn từ trước ⇒ vẫn recycle; và bất biến có thể kiểm chứng được mô phỏng: chèn đúng chỗ thì `enforceZOrder` **không** phải sửa gì). Đột biến để chắc test bắt lỗi thật: trả `recyclePresentedLayer` về `true` ⇒ test fail.
- **Đo trong run đang chạy `4hfix9`** (warmup, **chưa phải số của verdict**): harness báo **534 switch warmup, p50 15.34 ms** (`metrics.switchLatencyWarmupMs`) — đó mới là con số tổng có thẩm quyền. Phân rã bước (sáu mốc trong `switchStepSamples[].extra`): `attachSweep` **80.9 %** (10.077 ms), `layoutBroadcast` 6.7 % (0.833), `presentedView` **5.8 %** (0.725), `invalidateFocus` 4.1 % (0.517), `throttle` 1.3 % (0.158), `ensureView` 0.4 % (0.051), head (tổng − Σ mốc) 0.8 % (0.099). Tổng sáu mốc khép lại trong 0.099 ms. So với bundle cũ (`night4h`): `presentedView` từ 36 % xuống 5.8 %, `attachSweep` từ 58 % lên 80.9 % — hai bản sửa hạ đúng hai mốc chúng nhắm. (`switchStepSamples` là 1:1 với switch — `steps=535` so với `switches=534` — nên p50 12.239 của bảng phân rã là cùng tập mẫu, không phải tập con. Khoảng cách của nó với con số harness báo là **~3.1 ms transport** — phần tiêu tốn *ngoài* các mốc của app — và phần này tăng dần theo bundle: **1.34 ms** (`4hfix6`) → **2.02** (`4hfix7`) → **3.10** (`4hfix9`).)
- ⚠️ **Cảnh báo chưa gỡ: hai bundle hậu-sửa không cùng số, và phần lớn khả năng là do tải máy.** `4hfix6` (bundle `53f0fa88`, chỉ sửa 1+2) đo **p50 warmup 9.621** (n=566); `4hfix7`/`4hfix9` (bundle `2aacaa2c`, thêm sửa 3) đo **14.03 / 15.34**. Số tab như nhau (6), cùng sáu URL, cùng `switchIntervalMs` 3000, mọi tab đều chậm hơn ~50 % — nhưng **tải máy cũng khác ngay từ dòng đầu**: sampler của chính mỗi run cho `4hfix6` load trung bình **63.5 %** (1/6 dòng ≥90 %) so với **94.4 %** ở `4hfix9` (7/8 dòng ≥90 %), external 18 tiến trình ở cả hai. Một mức ~1.5× đều đặn trên mọi tab là đúng dạng tranh chấp CPU trên host 4 nhân/8 luồng này. Delta nguồn đã commit chỉ một file (`src/main/index.ts`, sửa 3 — listener `close` + hai guard, không chạy theo mỗi cú switch). Trạng thái trung thực: **đo được, tương quan với bundle, tương quan với tải, nguyên nhân còn mở** — không kết luận là regression của code. Phép thử tách biến: build lại `c55e894c` và đo đúng dải warmup đó **kèm ghi lại tải**, chạy **sau** verdict (trước verdict không recompile). Đáng chú ý cho verdict: ngưỡng cổng là **p50 ≤ 12 ms / p95 ≤ 18 ms ở pha workload** (`benchmark-real-soak-8h.cjs:226`), nên dải warmup của `4hfix6` (9.621) nằm dưới ngưỡng còn của `4hfix9` (15.34) nằm trên — không được gộp hai bundle thành một "số hậu-sửa".
- **Số cuối — run chạy đủ 240/240 phút; verdict FAIL ở hai cổng, và cả hai đều KHÔNG phải bằng chứng regression.** Bundle `2aacaa2c…`, `changedDuringRun: false` (md5 đầu == cuối). Gate: **peak FAIL** (`activeWorkingSetMB.max` **1670.30** vs ≤1600), **latency FAIL** (workload p50 **13.487** vs ≤12, p95 **20.853** vs ≤18), orphan/execution/teardown **PASS**, hai cổng slope **null** (`slopeGateApplicable: false`) vì leg run không chấm một slope gộp nhiều chế độ. Exit 1 là verdict FAIL của harness, không phải crash. Verdict đầy đủ: `plans/reports/runtime-verification/real-soak-4h-verdict-4hfix9.md`.
- **Bisect chạy trọn cả ba leg và trả lời được đúng câu hỏi của nó**: mỗi leg 60 phút, **switch giữ nguyên** (1075 / 1080 / 1077) trong khi **burst lines đổi 35,700 → 142,800 → 5** — dải **28,560×** — mà CPU app chỉ **12.125 / 12.770 / 12.099 s/phút** và **665.7 / 698.4 / 663.5 ms mỗi switch**, phẳng trong 5 %. Nghĩa là **chi phí switch không phụ thuộc khối lượng workload**: ở leg yên tĩnh nhất (5 dòng burst trong một giờ) app vẫn tiêu 12.1 s/phút. `msPer1000BurstLines` sụp (20046 → 5282 → 142918800) chỉ vì **mẫu số** sụp, không phải vì chi phí giảm.
- **Criterion 2**: `rendererActiveSlopeMBPerMin` **−0.0117**; chuỗi app-owned theo cửa sổ **+1.21** (warmup) → **+0.02** (workload) → **+0.02** (recovery) MB/phút — tăng là **fill lúc warmup** rồi phẳng. Per-process max **0.1096** so với 0.15 là **đạt**, nhưng endpoint delta của chính chuỗi đó là 39.77 MB / 179 phút = **0.222 MB/phút** — đạt là nhờ **fit LSQ trên đường cong giảm tốc**, không phải nhờ hai đầu mút; câu "0.1096 ≤ 0.15" phải đi kèm con 0.222.
- **Criterion 3**: max **152.674 ms được báo cáo, không được chấm**; harness tự chỉ ra trang — `Slowest Switch Destination: https://www.wikipedia.org (max 152.674ms, p95 26.192ms over 627 switches)`, và đuôi này trải **cả ba leg lẫn cả hai pha** nên là tái hiện được, không phải one-off.
- **Cảnh báo tải giữ nguyên giá trị cho con số latency**: đo ở ~94 % CPU trên host 4 nhân/8 luồng, cạnh ~24 `node.exe` còn sống từ phiên 09-21 22:28–23:44, một instance app thứ hai (00:37) và Chrome/Figma/Zalo/Orca của người dùng. A/B hậu-sửa trước đó cho cùng dải switch ở **9.621 ms (tải 63.5 %)** và **15.34 ms (tải 94.4 %)** — nên **13.487 ms là cận trên, không phải số sạch**.
- **Cổng peak không thể đạt bằng việc sửa retention**: `initialLoadedMB` **1574.46** vs ngưỡng 1600, tức app đã nằm sẵn trong 26 MB của ngưỡng **trước mọi workload**, rồi fill warmup cộng ~69 MB (`postWarmupMB` 1643.48). Đo được: peak 1670.30 (run này) so với 1692.17 (`night4h`) — tức **giảm ~22 MB** nhưng vẫn trên ngưỡng.
- **Criterion 5 — `npm run compile` xanh (exit 0), và suite renderer từ ĐỎ thành XANH: 145 pass / 0 fail.** Suite này **đỏ sẵn trên checkout sạch** (3 test) và **không phải do run này**: `f4d90277` chuyển tab strip từ HTML5 drag sang **pointer drag** — vì đường HTML5 đi qua OLE/DirectUI (`DUI70.dll`) và đã làm null-deref tiến trình khi kéo tab vào nhóm — nhưng để lại 3 test còn chạy đúng đường đã bỏ: wrap giờ **không bao giờ `draggable`** và không có listener `dragstart`/`drop`. Harness cũng **không thể diễn tả cử chỉ đó**: nó chỉ bắt `keydown` trên window, không có `document.elementFromPoint`, và `FakeElement` không có `closest` — hàm mà đường drop dùng để truy ngược từ node trúng đích. Đã bổ sung đúng ba thứ đó rồi chuyển test sang `pointerdown → move → pointerup`; **không sửa một dòng source nào**, chỉ test + harness. Đường HTML5 `drop` của header **vẫn được test**, vì payload ngoài (một file) là cú kéo duy nhất mà strip không tự sinh ra — và `commitCategoryDrop` từ chối id không phải session. Một kỳ vọng đổi nghĩa: `f4d90277` cũng làm **tab thả lên tab nhận nhóm của tab đích** — thiếu điều đó thì lần render sau sẽ xếp tab về nhóm cũ và cú kéo trông như bị huỷ, nên chính việc nhận nhóm mới làm một cú kéo khác nhóm trở nên quan sát được; assertion về thứ tự giờ nói đúng điều đó, còn **thứ tự nhóm vẫn không bao giờ bị xáo**.


- **Sửa 3 — keep-alive cho run benchmark (đường `window-all-closed`)**: cửa sổ cuối cùng bị đóng từ ngoài run thì app tự thoát sạch — `steps4h` (2026-09-21 15:22, pid 26464, uptime 6.77 min) là ca có bằng chứng: journal của chính app ghi theo thứ tự `shutdown.begin` (uptimeMs 406383) → `window-all-closed` → `before-quit -> will-quit` → `process.exit.event code 0`, và mọi bước teardown (`ledger.settleInFlight`, `terminal.persistSync`, `tabHost.flushAllSessions`, `cookies.flushStore`, `bridgeServer.dispose`) đều chạy xong trước khi thoát. Handler là `src/main/index.ts:942`, nên **bất kỳ** cú đóng cửa sổ từ ngoài nào cũng kết thúc một run đo. Bản keep-alive đã có trong cây làm việc **không cứu được**: Electron phát `closed` **trước** `window-all-closed`, mà hook `mainWindow.on('closed')` gọi thẳng `await shutdown(); app.quit();` **không** qua `refusesWindowClose()` — nên `window-all-closed` không bao giờ có lượt. **Sửa**: gate chính hook đó bằng `refusesWindowClose() && !isShuttingDown`, ghi `window-closed.ignored` + `console.warn`, để lần sau mất cửa sổ sẽ **hiện ra trong log** thay vì im lặng. Vị từ `refusesWindowClose()` đã có test sẵn (`test/main/performance-benchmark-contract.test.ts`, cả hai nhánh env + cả hai nhánh benchmark).
- **ĐÍNH CHÍNH (cùng ngày) — `4hfix6` KHÔNG chết vì cửa sổ đóng, nó bị harness của chính tôi giết**: log của wrapper `soak-4hfix-d` ghi `{"event":"attempt.stalled","tag":"4hfix6","idleMinutes":5.01}` rồi `{"event":"attempt.finished","code":null,"signal":"SIGTERM","elapsedMinutes":31.01}`. Xem **Sửa 4**. Vì vậy Sửa 3 được giữ lại nhưng **neo vào `steps4h`**, không neo vào `4hfix6` — và cũng vì thế lần đầu tiên kiểm chứng được Sửa 3 là một run chạy hết (`4hfix9`), không phải `4hfix6`.
- **Bằng chứng**: `npm run compile` exit 0; `.compiled/src/main/index.js` có guard (dòng 567, `window-closed.ignored`). Run chạy trên cây đã sửa để kiểm chứng là `4hfix9` — `4hfix7`/`4hfix8` **không** dùng được làm bằng chứng, xem mục dưới.
- **Sửa 4 — watchdog của harness tự giết hai run khoẻ (`4hfix6` phút 31.01, `4hfix7` phút 11.01; `4hfix8` chủ động dừng)**: `stallLimitMs = 5 phút` so với **mtime của file sampler**, nhưng nhịp ghi của `scripts/sample-external-load.cjs` là `INTERVAL_MS` 300 s **cộng** thời gian quét tiến trình bằng PowerShell của chính nó (đo được ~8–14 s), nên khoảng cách giữa hai dòng là **309–314 s > 300 s** (`4hfix6`: 00:46:26 → 00:51:35 → 00:56:47 → 01:01:55 → 01:07:03 → 01:12:11; `4hfix7`: 01:21:00 → 01:26:14). Comment ngay trên watchdog ("The sampler appends a row a minute") **sai**, và ngưỡng được đặt theo tiền đề sai đó. **Bằng chứng watchdog tự khai** — `soak-4hfix-d`: `{"event":"attempt.stalled","tag":"4hfix6","idleMinutes":5.01}` → `{"event":"attempt.finished","code":null,"signal":"SIGTERM","elapsedMinutes":31.01}`; `soak-4hfix-e`: cùng dạng, `idleMinutes:5.25` → `elapsedMinutes:11.01`. **Cơ chế (khớp với chính các con số đó)**: sau mỗi lần ghi, `idle` chỉ vượt 300 s trong khoảng **9–14 s** trước khi dòng kế tiếp tới, mà watchdog chỉ hỏi mỗi **60 s** — nên nó bắn xác suất ~15–23 % mỗi chu kỳ. `idleMinutes` của cả hai ca đều **sát trên 5.00** (5.01 / 5.25) chứ không phải 10–30 phút, tức sampler **vẫn sống**; một sampler chết thật sẽ cho idle lớn hơn nhiều. Số chu kỳ cần để trúng giải thích luôn thời điểm tử (11 phút ≈ 2 chu kỳ, 31 phút ≈ 6). Hệ quả: đây mới là lời giải cho chuỗi "run tắt im lặng ở phút 30 nào đó", và mọi suy luận trước đó gán các ca tử cho "app tự thoát" đều **không đứng vững cho `4hfix6`**. **Sửa**: ngưỡng **20 phút** (4× biên so với nhịp thô nhất), lấy mtime **mới nhất trong ba nhịp tim** mà run thật sự phát ra (dòng sampler, checkpoint, run log) — sampler thô không còn đọc thành "treo"; và đường kill đổi từ `child.kill()` sang **`taskkill /PID <pid> /T /F`**, vì harness bị treo (đúng ca mà watchdog sinh ra để xử lý) **không bao giờ** chạy tới handler SIGTERM của nó — handler đó mới là thứ dọn app, nên một tín hiệu trần để lại app đang giữ cổng bridge và lượt retry kế tiếp đâm vào nó. `4hfix6`/`4hfix7`/`4hfix8` đo được **0 số của cổng**; chạy lại thành `4hfix9`/`4hfix10`/`4hfix11` trên cùng bundle `2aacaa2c…`.

### Tối ưu — badge tab thôi kéo lại tab list và affinity qua IPC ở mỗi lần render
- **Cơ chế**: `updateAffinityBadges()` không nhận tham số và luôn gọi `api.getTabs()` **và** `api.getTerminalAffinities()`. `renderTabs()` chạy theo push `antifan:tabs:updated` ở 5 Hz, nên **mỗi** broadcast tốn hai `invoke` — mỗi lần một correlation entry, một promise và một payload deserialize trên main thread, đúng cái thread mà mọi cú switch, bridge RPC và terminal fanout cũng chạy trên đó. Broadcast vốn đã giao cả hai nửa dữ liệu đó.
- **Sửa**: `renderTabs()` truyền thẳng cặp vừa nhận; `updateAffinityBadges(deliveredTabs, deliveredAffinities)` dùng cặp đó khi nó là array/object và chỉ rơi về IPC khi caller không giữ cặp nào — nên đường gọi cũ vẫn nguyên hành vi, badge vẫn tính từ đúng dữ liệu mà broadcast đã render.
- **Bằng chứng**: `src/renderer/standalone.js:2805` (nhận cặp, `:2821-2826` resolve/fallback), `:4697` (truyền), `:4756-4757` (gán từ broadcast). `.compiled/src/renderer/standalone.js` **byte-identical** với bản trong cây làm việc (`cmp` sạch) — tức bundle `2aacaa2c…` mà soak `4hfix9` đang đo **đã chứa** bản sửa này ngay cả khi nó còn chưa commit; commit `3e6f7594` đóng khoảng cách measured-vs-delivered.
- **Đo giữa run `4hfix9`** (chưa phải số của verdict): chuỗi app-tự-sở-hữu trong cửa sổ workload cho tổng Tab `privateBytesMB` LSQ **−0.29 MB/phút** (WS −0.14) — renderer **không** tăng. Process tăng cam kết lớn nhất là **Browser pid 11648, +1.10 MB/ph**, đúng field `worstProcessSlopeMBPerMin` 1.1461 / `worstProcessRole` `Browser`; `FREEZE_SLO` **không có** ngưỡng per-process nên trị số đó không phá gate nào — nó là manh mối cho tiêu chí 1, và cửa sổ 8 phút là ngắn nên chỉ thành kết luận nếu còn tăng ở leg 3 `burst-off`. **(Đính chính sau verdict — câu trên chỉ đúng cho field đã dẫn.)** `worstProcessSlopeMBPerMin` là chuỗi **working set** và thật sự không được chấm ở đâu cả; nhưng nói chung thì **sai**: `appPrivateMaxSlopeMBPerMin` **có** được so với `rendererSlopeMBPerMin` (0.15) tại `benchmark-real-soak-8h.cjs:288-291` và là số hạng verdict trên **run một leg** qua `privateSlopeOk → slopeOk → isPassed` (`:293-299`, `:318`). Chỉ vì `4hfix9` là **leg run** nên cả hai số hạng slope là `null` và trị số per-process chỉ được **in** (`:2297`) chứ không chấm. Đừng đọc `processOk` như cổng slope — nó là cổng **orphan** (`:314`, in thành `orphanSloSatisfied`). Chi tiết: mục `Correction (final payload, 12:41)` trong plan.
- **Lưu ý so sánh**: `night4h` **không** chứa bản sửa này, nên mọi câu "renderer slope đã cải thiện/không cải thiện so với night4h" là so sánh **bundle**, không phải so sánh một biến.

### Đo — soak 4 h đầu tiên chạy đủ 240 phút: bước switch có số, và thủ phạm là một bundle
- **Kết quả (đo, không suy luận)** — `night4h`, bundle `46f1f609…` (419 file, `changedDuringRun: false`), 30/180/30 phút, 3.920 switch, 242 sample RAM, exit 1 vì verdict FAIL chứ không phải crash. Gate latency **FAILS** (workload p50 **23.259** vs ≤12; p95 **37.697** vs ≤18); peak **FAILS** (1692.17 vs ≤1600 — số của walk có tiến trình ngoài app); slope **không chấm** trên leg run; orphan 0; teardown sạch; `executionOk: true, teardownOk: true`. Verdict đầy đủ: `plans/reports/runtime-verification/real-soak-4h-verdict-night4h.md`.
- **Bịt đúng mục "chưa đo" của entry `switchTab` tách bước**: sáu mốc đã có mặt ở volume thật — p50 một switch **45.82 ms** ở band 0–10 phút = `attachSweep` **26.689** (58 %) + `presentedView` **16.677** (36 %) + `ensureView` **0.034**, head ~**1.4 ms**; cả run: **23.05** = **14.939** (65 %) + **7.016** (30 %). Vậy cái tốn tiền là **cặp attach–present**, không phải tạo view, và cả hai mốc đều **giảm một nửa** trong run.
- **Run không dừng một chỗ**: p50 theo band 30 phút 47.89 → 28.82 → 27.75 → 26.84 → 20.21 → 19.18 → 19.41 (mất 40 % ngay trong 30 phút đầu). Comparator **phẳng** trên cùng trục elapsed 0–10 (`legs4h2` 11.20/10.43, `perf4h` 15.97/15.64, `domprobe` 16.59/15.92), nên độ trôi thuộc **bundle này** chứ không phải harness; gate vì thế fail *sau khi* độ trôi đã lấy đi khoảng một nửa latency.
- **Bậc thang bundle (elapsed 0–10 phút, hai lần chạy mỗi bundle)**: `6a333f3c…` **19.00 / 20.45** vs `46f1f609…` **47.68 / 49.16** — spread trong cùng bundle ≤1.48 ms, khoảng cách giữa hai bundle **27.23 ms** (≈19×). Payload của chính `night4h` là điểm thứ tư (warmup p50 **47.599**, n=530).
- **Driver test đã tách được hai đại lượng**: CPU theo khối lượng burst (6.142 / 7.801 / 8.927 mỗi leg) trong khi latency **giảm** trên đúng ba leg đó (28.24 / 23.23 / 19.34). Leg xếp theo thời gian nên trục leg mang luôn trục elapsed; "driver không quan trọng" chỉ đúng cho latency, còn chi phí của nó nằm ở CPU (top consumer: console host pty child, 24.2 %).
- **Ràng buộc mới cho mọi A/B dùng md5 bundle**: `core.autocrlf=true` và **không có** `.gitattributes`, nên `git checkout HEAD -- <file>` ghi lại file theo CRLF (`standalone.js` 208.631 → 213.794 B, đúng một CR cho mỗi LF) và bản compile copy y nguyên. Vì vậy **md5 bundle không phải là phép thử đồng nhất** giữa hai cây được checkout riêng — phải so theo band elapsed (0–10 phút).
- **Chưa đo, kèm lý do**: (1) peak memory vẫn là số của walk theo parentage (đã ghi nhận va chạm Zalo) — phải bỏ cách lọc theo cây cha trước khi đọc nó như số của app; số app-only thật (committed private, 0.1323 MB/min, pid 19768) thì pass. (2) keep-alive `window-all-closed` vẫn là follow-up của plan, chưa sửa.


### Sửa lỗi — Sidebar terminal trắng tab: `renderTabs` ném `ReferenceError` sau bản refactor kéo-thả
- **Triệu chứng (user, kèm ảnh, không suy luận)**: "Khi di chuyển 1 Tab vô Group, nếu Tab đó đang [ngủ] thì phải lập tức mở ra, giờ nó mặc định đóng nên tôi ko có trong Group có tồn tại Tab". Sidebar chỉ còn **header nhóm**, không một tab row nào; nút `+` bấm không tạo gì; trong khi terminal vẫn mount và chạy bình thường.
- **Nguyên nhân (đo bằng `git show` + chạy app sống, không suy luận)**: `f4d90277` xoá `splitGlyphTitle()` và `createAffinityBadge()` nhưng để nguyên **4 call site** trong `ensureTerminalTabWrap()`. Lần `renderTabs()` đầu tiên ném `ReferenceError` ⇒ **toàn bộ** tab strip abort giữa vòng lặp: header không dựng, không tab row, và `+` trông như chết vì broadcast do nó phát ra không bao giờ repaint. Vì sao lọt: `tsconfig.json` chỉ include `src/**/*.ts` — `src/renderer/*.js` **không** nằm trong `checkJs`, nên `npm run typecheck` xanh mà ref chết vẫn sống.
- **Sửa** (`src/renderer/standalone.js`): khôi phục **nguyên văn** hai helper từ `f4d90277^`; thay HTML5 drag của tab row bằng **pointer events** (`pointerTabDrag` + listener ở `window`) để cú thả vào group không đi vào đường OLE/DirectUI của Windows — chính đường đã giết tiến trình (`STATUS_ACCESS_VIOLATION` trong `DUI70.dll`, minidump `ae140025`); bootstrap chờ danh sách phiên thay vì tin ngay `[]` (tránh đẻ shell rác) và **vẽ** danh sách đến muộn thay vì để một push rỗng chặn vĩnh viễn corrective paint.
- **Bằng chứng**: `node --check` OK; `npm run typecheck` exit 0; quét tên trên `standalone.js` (`tsc --allowJs --checkJs --lib ES2024,DOM`) sạch, chỉ còn 2 global UMD của xterm (`Terminal`, `FitAddon`) — đúng như chúng được nạp bằng `document.write` trong `standalone.html`. Sống, sau `Ctrl+Alt+R` (`reloadWindow()` nạp lại renderer từ đĩa): sidebar render đủ nhóm (`HARAVAN → F1GENZ Ăn Vặt` + `⤷ Terminal split-10`, `CUSTOMIZES → Levents`/`Mnbakery` + split, mở `ĐANG NGỦ` thấy `Hapas`, `Haravan CLI`…); `+` tạo **đúng 1** phiên (daemon `sessionCount` 46→47, `runningPtyCount` 23→24); toggle header nhóm chạy; kéo một tab **đang ngủ** (`Hapas`) thả vào header nhóm ⇒ daemon xác nhận `sleeping → running` và `category Customizes → Shopify` (đã hoàn nguyên về `sleeping`/`Customizes`, xác nhận lại bằng screenshot + `terminalListSessions`). Tồn kho sau đó: **20 tab thật + 12 split, 0 shell rác** `Terminal N`; `runningPtyCount` 23→8, đúng bằng 15 shell rác do bootstrap hỏng sinh ra được thu hồi.

- **Chưa đo / chưa sửa**: (1) *đơ 5-10s khi tạo tab / đánh thức* — đo trên daemon lúc warm: `create 295ms`, `sleep 115ms`, `wake 406ms`, `close 893-3279ms`; probe bắn sau `create` 30ms trả về ở 412ms ⇒ spawn **nghẽn event loop của daemon**. Nhưng **mọi** RPC vòng đời trả về **toàn bộ** session kèm transcript buffer (response `terminalNewSession` 135KB; buffer từng phiên tới 4.3MB ở `terminal-1`), nên renderer parse lại toàn bộ mỗi event — đây là hướng cắt payload, **chưa** làm. **Không** lật `DEFAULT_USE_CONPTY`: comment tại chỗ ghi rõ ConPTY làm Electron không thoát được sau teardown (quyết định đã đo trước đó), lật cờ là hồi quy. (2) *crash 23:00* (`DUI70.dll+0x3733e`) — bản này mới chỉ có mitigation pointer-drag, **chưa** restart app để xác nhận. (3) *WebContent đè nhau khi MCP chạy* — chưa đụng tới.
### Sửa lỗi — Lane test đỏ: tự chữa binding khi anchor chết, EPIPE nổi trong teardown PTY, và fixture ngâm thời gian thật
- **Triệu chứng (đo bằng `npm run test`, không suy luận)**: `test:fast` 1410 test / 5 fail; `test:main` 1403 / nhiều fail — gồm `bridge-attachment-dispatch` (mở tab sau khi tab đã đóng trả `success:false`), `open-tab-anchor-liveness` (gọi `openTab` với anchor chết không bị từ chối), `resource-stability` (chết `write EPIPE` từ `Socket._writeGeneric` ngay tại `await closeSession`), `goal-runner` ×2 (supervisor không giết runner treo), `cli-agent-launcher` (attachment hết hạn giữa chừng), `mcp-proxy-emitter` P6 (ring rotation quá 5 s).
- **Nguyên nhân (đo từng ca, không suy luận)**: (1) `openTab` từ chối anchor chết đúng contract cấp port (`TARGET_STALE`), nhưng **không ai self-heal ở tầng transport** — intent `open-tab` vẫn nhận `browserTarget` đã chết nên đi thẳng vào chỗ refuse. (2) `teardownSessionPty` gắn listener `error` no-op lên **đối tượng pty**, còn EPIPE phát ra trên **socket** `_inSocket`/`_outSocket`/`_socket` khi chúng bị destroy với write còn xếp hàng → 'error' không có listener ⇒ unhandled, rơi vào `closeSession`. (3) Một loạt test ngâm theo **đồng hồ thật** trong khi lane chạy ~1400 file song song: TTL `50 ms` hết trước cả khi bước renew đầu tiên tới, ring-rotation `waitFor` 5 s, queue pairing warm-up bằng ngân sách thời gian. (4) `headless` double chỉ mang mặt cắt cấu trúc, `instanceof WebContentsView` ném `TypeError` khi `WebContentsView` là chuỗi path → `invalidate` bị skip ngoài Electron.
- **Sửa**: `capability-transport.ts` — khi anchor chết và không có failover, intent `open-tab` được dispatch với `browserTarget` undefined (tab mới tự thành target, rotation đứng nguyên chỗ cũ `updateAttachmentTab`); port giữ nguyên refusal cho caller đòi tab gắn vào anchor chết. `terminal-manager.ts` — gắn listener `error` no-op lên cả **socket** của agent trước khi destroy. `native-tab-host.ts` — chỉ `reassertPresentedView()` khi release thực sự đổi cây (`attachedByHelper || !isActiveView`), và guard `typeof WebContentsView === 'function'` trước `instanceof`. Test lane: `bridge-pairing-queue-concurrency` chờ `replenishPairingQueue()` (hàm public đã có) thay vì ngân sách 30 s; `cli-agent-launcher` TTL `50 ms → 30 s` + `extensionMs 500 → 60 s` (renewal = `now + extensionMs`, không stack); `mcp-proxy-emitter` barrier settle `30 s` riêng, assertion giữ nguyên.
- **Bằng chứng**: `npx tsc -p ./` exit 0; `node --test` trên `bridge-attachment-dispatch` **8/8** + `open-tab-anchor-liveness` **4/4** (hai contract ngược chiều cùng xanh — port vẫn refuse khi gọi thẳng, dispatch self-heal khi anchor chết); `cli-agent-launcher` **13/13**; `npm run test:fast` **1404 pass / 0 fail / 6 skip**; `npm run test:main` **1401 pass / 0 fail / 2 skip** — trước đó cùng một cây đỏ ở `resource-stability` (`write EPIPE`), `cli-agent-launcher` (`Attachment has expired`), `bridge-attachment-dispatch` (`success:false`).
- **Chưa kiểm chứng**: `goal-runner` ×2 (supervisor giết runner treo) pass cả hai lần lane chạy, nhưng mechanism gốc chưa được chẩn đoán — nếu tái lởm, xem sát `waitFor` 20 s so với `spawnNode` + `killTree` trên máy đang tải; `render-surface`/`phase-02`/`native-tab-host` viewport mới chỉ chứng minh bằng test, chưa đo trên tiến trình Electron đang chạy.


### Sửa lỗi — Phiên Haravan admin bị đá văng mỗi 20-30 phút do Companion đẩy đè cookie danh tính
- **Triệu chứng (user, kèm ảnh + log, không suy luận)**: "Cache/Cookie/Password AntiFan đang quản lý kiểu gì mà mọi 20-30p đã mất và bắt Login lại". Phiên Haravan admin trong AntiFan liên tục bị trả về `/Account/Login?ReturnUrl=/connect/authorize/callback...`; lúc đo có **hai** tab admin cùng đứng ở đúng URL đó.
- **Nguyên nhân (đo trên app sống + SQLite, không suy luận)**: kênh đồng bộ Companion đẩy đè cookie **danh tính**. Chrome thật đang giữ `accounts.haravan.com/idsrv` (1498 B, hạn ~2026-10) và `idsv.device`; `SCOPE_PROFILES.ecommerce` khớp `haravan.com` theo suffix nên `accounts.haravan.com` **nằm trong scope**; `triggerAutoHydration()` chạy lại sau mỗi handshake MV3 (log 10-18 phút/lần) với scope mặc định (`chrome.storage.local` không có override nào) và `syncActiveTabOnly = false`. Jar `persist:profile-profile-2` của AntiFan khi đó giữ `idsrv.session=6F7B009FD3A3CA4F6B9BB894D9D47747`. `idsrv` và `idsrv.session` là **hai nửa của một** phiên server-side: ghi `idsrv` của trình duyệt khác vào là tách cặp, IdentityServer huỷ phiên rồi đá về login. Trùng nguyên lý single-owner đã chốt cho Google (`docs/research-cache-cookie-mechanism.md` P2#7) — chỉ khác là ở đây nó bị **thực thi sai** trên kênh extension.
- **Sửa**: `src/shared/identity-cookie-patterns.ts` (mới) là nguồn duy nhất — `IDENTITY_COOKIE_PATTERNS` + `isIdentityCookieName()`. Bên nhận `src/main/bridge/bridge-server.ts` lọc `rawCookies` **trước mọi filter khác** và cho **mọi** lớp credential — nhánh master-token/attachment trước đây nhận `rawCookies` nguyên vẹn, không đi qua allowlist nào cả — đồng thời trả thêm `authRejectedCount` và log `[antifan] cookie import refused …`. Bên gửi `src/extension/domain-scoper.ts` trả `false` cho cookie danh tính **trước** cả nhánh wildcard `all`/`*` và nhánh active-tab. Phạm vi **chỉ cookie danh tính**: `idsrv`/`idsv.*` (IdentityServer4), `.AspNetCore.*` + `__RequestVerificationToken`, `_secure_admin_session_id`, `sapo_admin_session`, và token **xoay vòng** `*PSIDTS`/`*PSIDRTS`/`*PSIDCC`. Cookie đăng nhập **tĩnh** của Google (`SID`/`HSID`/`SSID`/`APISID`/`SAPISID`/`LOGIN_INFO`) **không** bị chặn — P0#4 đã cố ý mở allowlist cho nhóm Google và P2#7 chỉ cấm **chia sẻ vòng xoay**. Context storefront (cart, theme preview, `_haravan_session`) giữ nguyên đường đồng bộ.
- **Bằng chứng**: `npm run compile` exit 0; `extension/background.js` 189 227 B, `cmp` khớp bản đã sync sang `%LOCALAPPDATA%\AntiFan\extension`. `extension-companion-pipeline` **12/12** (10 test cũ giữ xanh + 2 mới). Đột biến để chứng minh test bắt lỗi thật, không xanh do không chạy: filter bên nhận → `false` ⇒ test ranh giới **fail** (`importedCount` 4 ≠ 2, tức cookie danh tính lọt vào store); filter bên gửi → `false` ⇒ test scope **fail**. Luật bên gửi chạy trên **30 cookie Haravan thật** đọc từ SQLite của Chrome: `idsrv` + `idsv.device` bị chặn, 25 cookie storefront còn sync. Full suite: cây hoàn nguyên 3 file của bản sửa = **1392 test / 1385 pass / 5 fail**; có bản sửa = **1394 / 1386 / 6** (đúng +2 test mới; 4 fail cố định trùng nhau, phần lệch là suite phụ thuộc thời gian) — nhóm fail nằm ở refactor `browser-control-port.openTab` đang dở trong cây làm việc, không liên quan bản sửa này.
- **Chưa kiểm chứng trên tiến trình đang chạy**: bản này **chưa load** vào app đang mở — code Main nằm trong tiến trình Electron đã khởi động trước khi sửa, và extension trong Chrome cũng chưa reload. Phép thử sống cố ý **không** chạy: tiến trình đang chạy là bản **cũ**, nó sẽ **ghi** cookie đó và phá phiên Haravan thật của người dùng — đúng cái lỗi đang sửa. Cần restart AntiFan + reload extension rồi mới đo lại.


### Sửa lỗi — Chat theme không bao giờ được bảo phải dùng MCP trước khi sửa file
- **Triệu chứng (user, session Seahorse2 `01a0c1aa`, không suy luận)**: ảnh + "Mất chữ Trang Search" → agent grep/sửa Liquid trước; MCP inspect chỉ chạy sau khi user bảo "Dùng AntiFAN MCP check lại". `theme.qa_validate` lần đầu trỏ tab Google, timeout 60s, không phải check.
- **Nguyên nhân (đo)**: `SELF_QA_DIRECTIVE` chỉ nằm trong annotation-built prompt. Chat thường không có. Bridge AntiFan (`.omp/hooks/pre/`) **không chạy** khi cwd là `E:\Work\customizes\Seahorse2` (không có `.omp`). Hook user-scope `theme-qa-gate` chỉ nhắc **sau** khi đã ghi file theme, và nhắc 1/8 `tool_result` — phiên ngắn không bao giờ thấy.
- **Sửa** (`C:\Users\Admin\.omp\agent\hooks\post\theme-qa-gate.ts`): `tool_result` đầu tiên trong cwd theme (`.antifan/` / `templates/` / `/customizes/`) nối một lần `[theme-qa-gate:mcp-first]` (tabs.list → inspect.dom → screenshot.viewport; sau edit thì qa_validate trên đúng tab). `context` tiêm cùng nội dung một lần. `REMIND_EVERY = 1`. `session_start` reset cờ.
- **Bằng chứng**: `node --test --test-force-exit test/unit/theme-qa-gate-hook.test.mjs` — **26/26 pass** (22 cũ giữ xanh + 4 mới: MCP-first một lần trên tool_result không cần write, session_start re-inject, context tiêm 1 message, cwd không-theme không tiêm).



### Bổ sung — `switchTab` tách bước; ingest soak không còn nuốt row attribution
- **Vấn đề (đo trên `perf4h`, không suy luận)**: soak 4 h bị abort lúc 08:40 local sau 109.1 phút workload (`real-soak-8h-perf4h-checkpoint.json`, 141 sample / 2727 switch). Gate latency đang hiệu lực (p50 ≤ 12 **và** p95 ≤ 18) **FAILS** (15.723 / 20.656); peak 1607.08 MB **FAILS**; slope renderer 0.1185 **không chấm** vì `slopeGateApplicable: false`. So với `legs4h2` leg 1, p50 switch **+5.6 ms** và độc lập tải/thời gian — một **bước cố định** trong `switchTab`, không phải việc scale. Giả thuyết gán bước đó cho `2697a28c` (gỡ `isTemporarilyAttachedView` nên mọi switch đều recycle) **sai tiền đề**: `git show` chỉ đổi hành vi khi *đang giữ* attach-for-capture; soak switch không giữ capture. `refusedProcessCount` 0→1 cũng không phải cây app lạ: đúng **một** sample (`python.exe` 18.58 MB) trên 141.
- **Sửa — app** (`src/main/browser/native-tab-host.ts`): `markSwitchStep` ghi sáu mốc (`ensureView`, `attachSweep`, `layoutBroadcast`, `throttle`, `invalidateFocus`, `presentedView`) vào row `tabs`/`switch-steps` **chỉ khi** `ANTIFAN_BENCHMARK=1` (soak luôn set). Production: một phép thử null/bước, không cấp phát bucket.
- **Sửa — harness** (`scripts/benchmark-real-soak-8h.cjs`): ingest trước đây `continue` mọi dòng không phải `history`/`process` — row `switch-steps` (và cả `switched`) bị nuốt. Nay thu `tabs`/`switch-steps` vào `switchStepSamples` (trần 20.000, mirror `historySamples`); `buildReportPayload` **luôn** để field này là mảng (`[]` nếu thiếu, không bao giờ bỏ field).
- **Sửa — analyzer** (`scripts/analyze-soak-app-only.cjs`): mục `switch-steps` in p50/p95/max/mean và tỉ lệ trên mean; payload không mang mẫu → **`not collected`**, không bịa `0`.
- **Bằng chứng**: `npm run compile` exit 0; `native-tab-host-presented-view` **7/7** (có env → đúng 6 khoá hữu hạn; không env → không có row); `soak-payload-contract` **29/29** (field `[]` khi thiếu; ingest giữ switch-steps, vẫn bỏ `switched`, history nguyên); analyzer trên checkpoint thật in `not collected`; payload synthetic 1 row in bảng 6 bước. Verdict: `plans/reports/runtime-verification/real-soak-4h-verdict-perf4h.md`. **Không** chạy lại 4 h — cây app của user (pid 16884) còn sống; harness `rmSync` profile soak lúc launch.
- **Chưa đo, kèm lý do**: bước nào trả +5.6 ms — instrument có rồi, run abort trước khi nó có mặt trên bundle đang đo. Lần soak sống tiếp theo mới là phép đo.


### Sửa lỗi — Tab MCP trắng dù DOM sống; screenshot timeout / TARGET_BUSY_DRAINING
- **Triệu chứng (user, tab `46e999f1` levents.asia, không reload)**: OS screenshot pane trắng xóa; URL/title đúng; `document.readyState=complete`, 5587 node, innerText storefront. YouTube tab cạnh đó paint bình thường. `switchTab` recycle **không** lành. F5 thì hết.
- **Nguyên nhân (đo trên app sống)**: `captureScreenshot` viewport `Promise.race(capturePage, 600ms)` **bỏ** promise. Chromium không hủy raster → compositor WebContentsView chết, canvas guest `#ffffff` hiện ra. CDP timeout tiếp theo = `CAPTURE_TIMEOUT` / `TARGET_BUSY_DRAINING`. Recycle DirectComposition không hủy `capturePage` đang treo. Session MCP bind tab đã chết (`cd3887e0`) trả `CAPABILITY_NOT_FOUND` thay vì `TARGET_STALE`.
- **Sửa**: viewport capture dùng **một** `captureNativeViewportRaster` (không race-abandon, không retry `capturePage` thứ hai); timeout → CDP rồi `reassertPresentedView`. `reassertPresentedView` recycle tab đang trình bày **kể cả** khi attach-for-capture leak. Bound tab chết + explicit id trùng bound → failover / `TARGET_STALE`.
- **Bằng chứng**: Computer Use YouTube paint, Levents vẫn trắng sau bounce (chứng recycle không lành hung raster). Unit: `tab-devtools-host` 39/39 kể cả test 34 (đúng 1 `capturePage`); `native-tab-host-presented-view` 5/5 (leaked temp-attach vẫn recycle). Tab đang trắng **vẫn** cần **một** navigation để gỡ raster Chromium đã treo — code mới chặn wedge lần sau; process đang chạy chưa load bản này.


### Sửa lỗi — Tab trắng/đen sau MCP / DevTools dock dù DOM còn (F5 thì hết)
- **Triệu chứng (user báo kèm ảnh + Computer Use trên app sống)**: `bagamuioto.myharavan.com` vẽ **trắng xóa**; DevTools `mode: 'bottom'` → pane **đen** (`frameBackdropView` `#060910`) trong khi Elements vẫn thấy DOM; **đóng DevTools → trắng** (canvas `#ffffff`, không paint). F5 thì hết.
- **Nguyên nhân (đo trên cửa sổ `[DEV]` pid đang chạy)**: renderer sống, compositor WebContentsView chết. Dock bottom chiếm guest surface. `invalidate()` không sinh BeginFrame. Round-trip `getBounds()` ±1px **phá** visual. F5 lành vì `did-finish-load` → `updateLayout()`.
- **Sửa**: bỏ kick getBounds. `reassertPresentedView` recycle layer rồi `updateLayout()`. `toggleDevTools` mở `detach` (khớp context-menu Inspect). `devtools-closed` gọi reassert — đóng dock không để pane trắng. `show`/`restore` cũng `updateLayout`.
- **Bằng chứng**: Computer Use: đóng DevTools → trắng; F5 → PDP Bagamuioto paint. Unit `native-tab-host-presented-view` 4/4 (recycle, không còn `width-1`). Process đang chạy **chưa** có bản này — reload app.

### Sửa lỗi — Dark Mode AntiFan lộ xuyên storefront trên preset điện thoại bo góc
- **Triệu chứng (user báo kèm ảnh, không suy luận)**: trang collection `bagamuioto.myharavan.com` trên preset `xiaomi-14` có nền navy `#060910` quanh card trắng / filter bar chữ đen — đọc như Dark Mode của app đang nhuộm Web.
- **Nguyên nhân (đo trên tab sống)**: `html`/`body`/`main` nền `rgba(0,0,0,0)`, `color-scheme: normal`, **không** có `@media (prefers-color-scheme: dark)`. `applyTabDeviceEmulation` Case B đặt `view.setBackgroundColor('#00000000')` khi `getPresetCornerRadius > 0`. `applyDeviceCornerClipping` chỉ gỡ `#antifan-device-clip` (không còn punch góc). Single-tab ẩn phone chassis. Canvas trong suốt ⇒ `frameBackdropView` `#060910` lộ xuyên vùng trang không tô. `prefers-color-scheme: dark` vẫn true vì `nativeTheme.themeSource = 'system'` nhưng **không** restyle trang này.
- **Sửa**: Case B luôn `setBackgroundColor('#ffffff')` — khớp Case A (split) và canvas UA. Không tiêm CSS vào document (Appendix E / Shopify z-index âm).
- **Bằng chứng**: override thử `html, body, main { background: #fff }` trên tab thật khôi phục đúng storefront sáng; test `native-tab-host-viewport` khoá `#ffffff` cho **mọi** `DEVICE_PRESETS` kể cả `xiaomi-14` / iPhone bo góc.

### Bổ sung — Truy hồi Super Core: prompt thật hết bị nhiễu marker/số/từ chức năng, pack bám đúng project, và claim trả lời nhiều hơn xếp trước
- **Vấn đề (đo trên store thật, không suy luận)**: lớp truy hồi quote **mọi token trắng** thành term FTS, nên một prompt dán ảnh kèm đưa cả marker vào truy vấn: token `#1,` khớp **1977** claim, `[Image` **532**, `và` **432**, `bị` **65**. Hệ quả đo được: form AND (đúng) khớp **0** claim cho cả bốn prompt thật, buộc rơi xuống OR và dựng pool ứng viên từ toàn store (**2695 / 2754 / 607 / 593** dòng). Ba lỗi độc lập cùng lúc: (a) marker, số và từ chức năng đều là term; (b) FTS khớp **nguyên token**, nên `webcontent` (cách người viết gọi page renderer) khớp **0** claim trong khi dạng prefix khớp **68**; (c) pack **không mang scope project** — bridge biết `state.projectRoot` nhưng không truyền, nên claim của mọi project khác được xếp trước claim của chính repo đang làm.
- **Sửa — term** (`packages/super-core/src/terms.ts`): lọc marker `[…]` trên toàn chuỗi, bỏ token <2 ký tự / thuần số / không có chữ cái, bỏ **stopword** (danh sách đóng, có cả từ chức năng tiếng Việt), dedupe theo dạng đã fold, và phát term dưới dạng **prefix** (`"webcontent"*`). `đ` là **chữ riêng** (U+0111, NFD không tách) nên unicode61 giữ nguyên — đo được `"được"` **197** claim vs `"duoc"` **6** — vì vậy stopword cho các từ đó phải viết đúng dạng đã fold (`đuoc`, `đay`, `đang`, `đi`) kèm dạng không dấu; những từ mơ hồ với nội dung (`nen` = nền, `toi` = tối, `dau` = đau, `den` = đen, `dai` = dài) bị **loại khỏi** danh sách để không nuốt mất term thật. Prompt **không còn term nội dung** giờ trả về **rỗng**, không còn lấy "20 claim mới nhất" của toàn store (`text` rỗng/không truyền vẫn giữ nguyên hành vi cũ).
- **Sửa — scope** (`packages/super-core/src/index.ts`): `resolveScope(projectRoot)` khớp theo **thành phần path** (không phải tiền tố chuỗi, nên `apps/demo` không trả lời cho `apps/demo2`), ưu tiên **unit bao quanh** (một root lồng trong project vẫn thuộc project đó: `…\AntiFan\packages\super-core` → `apps/antifan`), chỉ khi root nằm **trên** mọi unit mới lùi về **hậu tố dài nhất** có unit dưới nó (nhờ đó `…\apps\AntiFan` không khớp nhầm unit tên `antifan-dogfood-clone`). `query()` nhận scope như **ưu tiên chứ không phải bộ lọc** — claim trong scope xếp trước, phần còn lại vẫn lấp chỗ trống nên project thiếu bằng chứng vẫn nhận kiến thức liên project — và `scopeTier` đi cùng phép chấm điểm lẫn thứ tự SQL; `contextPack` ghi `scopeKey` vào hàng `packs` **và** vào chính `packId` (cùng task ở hai root = hai pack, không tái dùng nhầm). `SCHEMA_VERSION` 10 → **11**, chỉ `ALTER TABLE packs ADD COLUMN scopeKey TEXT`; hàng cũ giữ NULL nghĩa là "định danh trước khi có scope".
- **Sửa — thứ hạng** (`terms.ts` + `index.ts`): `contentCoverage` đếm số term của yêu cầu mà statement mang được. Trong pool OR — nơi chỉ có ứng viên khớp **một phần** — claim trả lời nhiều term hơn xếp trước claim được chứng minh tốt hơn nhưng trả lời ít hơn; bm25 xếp theo độ hiếm token nên tự nó không làm được việc này.
- **Bổ sung — mặt MCP** (`scripts/antifan-omp-mcp.cjs`, `src/main/tools/core-capabilities.ts`): `scope` được khai ở **cả hai phía** cho `core.context_pack`, `core.context_pack_v2` và `core.recommend` — bảng proxy quảng cáo và catalogue đăng ký — vì cổng `check-mcp-budget-dominance` bắt schema `core.*` khớp **chính xác hai chiều** (proxy *là* implementation). Mô tả dedupe của `core.context_pack` cũng sửa `(task, platform, sessionId)` → `(task, platform, sessionId, scope)` cho khớp khoá pack thật. `core.recommend` nhận cùng tham số vì nó dựng pack bằng đúng `contextPack`, nên bỏ sót ở đó là cùng một defect hiện ra qua tên tool khác.
- **Sửa — tính hermetic của test** (`test/unit/context-bridge.test.mjs`, `test/unit/bridge-receipt-coverage.test.mjs`): hai suite chấm pack/refusal dựa trên giả định môi trường **không** mang policy anti-direct, nhưng policy đó **truyền theo biến môi trường** của tiến trình chạy test (đúng thiết kế: nó phải theo xuống subagent). Chạy từ một session anti-direct — chính harness này — làm `context-bridge` đỏ **20/27** và `bridge-receipt-coverage` đỏ **1/1** dù code không sai (đo được: pack biến thành `BRIDGE_CONTEXT_SKIPPED`, refusal biến thành `REFUSED_CORE_RETRIEVAL_POLICY`). Nay hai suite **xoá `ANTIFAN_ANTI_DIRECT` / `_ORIGIN` lúc nạp module** và thêm hai khoá đó vào `ENV_KEYS`, nên test nào muốn policy thì set tường minh; kết quả không còn phụ thuộc session chạy.
- **Sửa — bridge** (`.omp/hooks/pre/antifan-core-bridge.ts`): lệnh `pack` được truyền thêm `scope: state.projectRoot` (root đã resolve, **không** suy từ prompt). Trước đó pack chỉ mang `task`/`limit`/`platform`, nên toàn bộ tính năng scope không bao giờ tới được lượt tiêm context.
- **Bằng chứng (đo, không suy luận)**: `packages/super-core` — **37/37 test pass** (32 test cũ giữ nguyên xanh + **5 test mới**: term là chữ nội dung và phát dạng prefix gồm ca `'Đây nữa, và nhé'` ⇒ rỗng và ca `đ` giữ dấu; từ chức năng không truy hồi được claim, `webcontent` tới được `WebContentsView`, prompt không term **không** trả hàng mới nhất; claim mang nhiều term hơn thắng claim PROMOTED mang ít hơn trong pool OR; **prompt chỉ có marker ảnh `[Image #1, 1919x1039]` ⇒ 0 term và trả rỗng, không ném lỗi**; `resolveScope` theo root lồng nhau và theo hậu tố; pack hai root khác `packId` + `scopeKey` ghi đúng trong bảng `packs`). Đo **read-only trên store thật** với đúng prompt `Mất luôn chữ Google? [Image #1, 1568x882] WebContent bị gì rồi`: pool OR còn **191** dòng (trước **2695**), trong đó **64** thuộc scope `apps/antifan`; `resolveScope('E:\Work\apps\AntiFan')` ⇒ key `apps/antifan` + **17** unit; 12 hàng đầu trong scope là claim về `WebContentsView`/Google auth và **2** hàng mang cùng lúc 2 term nội dung. Migration 10→11 chạy trên **bản sao store thật**: version `11`, cột `scopeKey` có mặt, **259** hàng `packs` giữ nguyên. `npx tsc` trên file hook: **không phát sinh lỗi mới** (chỉ còn lỗi `Promise.withResolvers` cần lib es2024 — có sẵn từ trước; file hook không nằm trong `tsconfig.json` của repo nên chỉ kiểm được cú pháp/kiểu cục bộ). **Nối dây bridge → CLI → Core được chấm bằng test, và test đó đỏ trên bản pre-fix**: `test/unit/context-bridge.test.mjs` **27/27 pass** (26 case cũ + **1 case mới**): DB tạm được seed bằng **CLI thật** (`antifan-core.cjs import` một fixture 2 unit, `apps\AntiFan` và `themes\har`), hai claim mang **cùng** ba term của prompt, claim trong scope **yếu hơn** (OBSERVED, 1 evidence) còn claim project khác **mạnh hơn** (PROMOTED, 3 evidence) — khẳng định claim của project gọi **đứng trước** trong message được tiêm và **cả hai** vẫn có mặt (scope là ưu tiên, không phải bộ lọc); bỏ `scope` khỏi lệnh `pack` ⇒ case này **fail** (`pass 0 / fail 1`) còn code khác không đổi. **Lane mjs đầy đủ**: `node --test --test-force-exit "test/unit/*.test.mjs"` — **590 test, 585 pass, 0 fail, 5 skip, exit 0**. **Cổng MCP**: `node scripts/check-mcp-budget-dominance.mjs` **OK** (129 tool quảng cáo đối chiếu catalogue; 52 entry `core.*` có dispatch; sau `npx tsc -p ./` exit 0).
- **Chưa sửa, kèm lý do (không bỏ im lặng)**: `findSimilar` (được `pack-v2` dùng) vẫn quét **toàn store** — nó là truy vấn *tương tự*, không phải tập claim được tiêm, nên chưa gắn scope trong lượt này; muốn gắn thì phải thêm tham số cho **cả sáu** collection (claims, cases, decisions, anti-patterns, workarounds, fix patterns) chứ không chỉ `claims`, nên làm một nửa sẽ tạo contract nửa vời. `contextPackV2` vì vậy có thể mang thêm case/pitfall của project khác trong các mục phụ, trong khi `claims` — phần được tiêm và được chấm — đã theo scope.

### Sửa lỗi — Mất chữ Google trên `google.com`: nền canvas bị tiêm vào document, không phải lỗi tải ảnh
- **Triệu chứng (user báo kèm ảnh chụp thật, không suy luận)**: trang `google.com` **mất hẳn logo "Google"** (khoảng trắng trên ô tìm kiếm) trong khi ô tìm kiếm và các nút vẫn **tối**, nền trang lại **trắng** — đúng câu hỏi "Mất luôn chữ Google? WebContent bị gì rồi".
- **Nguyên nhân (truy theo git + quy tắc CSS, không đoán)**: `aab58ecf` (fix cho theme Shopify Horizon) tô nền canvas bằng **CSS tiêm vào chính document** — pseudo-element `html::before` màu trắng ghim ở z-index âm nhất. Nhưng CSS 2.1 Appendix E chỉ cho canvas **thừa hưởng** nền của `body` khi `html` **không** khai nền riêng; một pseudo-element của root nằm **trên** phần nền được thừa hưởng đó. Ở chế độ tối, Google đặt nền trên `body` (`rgb(34, 36, 42)`) và vẽ logo bằng **SVG trắng** ⇒ logo trắng nằm trên lớp phủ trắng nên **vô hình**, còn các control Google tự vẽ vẫn tối ⇒ ra đúng ảnh chụp (nền trắng, control tối). Lỗi thuộc **quy tắc lan nền**, không phải ảnh/font bị chặn.
- **Sửa**: trả việc tô nền về **lớp native** thay vì document — `view.setBackgroundColor('#ffffff')` khi tạo view và khi khôi phục sau crash, xoá hẳn `DEFAULT_CANVAS_CSS`, và capture tier-2 dùng `Emulation.setDefaultBackgroundColorOverride` màu trắng (teardown trong `finally`); `462a48b4` bỏ nốt override nền ở đường capture vì đường đó không cần nữa. Nền do site khai vẫn lan xuống canvas đúng chuẩn CSS vì nền native **không phải** một CSS background của root — đúng điều theme Horizon cần, mà không cần lớp phủ nào trong document.
- **Bằng chứng (đo, không suy luận)**: commit `a25d9f99` + `462a48b4`; **kiểm tra trực tiếp trên app đang chạy** (tab nền, DOM thật): logo `svg` **tồn tại**, `visibility: visible`, `opacity: 1`, khung **272×92**, `document.elementFromPoint` tại tâm logo trả **chính logo** (không lớp nào phủ), `html::before` và `body::before` đều `content: none` (hết fill), `body` nền `rgb(34, 36, 42)` do chính Google đặt; tab kiểm tra đã đóng sau khi đo. `npm run compile` **exit 0** (emit-integrity + budget-dominance + payload fixture).

### Sửa lỗi & Bổ sung — Theme Studio & QA Cockpit: hết cắt chữ / hết co thẻ, cuộn được, và CRUD đầy đủ cho mục kiểm tra
- **Triệu chứng (user báo trên panel thật, không suy luận)**: (1) danh sách checklist **không cuộn được** và chữ bị **cắt** ở đáy panel; (2) không thể **thêm / sửa / xóa** mục kiểm tra — checklist là danh sách cứng 44 mục.
- **Nguyên nhân (đọc CSS, không đoán)**: `.theme-studio-modal` là flex column `overflow: hidden` với `height: min(88vh, 840px)`, nhưng `.theme-studio-body` và `.theme-studio-tab-content` khai `height: 100%` — một chiều cao **theo cha** trong khi cha đã bị `max-height: 90vh` chặn, nên tổng chiều cao vượt khung và phần dôi ra bị `overflow: hidden` **cắt**, cuộn không bao giờ tới được. Trong danh sách, `.theme-phase-card` không có `flex-shrink`, nên là flex item của cột nó bị **co lại** theo khung ⇒ nội dung bị `overflow: hidden` của chính thẻ cắt cụt.
- **Sửa — layout** (`src/renderer/toolbar.css`): `.theme-studio-body` / `.theme-studio-tab-content` đổi từ `height: 100%` sang `flex: 1 1 auto` + `min-height: 0` + `max-height: 100%` (chiều cao suy từ **không gian còn lại**, không từ một tỉ lệ của cha đã bị chặn), `.theme-phase-card` thêm `flex-shrink: 0` + `min-height: fit-content` (thẻ cao theo nội dung, không bị ép), `.theme-checklist-scroll` giữ `flex: 1 1 auto` + `min-height: 0` + `overflow-y: auto !important` kèm scrollbar tuỳ biến và `overscroll-behavior-y: contain`. Chữ dài trong mục không còn tràn: `.theme-item-name` / `.theme-item-desc` / `.theme-item-qa-point` nhận `word-break: break-word` + `overflow-wrap: break-word`, `.theme-item-qa-point` thêm `max-width: 100%`.
- **Bổ sung — CRUD đầy đủ** (`src/renderer/toolbar.html`, `src/renderer/toolbar.ts`): modal `#themeItemEditOverlay` (Nhóm trang / Mã / Tên / Mô tả / Điểm QA / Route) với `#btnThemeItemAdd` ở thanh công cụ, nút `+ Thêm` trên **từng thẻ trang**, và nút `✏️ Sửa` / `🗑️ Xóa` trên **từng mục**; xóa có `confirm`. Mã mục được **sinh theo tiền tố trang** (`HOM-`, `PDP-`, `QAG-`…) và tự nhảy số kế tiếp, đổi dropdown trang thì mã đổi theo (chỉ khi đang tạo mới, không đè khi đang sửa).
- **Bổ sung — lưu trữ** (`src/renderer/toolbar.ts`): checklist trước đây chỉ lưu **cờ `done`** (`Record<id, true>`), nên mục thêm tay không có chỗ tồn tại. Nay có khoá thứ hai `${prefix}_custom_items:<scope>` lưu **toàn bộ danh sách mục** cho **đúng scope** (origin + workspace) đang mở; `loadThemeChecklist` hợp nhất danh sách đã lưu với cờ `done`, thiếu khoá danh sách thì rơi về `DEFAULT_THEME_CHECKLIST`; nút Reset xoá **cả hai** khoá. Mục mới cũng vào báo cáo Markdown xuất clipboard.
- **Bổ sung — thu gọn thẻ trang**: bấm header thẻ để thu/mở (chevron ▶/▼), trạng thái thu giữ theo phiên; trang rỗng có dòng nhắc kèm nút thêm mục đầu tiên; Escape đóng modal (thêm vào chuỗi xử lý `keydown` sẵn có, đứng trước `themeQaOverlay`).
- **Bằng chứng (đo, không suy luận)**: `npm run compile` **exit 0** (emit-integrity 403 file + extension bundle + payload fixture); `test/renderer/theme-checklist-studio.test.js` **15/15 pass** (13 case cũ giữ nguyên xanh + **2 case mới**: thu/mở thẻ trang, và CRUD đầy đủ thêm → sửa → xóa kèm kiểm tra số dòng render trở về đúng 44). Trong pipeline `test:fast` (1377 test) nhánh renderer xanh toàn bộ; **1 test đỏ duy nhất** là `test/main/bridge-pairing-queue-concurrency.test.js` — test main-process về hàng đợi pairing, **không dùng chung module nào** với `src/renderer/toolbar.*`, và đỏ vì `waitFor` 30 s hết hạn dưới tải song song của cả suite (fail độc lập với thay đổi này).

### Sửa lỗi — Instrument soak: sáu defect đã xác minh (leg, CPU theo role, cổng đọc sai) và một defect app (store history)
- **Vấn đề (đo trên run, không suy luận)**: một run soak chỉ hữu ích bằng thứ nó ghi lại được, và sáu điểm đã làm số ghi ra **đọc sai** — mỗi điểm có bằng chứng riêng trong `plans/260920-0100-soak-4h-workflow-performance/plan.md` (mục *Harness findings*). Nặng nhất: một leg **chưa chạy xong** vẫn báo `observedMinutes` bằng **đúng số phút đã lên lịch** (60 phút trong khi mới quan sát 29), một lượt switch đầu tiên của leg mới bị đặt nhịp theo **interval của leg trước**, và một pid không có role bị in là `unattributed` **kể cả khi nó là chính main process của run** (`rootPid`) — đúng tiến trình tốn CPU nhiều nhất.
- **Sửa — harness** (`scripts/benchmark-real-soak-8h.cjs`): (1) `nextBurstTime` được đặt lại theo interval **của leg mới** lúc vào leg, nên burst đầu của một leg không còn theo nhịp của leg trước; (2) leg **đang mở** được đóng tại **biên phase thật** (`workloadEndTime`, hoặc lúc lỗi/kết thúc) và mang cờ `open`, còn `observedMinutes` của leg mở được tính từ **mốc dữ liệu cuối cùng mà phép fit thực sự dùng** — không bao giờ lấy span đã lên lịch; (3) cửa sổ fit của `calculatePerProcessSlopes` được **mang vào payload** (`processSlopeWindowStart/End`) và một cửa sổ không suy ra được thì **trả rỗng** thay vì lặng lẽ fit cả run rồi dán nhãn "workload"; (4) row không có role nhận **`type`** làm role, nên không còn dòng role rỗng; (5) `historySamples` được mang ra payload root cho mục history của analyzer; (6) comment trên `switchSamples` nói điều dữ liệu cho thấy (giả thuyết "đụng scrape 60 s" đã bị bác: 1/10 lượt chậm nhất nằm trong 3 s của một scrape và nó là lượt **nhỏ nhất**).
- **Sửa — analyzer** (`scripts/analyze-soak-app-only.cjs`): pid `rootPid` được in là **`browser (main)`** thay vì `unattributed`; mục leg in **CPU theo từng pid** (không chỉ dòng top) và thêm mục **history flush** (count, p50/p95/max, tổng, giá theo 1000 dòng và theo switch — payload không mang mẫu thì in **`not collected`**, không bịa `0`); leg **suy diễn** được neo vào **sample workload đầu tiên quan sát được** thay vì `startedAt + warmup` (và in rõ đã dùng neo nào); cổng app-only in **`deciding reading` = per-process max** cạnh tổng — đo trên checkpoint: pid 29832 `0.21 MB/min` (**BREACH** 0.15) trong khi **tổng** của 5 tab app là `0.44` và từng renderer phẳng/giảm có thể che đúng tiến trình đang leo.
- **Sửa — app** (`src/main/browser/history-manager.ts`): `updateTitle` với **đúng title cũ** trả về sớm — không bump `mutationVersion`, không arm debounce, nên một trang tự đổi title về giá trị cũ không còn kéo theo việc ghi lại store ~3.7 MB; `persistSync` **đặt lại `persistArmedAt`** sau khi flush đồng bộ, để mutation kế tiếp **debounce** thay vì đụng trần và flush ngay.
- **Sửa — regression do chính đợt này suýt gây ra (đã bắt khi review diff, không để lọt)**: ba dòng `stateMeta.requestCount/switches/bursts` ở **đường ghi report cuối** bị xoá nhầm (chúng chỉ còn được gán trong `saveCheckpoint()`), nên payload cuối sẽ đọc counter của **checkpoint cũ** — hụt tới một chu kỳ checkpoint switch/burst của run đang được chấm. Đã khôi phục kèm comment nói vì sao phải đọc biến sống tại đó, và bỏ luôn comment + `finishedAt` bị lặp.
- **Bằng chứng (đo, không suy luận)**: `test/unit/soak-payload-contract.test.mjs` **24/24 pass** — và **5/5 case mới đỏ trên bản pre-fix** (chạy `git show HEAD:scripts/benchmark-real-soak-8h.cjs` vào file tạm rồi chấm chính các case đó: `open` undefined, `observedMinutes` 60 thay vì 10, role rỗng thay vì `Browser`, thiếu window history); `test/unit/soak-analyzer-cpu.test.mjs` **14/14**; `test/unit/soak-latency-gate.test.mjs` + `soak-launch-args.test.mjs` xanh; `node --check` sạch cả hai script; `npm run compile` **exit 0** (emit-integrity + extension bundle) và `test:main` trên file đã compile: **7/7** gồm hai case mới của store history (ghi lại: `unchanged title updates must not trigger disk writes`); `node scripts/check-bottlenecks.mjs` **OK — every declared status matches HEAD**. `FREEZE_SLO`, `evaluateFreezeVerdict` và mọi ngưỡng **không bị đụng** trong đợt này (diff được lọc riêng để xác nhận).
- **Báo cáo kèm theo**: `plans/reports/runtime-verification/renderer-retention-lead-analysis-20260920.md` (refute đường terminal bằng chính các trần trong code: `scrollback: 10000`, `MAX_RECOVERY_QUEUE_BYTES`/`MAX_RECOVERY_QUEUE_CHUNKS`, `MAX_HYDRATION_WRITE_CHARS` — mọi cấu trúc renderer đều bị chặn, nên độ dốc tuyến tính phải đến từ tầng engine/allocator; legs 2–3 chưa chạy nên dữ liệu hiện có **không** phân biệt được) và `.../switch-tail-spike-analysis-20260920.md` (đuôi là **điều kiện lặp ở steady-state**, không phải outlier: 22/1143 lượt > 35 ms, 8/10 lượt chậm nhất nằm trong workload, mọi destination đều chạm 36–45 ms, khoảng cách trung vị ~105 s, và cả phân bố dịch ~2.5× — dịch **bên trong** `NativeTabHost.switchTab()` theo telemetry của app, `tabs.layout` vẫn < 0.2 ms).
- **Chưa sửa, kèm lý do (không bỏ im lặng)**: `updateAffinityBadges` vẫn gọi `api.getTerminalAffinities()` mỗi lượt broadcast 5 Hz (~72.000 lượt/4 h) vì **không** có kênh push cho affinity. Cache ở renderer đã bị **từ chối có bằng chứng**: phía main còn writer ngoài tầm renderer (`native-tab-host.ts:1667` revive, `:6221` xoá entry, `:7073-7120` cập nhật theo tab), nên một cache không chứng minh được là tươi và cái giá của nó là badge **sai** — đắt hơn phần CPU tiết kiệm. Đường đúng là mang affinity vào payload broadcast như danh sách tab (không có cửa sổ cũ), việc đó đụng payload contract của main nên để thành việc riêng.

### Bổ sung — Soak: đọc được leg **giữa run** (bisect có kết quả sớm một chặng) và đọc **toàn bộ chuỗi switch** (đuôi latency quy được chủ)
- **Vấn đề (đo, không suy luận)**: hai điểm mù của instrument đều làm mất thời gian làm việc. (1) `legSlopes` chỉ được ghi khi harness kết thúc, nên một leg đã chạy xong vẫn **không đọc được** từ payload — câu trả lời của bisect ("chi phí theo **dòng** hay theo **thời gian**") đến muộn 2 tiếng rưỡi so với lúc nó đã có thể có. (2) Harness gom **mọi** lượt switch vào `switchSamples` (`{at, ms, tabId, phase}`) rồi **bỏ** cả chuỗi khi dựng report, chỉ giữ 10 lượt chậm nhất: mười dòng nói được "có đuôi", không nói được **cái gì tạo ra đuôi** — một stall do đụng đường ghi terminal và một stall không liên quan cho ra **cùng** top-10.
- **Sửa — phía đọc** (`scripts/analyze-soak-app-only.cjs`): khi payload không có `legSlopes`, mục **leg bisect** suy cửa sổ leg từ chính schedule của run (`config.legs` + `startedAt` + `warmupMinutes`) và ghi rõ **`PROVISIONAL`** ở tiêu đề, ở từng dòng leg và ở dòng hướng dẫn đọc: biên đó là **theo lịch**, không phải **quan sát được**, nên một lần máy sleep hay một phase bị trễ sẽ dịch chúng; số burst quan sát được là **chưa biết** nên cột B/1000 dòng in `n/a` thay vì tính từ một con số bịa. Cột per-line chỉ in số khi leg đã chạy **≥ 1000 dòng** quan sát được (dưới ngưỡng đó thương số là một số rất lớn trông như một phép đo; leg `burst-off` 3 dòng từng in `-699050667`). Payload cuối (`legSlopes` quan sát được) thay thế bản đọc suy diễn này.
- **Sửa — phía ghi** (`scripts/benchmark-real-soak-8h.cjs`): payload mang **toàn bộ chuỗi switch** (trần 20.000 dòng — 4 h ở nhịp 3 s là ~4.800 — kèm `switchSamplesDropped` bên cạnh trần, để một chuỗi bị cắt không bao giờ đọc như một chuỗi đầy đủ), mỗi dòng được đóng dấu **leg** đã chạy (`baseline#1`, `burst4x#2`, …) và phase; leg mang thêm `ordinal` vì một bisect quay lại regime cũ sẽ trộn leg 1 với leg 3 trong mọi bảng khoá theo tên. `buildReportPayload` được export để test chấm **chính hàm thật**.
- **Bổ sung — mục `switch tail`** trong analyzer: recompute **độc lập** quantile workload và **nói thẳng** là nó *khớp* hay *mâu thuẫn* với gate đã báo (một gate chấm một chuỗi khác là lỗi im lặng); số stall theo phase và **theo leg** (chính là A/B khối lượng ghi terminal trong **cùng một run**, cùng tập trang, cùng nhịp switch); vị trí từng stall trong chu kỳ lấy mẫu 60 s của chính harness, in cạnh phân bố của **mọi** lượt switch, để một cụm chỉ là nhịp scrape của harness thì lộ ra là cụm.
- **Sửa — hai field/tên nói sai điều nó đo** (`scripts/benchmark-real-soak-8h.cjs`): (1) `stats.activeWorkloadMinutes` cũ là *elapsed-since-start trừ suspend*, nên run 4 h đọc `10` khi mới vào phút thứ 10 của warmup 30 phút — nay field này là **span thật của workload phase**, đo từ mốc `activeAt` (đã trừ suspend) của chính các sample workload, và giá trị cũ được giữ dưới tên đúng là `activeMinutesSinceStart`. Phase chưa chạy ⇒ `null` (chứ không phải `0`, vì "chưa đo" và "đo được 0 phút" là hai sự kiện khác nhau). (2) Comment trên `switchSamples` mời gọi đúng giả thuyết mà dữ liệu của run 4 h đã bác (1 trong 10 lượt chậm nhất nằm trong 3 s của một scrape và nó là lượt **nhỏ nhất**) — nay comment nói điều dữ liệu cho thấy và nói chuỗi đầy đủ cho phép tính bằng **số đếm** thay vì tranh luận từ mười mẫu.
- **Bằng chứng (đo, không suy luận)**: `test/unit/soak-payload-contract.test.mjs` gọi **chính** `buildReportPayload` — chuỗi đầy đủ được mang (120/120), trần + số bị bỏ (25.000 ⇒ 20.000 + 5.000), top-10 vẫn xếp theo ms chứ không theo thứ tự tới, series rỗng ⇒ `[]` chứ không mất field, meta thiếu `switchSamples` không ném lỗi, span workload = **60** khi elapsed-since-start là 4.9 (đỏ nếu ai đó quay lại đọc field cũ), span trừ suspend (raw 33.3 phút ⇒ **10** phút), phase chưa chạy ⇒ `null` — **8/8 pass**, cộng `test/unit/soak-latency-gate.test.mjs` **6/6 pass** (tổng 14/14). Fixture analyzer: leg suy diễn trên dữ liệu workload thật của run 4 h (3 leg, frame thật, `PROVISIONAL`, per-line `n/a`), leg quan sát với số burst chèn tay (per-line tính được, và bị chặn dưới 1000 dòng), checkpoint của run đang chạy (3 cửa sổ suy diễn, `no usable window (0 frames)` — đúng, workload bắt đầu 07:04), và ba đường của `switch tail` (khớp gate / mâu thuẫn gate / artifact cũ không có chuỗi). Run leg đang chạy **không** có các instrument này (đã nạp code lúc 06:34, sửa lúc 06:52-07:02) — đuôi của nó đọc từ 10 dòng chậm nhất và giới hạn đó được ghi trong verdict, không được im lặng bỏ qua.
- **Bổ sung — probe phân loại retention** (`scripts/probe-renderer-retention-class.cjs`): bước kế tiếp của finding retention (pid 47536) là heap diff, nhưng một V8 heap snapshot vừa **buộc GC** trong đúng tiến trình đang được đo, vừa **chỉ thấy object V8** — nên nó không trả lời được nghi phạm còn lại (phía Blink: node của scrollback, attribute/string table), thứ hiện ra ở `nodes` chứ không ở JS heap. Probe gắn vào DevTools endpoint của app đang chạy và in **loại** bộ nhớ đang leo theo **từng document**: `nodes`/`documents`/`jsEventListeners` là số đếm chính xác (leo đơn điệu = retention, phân loại theo ngưỡng/phút), `JSHeapUsedSize` là răng cưa nên cần **cả** biên độ **và** tỉ lệ bước tăng mới gọi là leo, còn `RecalcStyleCount`/`LayoutCount` là counter công việc tích luỹ nên in **rate** thay vì phân loại. Counter nào protocol từ chối thì ghi `unsupported` và document đó là `UNMEASURED`, **không** ghi 0 — số 0 đọc ra như một chuỗi phẳng khoẻ mạnh.
- **Bổ sung — `SOAK_APP_ARGS`** (`scripts/benchmark-real-soak-8h.cjs`): probe cần app mở DevTools endpoint, nên harness truyền thêm switch vào argv (`SOAK_APP_ARGS="--remote-debugging-port=0"`) và **ghi lại** argv đó (log lúc launch + `config.appArgs` trong payload), vì một run mang endpoint debug mà không được ghi thì số của nó sẽ bị đọc như baseline sạch. Mặc định rỗng ⇒ mọi run đo không mang thêm switch nào.
- **Bằng chứng (đo, không suy luận)**: probe `--self-test` **11/11** (endpoint giả: ba document leo theo ba kiểu — `DOM_NODES` 600 node/phút, `EVENT_LISTENERS` 30 listener/phút, document phẳng ⇒ `FLAT`; heap răng cưa −10 MB rồi +4.5 MB **không** được gọi là leo; document không đọc được counter ⇒ `UNMEASURED`), **đối kháng đã kiểm**: làm phẳng chuỗi nodes trong **bản sao** của fake ⇒ self-test **fail đúng chỗ** (`JS_HEAP` thay vì `DOM_NODES`), bản gốc hoàn nguyên ⇒ 11/11. `SOAK_APP_ARGS`: `test/unit/soak-launch-args.test.mjs` **4/4** (rỗng/khoảng trắng ⇒ `[]`; giữ đúng thứ tự token; giá trị có quote bị **cảnh báo** và trả nguyên văn thay vì tách im lặng; mặc định rỗng khi không set) và `test/unit/soak-payload-contract.test.mjs` ghim `config.appArgs` trong payload. Ba suite của script soak: **19/19 pass**.
- **Bằng chứng cho việc đổi tên field (số sống)**: chính checkpoint của run leg đang chạy (harness nạp lúc 06:34, trước fix) đọc `stats.activeWorkloadMinutes: 20` khi `samples` chỉ có **21 frame warmup** và workload chưa bắt đầu — đúng cái tên nói sai điều nó đo; run này không mang fix nên verdict của nó phải đọc field đó theo nghĩa cũ.

### Sửa lỗi — Renderer chrome tự leo bộ nhớ theo giờ: mỗi lượt broadcast state tốn thêm một vòng RPC và 48 lượt parse selector
- **Triệu chứng (đo trên soak, không suy luận)**: soak 4 h (`scripts/benchmark-real-soak-8h.cjs`, 240 phút, 6 tab + 1 terminal, broadcast state tới 5 Hz) cho thấy tiến trình renderer chrome (pid 47536 — toolbar + sidebar terminal + backdrop là **một** site `file://`) leo **+0.17 MB/phút** theo bản private (committed) suốt 178 phút workload: 84 → 99.7 MB, đơn điệu ở mọi cửa sổ trượt (0.167 → 0.162 → 0.159), chỉ nhả **84 %** khi lượt teardown đóng view. Gate đang đặt 0.15 MB/phút ⇒ một phiên dài kết thúc bằng restart, và restart là mất toàn bộ tab + terminal đang sống.
- **Nguyên nhân**: hai consumer chạy trên **mỗi** lượt broadcast, cả hai nằm trong đúng tiến trình đó. (1) `api.onTabsUpdated((tabs) => …)` (`src/renderer/standalone.js:4692`) đã nhận danh sách tab, nhưng `updateAffinityBadges()` ngay dưới nó gọi lại `api.getTabs()` cho **cùng** dữ liệu — hai bên dựng từ cùng `tabHost.getTabList()` (`bridge-server.ts:2675` cho RPC, `native-tab-host.ts:7787` cho broadcast) ⇒ thêm một `invoke` mỗi lượt, kèm correlation entry, promise và mảng tab deserialize trên **main thread** — đúng luồng mà mọi lượt switch, bridge RPC và terminal fanout cũng chạy; đo được ~106.800 lượt trong 4 h. (2) `renderTabs()` (`src/renderer/toolbar.ts`) dựng mỗi tab bằng `innerHTML` rồi mỗi lượt render lại **query 8 selector cho mỗi tab** — 7 con (`.tab-index-badge`, `.tab-spinner`, `.tab-icon`, `.tab-status-dot`, `.tab-title`, `.tab-audio-btn`, `.tab-agent-badge`) cộng `[data-tab-id="…"]` — và vì chữ ký render có `title`, fixture đổi tiêu đề 200 ms/lượt đẩy nó tới 5 Hz: **48 lượt parse selector mỗi lượt render**.
- **Sửa**: `updateAffinityBadges(deliveredTabs)` nhận danh sách từ broadcast khi caller đang giữ nó (đường `onTabsUpdated`), chỉ tự fetch khi không có; 5 call site còn lại (picker/category/session) giữ nguyên vì chúng không giữ list. `renderTabs()` cache ref của 7 con theo **từng element** bằng `WeakMap` (`tabRefsCache` + `cacheTabRefs`, cùng convention với `computeTabsSignature`/`computeBookmarksSignature` trong file), điền lazy ở lượt render đầu của element đó; lượt tra tab theo id đổi từ `tabList.querySelector('[data-tab-id="…"]')` mỗi tab sang **một** lượt quét `children` + `getAttribute`.
- **Bằng chứng (đo, không suy luận)**: probe DOM thật (jsdom + bundle đã emit — `scripts/probe-toolbar-render-cost.cjs`) chạy trên chính `toolbar.html`/`toolbar.js` của bản build: lượt render đầu = **54** `querySelector` (42 = 7 con × 6 tab khi điền cache, + 6 `.tab-close`, + 6 `.tab-audio-btn` để dựng handler) và lượt render steady-state (đúng đường 5 Hz) = **0** `querySelector`, chỉ 12 lượt `getAttribute` đọc trên danh sách children đã materialize; thêm 1 tab = 9 (7 điền cache + 2 handler), xoá 1 tab = 0. Cùng probe khẳng định cache không làm cũ nội dung: đổi title ⇒ `.tab-title` = `RETITLED`, bật agent ⇒ badge `inline-flex`, `isLoading` ⇒ spinner `inline-block`, `isAudible` ⇒ nút audio `inline-flex`. Hợp đồng "đưa list vào thì không fetch lại" được ghim trong `test/renderer/terminal-tab-categories-sleep.test.ts` (hai nguồn được đặt **title khác nhau**, badge phải đọc bản delivered) và **kiểm chứng bằng đột biến** trên bundle mà harness thật sự nạp (`.compiled/src/renderer/standalone.js`): trả `api.getTabs()` về nguyên trạng ⇒ **46 pass / 1 fail** đúng ca "a delivered tab list must not be re-fetched"; hoàn nguyên ⇒ 47/47 xanh. `npm run compile` exit 0 (emit-integrity 403 file) và `npm run test:fast` **1371 test / 0 fail**.
- **ĐÍNH CHÍNH (đo lại, không suy luận) — độ trôi KHÔNG do hai đường này**: preflight 90 phút trên **bundle đã sửa** (`SOAK_REPORT_TAG=preflight-fixed`, cùng driver rate, cùng role `chrome:standalone+chrome:toolbar+chrome:frame-backdrop`, cùng cửa sổ workload, cùng cột private bytes) cho độ dốc **0.2234 ± 0.0070 MB/phút** (82.43 → 94.13 MB, +11.70 MB / 58 phút), tức **cao hơn** mốc trước sửa **0.1699 ± 0.0010 MB/phút** (85.48 → 117.54 MB, +32.06 MB / 179 phút) — chênh +0.0535 MB/phút (~7.5σ). Nên kết luận "renderer chrome tự leo bộ nhớ theo giờ" **không** còn được gắn với hai fix broadcast: hai fix được biện minh bằng **công việc mỗi lượt broadcast đã bị xoá** (probe đếm mutation, mục dưới), **không** bằng độ trôi. Trạng thái đo được của độ trôi: **tuyến tính và ổn định** suốt cửa sổ (hiệp hai 0.209 ≈ hiệp một 0.243; floor rolling-min 10 frame vẫn leo +2.79 MB trong 20 phút cuối) ⇒ không phải buffer có trần đầy rồi phẳng (xterm scrollback = 10.000 dòng, `src/renderer/standalone.js:2034`, nếu là scrollback thì phải phẳng sau ~16 phút — đo được là không). Hai run khác bundle nhưng **cùng chi phí trên mỗi dòng output terminal**: 298 B/dòng (4 h) và 336 B/dòng (preflight) ⇒ nghi phạm chuyển từ đường broadcast sang **đường ghi terminal**; confound còn để mở và nói rõ: cả hai run đều ~10 dòng/giây nên một driver tỉ lệ với **thời gian** cũng khớp, và đó là câu hỏi đang được trả lời bằng bisect trong cùng một run (`SOAK_LEGS`, mục Phase 4c của plan `260920-0100`), không bằng thêm một run 4 h nữa. Cửa sổ "tạo session terminal lúc boot" (1 PTY ~83 MB) **không** phải chỗ sửa: `#terminal` là container luôn hiện, không có đường thu gọn nào trong renderer, và chính call đó là đường **khôi phục** session đã lưu (`TerminalManager.startTerminal` → `readSavedSessions`), nên bỏ nó sẽ làm biến mất terminal của người dùng.

### Bổ sung — Soak: bisect trong **cùng một run** (nhiều chặng, một biến) và cổng độ dốc biết mình không chấm được
- **Vấn đề**: độ dốc đo trên cả workload phase chỉ nói *rằng* một tiến trình leo, không nói **driver** nào làm nó leo — host load, tập trang và công việc nền đều đổi giữa hai run, nên so hai run khác nhau luôn để mở confound "chi phí theo **dòng**" so với "chi phí theo **thời gian**" (run 4 h và preflight đều ~10 dòng/giây nên cả hai giả thuyết khớp số liệu y hệt).
- **Sửa** (`scripts/benchmark-real-soak-8h.cjs`): thêm `SOAK_LEGS="tên:phút:burstLines:burstIntervalMs,..."` chia **workload phase** thành các leg liên tiếp, mỗi leg một mức burst. Report mang `config.legs` (schedule), `legs` (mốc chuyển leg **quan sát được** trong lúc chạy, kèm số burst **quan sát được** của từng leg) và `legSlopes` (độ dốc theo **từng leg**, cửa sổ lấy từ mốc thực tế, kèm `topSlopes` cho verdict trên terminal và `slopes` — bảng xếp hạng **đầy đủ** theo committed private bytes, để truy **cùng một pid/role** qua các leg mà không phụ thuộc việc nó có nằm trong top 6 của leg đó hay không). Leg được khoá bằng **tên và cửa sổ** nên `baseline,burst4x,baseline` là ba leg chứ không phải một leg nuốt leg giữa. Khi có leg, độ dài run suy ra từ legs (`RUN_MINUTES`, không lấy `SOAK_DURATION_MINUTES` làm mốc để không thêm một đoạn không được gán cho leg nào), và **cổng độ dốc không chấm** run đó: `slopeSloSatisfied: null`, `slopeGateApplicable: false`, verdict `PASSED_SLOPE_NOT_GRADED` — một độ dốc gộp ba regime là số vô nghĩa, không phải kết luận; các cổng còn lại (peak memory, latency, orphan, execution, teardown) vẫn chấm bình thường.
- **Bằng chứng**: `node --check` sạch; lịch leg kiểm bằng tay trên đúng spec đang dùng (`baseline:60:300:30000,burst4x:60:1200:30000,burst-off:60:1:600000` ⇒ workload 180 phút, run 240 phút, biên 30/60/90/150 phút, 60 frame mỗi leg ở chu kỳ lấy mẫu 60 s), và run thật phải in `[soak] Leg N: …` ở đúng biên cùng `config.legs`/`legSlopes` trong report; kết quả bisect ghi vào `plans/reports/runtime-verification/real-soak-4h-verdict-20260920.md` §11 khi run kết thúc. Phía đọc có `scripts/analyze-soak-app-only.cjs` in thêm mục **leg bisect**: mỗi leg một bảng per-pid (private first→last, độ dốc ±1se, độ dốc của **floor**, và B/1000 dòng suy từ số burst **quan sát được**), rồi bảng **cross-leg** truy cùng một identity qua ba leg — đã kiểm bằng cách chia cửa sổ workload của run 4 h thành ba leg giả: cùng dữ liệu ⇒ cùng độ dốc (0.17 / 0.18 / 0.17 MB/phút cho pid chrome), tức bảng đo đúng thứ nó khai, và `B/1000 dòng` khớp ~250 B/dòng khi chia đúng số dòng thật của regime.

### Bổ sung — Soak: cổng latency chấm percentile, `max` chỉ để báo (quyết định của user)
- **Vấn đề**: cổng latency chấm `max <= 35 ms`. Run 4 h vượt ở **50.976 ms** trong khi p50 **10.346** / p95 **15.513** vẫn trong ngưỡng, và bảng theo destination cho thấy **cả 6 đích đều vượt** (38.10–50.98 ms, mỗi đích 682 lượt, p50 10.1–10.9) — tức một stall toàn cục ở **~0.2 %** của 4090 lượt switch (8 trong 10 lượt chậm nhất nằm trong workload, rải khắp run), **không** phải chi phí của một trang nặng và **không** phải artifact khởi động. Cổng vì vậy fail trên một sự kiện nó không quy được chủ, và mỗi run 4 h sau đó cũng sẽ fail y như vậy.
- **Sửa** (`scripts/benchmark-real-soak-8h.cjs`): cổng chấm **p50 ≤ 12 ms và p95 ≤ 18 ms** trên workload phase; `max` **được báo cáo, không chấm** (user chốt 2026-09-20, chọn đúng phương án này trên số của run 4 h). Cụ thể: xoá số hạng `switchLatencyMaxMs: 35` khỏi `FREEZE_SLO` (một ngưỡng nằm trong object SLO mà không ai chấm là bẫy cho người đọc sau), bỏ số hạng `switchMax <= …` khỏi `latencyOk` kèm lý do + ngày ngay tại chỗ, payload thêm `switchLatencyMaxMs` (số đo) và `latencyMaxGated: false` (hình dạng cổng) để **một ngưỡng bị bỏ** phân biệt được với **một ngưỡng được pass**, printer đổi `max=… (SLO <= 35ms)` thành `(reported, not graded)`. Phân phối đầy đủ vẫn nằm trong `switchLatencyMs` (`min/p50/p95/max`) nên không mất thông tin nào về đuôi.
- **Bằng chứng**: `test/unit/soak-latency-gate.test.mjs` (6 case) gọi **chính** `evaluateFreezeVerdict` xuất từ harness — không phải bản sao, vì một ngưỡng chép lại trong test thì pass trong khi production fail. **Đối kháng đã kiểm**: đặt lại đúng số hạng `switchMax <= 35` ⇒ 2 case đỏ (`max` 50.976 với p50/p95 trong ngưỡng, và `max` 900 khi p50/p95 đúng biên); hoàn nguyên ⇒ **6/6 pass**. Case "series rỗng không được pass" giữ bất biến cũ (`switchLatencyMs` thiếu ⇒ quantile null ⇒ `Infinity` ⇒ fail, không pass nhờ fallback `0`). Entry point không đổi: `main()` được bọc trong `if (require.main === module)`, thân `main().catch(…)` giữ nguyên từng chữ; **không** chạy thử harness bằng lệnh thật vì nó `rmSync` chính profile của run đang chạy — thay bằng probe chứng minh ngu nghĩa guard (`node file` ⇒ chạy `main`, `require(file)` ⇒ không) cùng test unit (require trả 2 export và không khởi động run).

### Bổ sung — Soak: telemetry bộ nhớ theo từng tiến trình kèm role (benchmark mode, không ảnh hưởng runtime)
- **Vấn đề**: `app.getAppMetrics()` gộp theo *process type* — mọi renderer đều là `Tab`, nên rò rỉ ở window-chrome và ở page-renderer **không phân biệt được**, verdict 2 h kết thúc bằng "điều tra renderer creep" mà không có chủ sở hữu; Electron còn báo mọi page `WebContentsView` là type `window`, cùng type với app chrome.
- **Sửa** (`src/main/index.ts`): `describeLiveWebContentsRoles()` gắn pid → role, lấy tên tab từ tab host (`getTabWebContents` theo từng pane, offscreen/mobile được đánh dấu) và chỉ `mainWindow.webContents` là `window`; một pid mang nhiều view `file://` (toolbar + sidebar + backdrop là **một** site) thì **ghép** role (`chrome:standalone+chrome:toolbar`) thay vì lấy view nào được liệt kê trước; thêm sampler 60 s (`recordProcessMetrics('periodic', true)`) để phần tăng *giữa* first paint và shutdown không còn bị mù.
- **Bằng chứng**: payload soak 4 h mang 8 pid của app (mỗi pid có `role` + `url`), và chính nó chỉ ra pid 47536 là tiến trình duy nhất leo ở bản private trong khi 4 page fixture (pid 33800) **giảm** (−0.025 MB/phút) cùng cửa sổ — nếu chỉ có bản gộp theo type thì hai sự kiện này không thể tách ra.

### Sửa lỗi - Toolbar dựng lại hai khối DOM không ai xem: dropdown Chrome Profile và panel điện thoại
- **Vấn đề (đo được, không suy luận)**: state broadcast tới 5 Hz (~106.800 lượt trong 4 h) gọi `renderChromeProfiles()` và `renderPhoneStatus()` trên **mọi** lượt, và cả hai dựng lại DOM vô điều kiện dù input không đổi.
- **Nguyên nhân**: `renderChromeProfiles()` luôn `innerHTML = ''` rồi dựng lại từng item kèm một closure `onclick` mới, thêm divider và hai dòng Session Vault — trong khi dropdown đang đóng và input (danh sách profile + profile đang active) không đổi. `renderPhoneStatus()` luôn gọi `renderPhoneModalContent()`, mà hàm này là một phép gán `innerHTML = <template>`: parse HTML + dựng lại subtree, 5 lần/giây dù panel đang đóng.
- **Sửa**: dropdown được chặn bằng chữ ký giá trị (`computeChromeProfilesSignature`), cùng convention với `computeTabsSignature`/`computeBookmarksSignature` đã dùng trong chính handler đó; panel chỉ render khi `phoneStatusOverlay` đang mở (`display: flex`), và `openPhoneStatusModal()` vẫn render thêm một lần trước khi hiện — nên panel đang mở không thể giữ trạng thái "đã kết nối" cho một máy vừa rút.
- **Bằng chứng (đo, không suy luận)**: `scripts/probe-toolbar-render-cost.cjs` (jsdom + bundle đã emit, chạy trên chính `toolbar.html`/`toolbar.js` của bản build) — lượt push đầu dựng dropdown = **5** mutation (5 dòng), push y hệt = **0**; đổi tên profile = **6** mutation và tên mới có trong DOM (chặn không làm cũ menu); panel đóng = **0** mutation trong body nhưng badge vẫn theo state (`iPhone 15 Connected`, `display` từ `none` sang `inline-flex`); panel mở = **1** mutation; rút cáp khi panel đang mở = **1** mutation với đúng nội dung "Không phát hiện thiết bị" (chặn không che mất ca disconnect). Toàn bộ 19 check của probe PASS.
- **Giới hạn còn lại (nói rõ, không hứa)**: hai đường này không có test unit — harness renderer ("FakeElement") không parse `innerHTML` nên nó không đếm được khối DOM này; thứ ghim chúng là probe chạy tay ở trên, với ngưỡng là **0 mutation trên push không đổi**, nên phải chạy lại probe sau khi sửa `src/renderer/toolbar.ts`.

### Sửa lỗi - Chụp ảnh cho vô ích trên view không có surface: `CAPTURE_TIMEOUT` gọi sai nguyên nhân
- **Triệu chứng**: lượt chụp của agent "lúc ăn lúc không" theo kiểu treo — cùng một tab, cùng một lệnh, khi trả ảnh trong vài trăm ms, khi đứng ~12 s rồi trả `CAPTURE_TIMEOUT` dù pane đang vẽ bình thường; nguyên nhân bị gọi sai nên không có gì để sửa theo.
- **Nguyên nhân** (đọc mã + đo trên app thật, không suy luận): lượt chụp copy surface của view **nằm trong** `contentView` của cửa sổ. Một view bị giao dịch khác tháo ra ngoài (attach-tạm của lượt chụp trước, lượt switch/close, renderer crash) **không nhận BeginFrame** nên `capturePage()` không bao giờ settle và `Page.captureScreenshot` chờ hết bound: bound native-raster 4 s + probe no-surface 8 s = ~12 s trước khi trả lỗi. Mã cũ suy ra "sẵn sàng" từ sổ sách `getActiveTabId()` — thứ nói cửa sổ *nên* trình bày tab nào, không nói view nào *đang* ở trong cửa sổ.
- **Sửa**: thêm `TabDevToolsHost.assertCaptureSurfacePresent()` đọc attachment **cục bộ** (`isTabViewAttached(view)`, `tab-devtools-host.ts:1817-1834`) trước mọi lượt chờ compositor, gọi ở **cả hai** điểm vào chụp (`captureScreenshot`'s action và `captureVerificationScreenshot`'s action — grep xác nhận chỉ có 2 call site). View của tab đang active được sửa **một** lần qua `reassertPresentedView()` (bất biến của entry ở trên) rồi mới từ chối; tab không phải active từ chối ngay bằng `NO_RENDER_SURFACE` kèm câu chỉ đúng việc phải làm. Target offscreen (OSR) và host không trả lời được attachment đi tiếp như cũ — guard không bao giờ tự bịa ra một surface thiếu.
- **Bằng chứng**: `node --test .compiled/test/main/tab-devtools-host.test.js` → **36/36 pass** (3 ca mới: từ chối nhanh + không gọi raster, tab đang active tự sửa rồi chụp, target nền bọc attach-for-capture không bị từ chối). Kiểm chứng bằng **đột biến** trên bản emit: cho guard early-return → ca 29 và 30 **đỏ**, ca 31 vẫn xanh (mỗi ca ghim một nửa của fix); hoàn nguyên → 36/36 xanh lại. `npm run test:main` → **1369 pass / 1 fail**, và ca fail (`bridge-pairing-queue-concurrency`, `waitFor` 30 s dưới tải lane) chạy **một mình pass trong 6,03 s**, không tham chiếu tầng capture (chỉ có một mock `captureScreenshot()` trong context giả) ⇒ flake do tải, không phải hồi quy. `npx tsc -p ./` sạch.
- **Bằng chứng trên app thật** (bundle đã sửa, instance pid 32392 boot lúc 22:53:28 sau lần emit 22:50:56): chụp **cả hai** điểm vào qua MCP trên app đang chạy — tab đang trình bày (`anti.screenshot.viewport`, ảnh viewport đủ), tab **nền** đi qua `runWithAttachedTabView` (ảnh 1904×893 của preset `custom-1904x893`), và `anti.screenshot.full_page` trên chính tab nền đó (ảnh toàn bộ chiều cao tài liệu tới footer/newsletter) — cả ba trả ảnh thật, không lượt nào chạm `CAPTURE_TIMEOUT`; đây đúng là ca mà guard đọc attachment **bên trong** wrapper nên nếu guard sai thì lượt chụp nền sẽ bị từ chối (`NO_RENDER_SURFACE`) hoặc treo.
- **Đã có hiệu lực**: app đã tự khởi động lại sau lần emit (pid 32392 > emit 22:50:56) nên bundle đang chạy đã chứa guard; không cần restart thêm cho fix này (các fix Cache/Cookie ở trên vẫn cần đúng một lần restart — đã xảy ra cùng lúc).

### Sửa lỗi — Cửa sổ có thể ở lại không trình bày tab nào: view đang xem bị bỏ ngoài `contentView`
- **Triệu chứng**: cửa sổ trắng (không tab nào được trình bày) sau một thao tác tab hợp lệ, và **không có dòng lỗi nào** trong log vì không nhánh nào coi trạng thái đó là lỗi — người dùng chỉ thấy nội dung trở lại khi tự bấm lại đúng tab đang active. Cùng lớp lỗi này làm đường chụp ảnh của agent hỏng theo kiểu "lúc ăn lúc không": lượt chụp attach tạm view để đo, rồi một giao dịch khác chen vào và để view đó nằm ngoài cửa sổ.
- **Nguyên nhân** (đọc mã, không suy luận): cửa sổ chỉ có **một** pane trình bày **một** tab, và mọi giao dịch tab chạy trên main loop nhưng đều trải qua `await`, nên một lượt attach-tạm-để-chụp, một lượt kích hoạt của session khác, hoặc một renderer crash có thể rơi vào giữa lúc attach và lúc kiểm tra kế tiếp. Ba nhánh khi đó bỏ view đi mà không đưa lại: (1) nhánh từ chối present tab thuộc agent-plane (`offscreen`/`ephemeral`) `return false` trên một cửa sổ có thể đang rỗng; (2) nhánh release của lượt attach-tạm chỉ gọi `webContents.invalidate()` trên view **đã bị tháo** — invalidate trên view ngoài cửa sổ không vẽ lại gì; (3) nhánh switch/close quyết định ngăn xếp hiển thị từ ảnh chụp `this.window.contentView.children` đã cũ, và nhánh switch còn **tháo** view mà một lượt attach-tạm đang giữ, phá phép đo của caller rồi để lượt release quyết định từ một ảnh chụp còn cũ hơn.
- **Sửa**: đặt bất biến "mọi giao dịch tab kết thúc bằng việc cửa sổ trình bày view của tab đang active" thành một hàm duy nhất `NativeTabHost.reassertPresentedView()` (tự attach lại và `layOutDetachedView` khi view thiếu surface), rồi gọi nó ở đúng những nhánh trước đây bỏ view đi mà không đưa lại: từ chối present agent-plane, release attach-tạm, switch thất bại, và nhánh đóng tab đang trình bày. Lượt switch không còn tháo view đang bị một attach-tạm giữ (`isTemporarilyAttachedView`) — việc đó phá phép đo của caller và để lượt release quyết định ngăn xếp hiển thị từ một ảnh chụp đã cũ. Mỗi lần bất biến thật sự phải sửa (view bị tháo), host ghi `tabhost.presentedViewReattached` vào journal kèm `tabId`.
- **Bằng chứng**: `node --test .compiled/test/unit/native-tab-host-presented-view.test.js` → 2/2 pass, và chạy trên emit **sạch** (journal trỏ về runtime tạm: `ANTIFAN_RUNTIME_DIR` phải được set trước khi module host được require, nếu không event unit-test rơi vào journal thật của người dùng). Hai ca test không trùng nhau — kiểm chứng bằng đột biến trên bản emit riêng: control (nguyên fix) 2/2 pass; **làm rỗng** `reassertPresentedView()` → chỉ ca "refusing to present an agent-plane tab…" đỏ; **bỏ guard** `isTemporarilyAttachedView` → chỉ ca "a switch made while a capture holds another tab…" đỏ ⇒ mỗi ca ghim đúng một nửa của fix, không ca nào là test trang trí. `npm run compile` sạch; app đã restart để nạp bundle mới (pid 44388 → 13628, port 20130), 11 tab khôi phục nguyên partition `persist:profile-profile-2` và MCP rebind lại đúng instance mới.

### Sửa lỗi — Kênh extension Companion: allowlist bên nhận âm thầm bỏ toàn bộ cookie Google
- **Triệu chứng (người dùng)**: "không đồng bộ được từ Chrome" và "lúc ăn lúc không" — popup báo "✅ Đã đồng bộ thành công N cookies" nhưng AntiFan vẫn chưa đăng nhập Google.
- **Nguyên nhân** (đo trên máy thật, không suy luận): extension lọc cookie theo `SCOPE_PROFILES` (`src/extension/domain-scoper.ts:3-21`, có `google.com`, `youtube.com`, `googleusercontent.com`, `accounts.google.com`, `gstatic.com`, `google.com.vn`), nhưng allowlist bên nhận `DEFAULT_EXTENSION_ALLOWED_DOMAINS` (`src/main/bridge/bridge-server.ts`) chỉ có 10 miền thương mại điện tử — **thiếu toàn bộ nhóm Google**, nên `rawCookies.filter(...)` ở handler `/api/cookies/import` bỏ hết cookie Google/YouTube trước khi ghi vào jar. Ca thật: `POST` 3 cookie Google/YouTube + 1 cookie ngoài phạm vi (`totalReceived: 4`) trả về `importedCount: 0, skippedCount: 0, failedCount: 0` — im lặng hoàn toàn, không đếm, không log. Cùng lúc `SYNC_ACTIVE_TAB` (`src/extension/background.ts`) bỏ qua kết quả `dispatchDeltaSync(...)` và trả `count: targetCookies.length` (số cookie *đã thử gửi*), nên popup báo thành công cho một lượt đồng bộ không nạp được gì.
- **Sửa**: `DEFAULT_EXTENSION_ALLOWED_DOMAINS` bổ sung đúng những miền mà extension được phép gửi nhưng bên nhận chưa nhận — `google.com`, `youtube.com`, `googleusercontent.com`, `gstatic.com`, `google.com.vn`, `shopifycloud.com`, `bizweb.vn` (không thêm `accounts.google.com`: đã nằm trong luật hậu tố `.google.com`; danh sách vẫn giữ thủ công vì đây là biên giới hạn quyền, không suy ra tự động từ phạm vi của bên gửi). Phản hồi `/api/cookies/import` thêm `filteredCount` để cookie bị allowlist cắt được **báo** thay vì rơi im lặng. `SYNC_ACTIVE_TAB` trả số cookie bên nhận **thực nhận** (`dispatchDeltaSync` đã trả `{success,count,error}`) kèm `attempted`; `extension/popup.js` chỉ báo ✅ khi `count > 0`, còn `count === 0` thì cảnh báo "chưa có cookie nào được nạp" kèm số đã thử. Khôi phục Session Vault nay đếm cookie đăng nhập Google (`SID`/`HSID`/`SSID`/`APISID`/`SAPISID`/`__Secure-*`/`LOGIN_INFO` trên miền Google/YouTube) và cảnh báo khi bản sao lưu không có cookie đăng nhập nào, thay vì báo "nạp thành công" cho một file chỉ có cookie phụ. Gợi ý khi clone Chrome trả 0 cookie (`chrome-profile-sync.ts`) nay nêu đủ hai rào chắn (App-Bound Encryption v20 **và** Chrome ≥136 chặn remote-debugging trên user-data-dir mặc định) và chỉ đúng đường chạy được: 🧩 Tiện ích AntiFan Companion trước, Session Vault sau.
- **Bằng chứng**: probe `scripts/probe-companion-grant-scope.cjs` chạy đúng route thật `/api/cookies/import` với grant mặc định — trước sửa `importedCount: 0`, không cookie nào vào store; sau sửa `importedCount: 3`, `filteredCount: 1`, cả 3 cookie Google/YouTube vào jar đúng domain, cookie ngoài phạm vi vẫn bị chặn. Test hợp đồng mới `test/main/extension-companion-pipeline.test.ts` ("default grant reaches every domain the companion extension is scoped to send") **fail trước sửa** (nêu đúng 8 miền bị bỏ: 6 nhóm Google + `shopifycloud.com` + `bizweb.vn`) và **pass sau sửa**: nó chạy một lượt import cho **mỗi** regex trong `SCOPE_PROFILES` nên hai danh sách không thể lệch nhau lần nữa mà không đỏ CI. Test `test/main/extension-sync-report.test.ts` ("the popup is told what the bridge accepted…") chốt đúng hợp đồng mà popup đọc: dựng bridge thật với grant chỉ mở `myharavan.com`, extension gửi 2 cookie (1 Google + 1 storefront) → `attempted: 2`, `count: 1`, chỉ cookie storefront vào jar; patch ngược build về `count: targetCookies.length` thì **đỏ** (`actual: 2, expected: 1`), khôi phục thì xanh. `npm run test:main` **1369 test / 1367 pass / 0 fail** (2 skip); `npm run test:unit` **1155 test / 1150 pass / 0 fail** (5 skip).
- **Cần làm để có hiệu lực**: restart AntiFan Desktop (bundle main đã đổi) — với extension đang cài, đường nạp Google hoạt động ngay vì extension đã gửi cookie từ trước, không cần cài lại. `npm run compile` đã rebuild `extension/background.js` và sync sang `%LOCALAPPDATA%\AntiFan\extension`; để popup hiện số thật và cảnh báo mới thì reload extension tại `chrome://extensions`.
- **Giới hạn còn lại (không phải bug sửa được)**: cookie xoay vòng `*PSIDTS`/`*PSIDRTS` vẫn là single-owner (xem mục P2#7 trong `docs/research-cache-cookie-mechanism.md`) — nếu vừa duyệt Google ở Chrome vừa duyệt ở AntiFan thì Google sẽ luân phiên vô hiệu một bên; cách xử lý là đồng bộ lại một chiều khi cần.

### Sửa lỗi — Cơ chế Cache/Cookie: chọn sai jar, phiên chết theo kill, và kho dữ liệu chết 5 GB
- **Triệu chứng (người dùng)**: "rất nhanh hết" (đăng nhập mất sau khi đóng app), "lúc ăn lúc không" (cùng một menu sync, khi được khi không), "không đồng bộ được từ Chrome".
- **Nguyên nhân** (đo trên máy thật, không suy luận): (1) ba menu "Sync Google Chrome Profile" và hai mục Session Vault không đi cùng một đích — hai trong ba menu lấy `getActiveTabSession()`, nên cookie import rơi vào jar của **tab đang focus**: nếu tab đó là ephemeral (RAM) thì mất ngay, nếu là capsule thì nằm ngoài jar profile; (2) migration capsule ghi vào `persist:profile-capsule-capsule-<uuid>` — không resolver nào đọc được namespace đó, nên 41 store ≈ 25.756 cookie (trong đó 58 store có cookie auth Google) là ngõ cụt ghi-mà-không-đọc; (3) cookie do **trang** set chỉ bền khi thoát êm (`flushStore` ở `shutdown`), nên kill/crash là mất phiên, và cookie session (không có `expires`) trước giờ không được lưu đĩa; (4) `--disk-cache-dir` chưa bao giờ redirect được gì (đo 0 byte) trong khi cache của jar sống nằm ở `Partitions/<name>/Cache` và lớn hơn cả ngưỡng `--disk-cache-size` đặt ra.
- **Sửa**: xoá hẳn `getActiveTabSession()` khỏi `src/main`; mọi thao tác credential đi qua **một** resolver `NativeTabHost.resolveTargetProfileSession()`, fail-closed bằng `TARGET_SESSION_INVALID` nếu đích không phải `persist:profile-*`; `local-session-vault.resolveCredentialJar()` từ chối jar RAM (`EPHEMERAL_TARGET_REJECTED`) và jar workspace (`CAPSULE_TARGET_REJECTED`) ở tầng thấp, export rỗng bị chặn bằng `EMPTY_STORE_REJECTED`; migration cắt mọi tiền tố `capsule-` trước khi ánh xạ nên đích trở lại namespace mở được; `browser/cookie-durability.ts` gắn `cookies.on('changed')` cho mọi partition cấu hình qua đường app, debounce 1 s rồi `flushStore()`, và cookie session khi import được cấp TTL 30 ngày; bỏ switch `--disk-cache-dir` cùng ghi chú đo lại (cache partition không bị `--disk-cache-size` chặn — 168–171 MB so với 128 MB); thêm `npm run prune:dead-stores` (dry-run mặc định, `--apply` mới xoá) chạy đúng cleaner đã ship với veto `livePartitions` suy từ `saved-tabs.json` + profile Chrome, fail-closed nếu state file không parse được.
- **Bằng chứng**: `npm run test:main` **1365 test / 0 fail**; nhóm test hợp đồng (`chrome-profile-sync*`, `local-session-vault`, `capsule-partition-migration-pure`, `partition-migration-gate`, `profile-ownership`, `capsule-partition-cookie-isolation`, `dead-store-cleaner`, `cookie-durability`) **58/58**; smoke kill cứng thật (`node scripts/smoke-cookie-hardkill-durability.cjs`): jar được arm **còn** cookie, jar control (không debounce) **mất** đúng như khiếm khuyết dự đoán, `armedReArm=false` chứng minh đường production tự arm, và `FLUSH_OBSERVED` chốt điểm kill theo cơ chế thay vì theo đồng hồ — PASS cả khi app thật đang mở; `smoke-boot-housekeeping` PASS (2 lần boot: lần 1 defer store bị migration giữ, lần 2 thu hồi); `smoke-cdp-hydration` PASS (cookie vào đúng jar); reclaim thật trên root người dùng: **84 store chết + 5 file mồ côi = 4.925,21 MB**, chạy lại `scanned: 0` (idempotent), `Partitions/` chỉ còn jar sống `profile-profile-2`, data root 5,7 GB → **854 MB**; `Profile/Cache` bị NetworkService của app đang chạy giữ nên được **defer** đúng thiết kế rồi thu hồi ở lượt offline (220 MB → 2 KB). Chi tiết: `docs/research-cache-cookie-mechanism.md` §5, `plans/reports/260919-cookie-cache-ultra-packet.md`.
- **Giới hạn còn lại (không phải bug sửa được)**: Chrome ≥ 127 mã hoá cookie bằng App-Bound Encryption v20 và Chromium xoá những dòng không giải mã được, nên import từ profile Chrome *thật* qua clone + CDP trả 0 cookie — app báo đúng nguyên nhân và đường thay thế (Session Vault), không còn im lặng "thành công 0 cookie".

### Sửa lỗi — Reclaim store chết chạy song song migration: EPERM giả, và nguy cơ xoá nguồn giữa lúc copy
- **Triệu chứng**: pass housekeeping ở boot ghi `errors: ["delete failed … capsule-capsule-<uuid>: EPERM, Permission denied"]` và bỏ lại store chết; probe trên app thật cho thấy đúng thư mục đó bị giữ (`renameSync` = `EPERM`/`EBUSY`) trong khi các thư mục khác trong `Partitions/` thì rảnh.
- **Nguyên nhân**: `src/main/index.ts` gọi `migrateLegacyCapsuleToProfile()` dạng fire-and-forget rồi chạy `pruneDeadStores()` ngay sau đó, cùng tick. Migration đọc mọi partition `persist:capsule-*` (`native-tab-host.ts:3592`, `:3595`) — việc đọc mở cookie database của partition nguồn và handle đó sống hết vòng đời tiến trình, nên reclaim vừa vấp EPERM vừa có thể xoá thư mục nguồn sau khi migration đã mở nhưng trước khi đọc xong (mất cookie im lặng, marker vẫn được ghi).
- **Sửa**: reclaim được chain sau migration (`void legacyMigration.then(reclaimDeadStores)`) nên luôn thấy đĩa tĩnh; `CleanupReport` thêm `deferredPaths` — `EPERM`/`EBUSY` được phân loại là "đang bị giữ, hoãn sang lần khởi động sau" thay vì lỗi. Marker của migration làm deferral hữu hạn: boot sau migration no-op ⇒ không ai giữ ⇒ reclaim xong.
- **Bằng chứng**: `npm run smoke:boot-housekeeping` (app thật, 2 lần boot, root tạm): lần 1 xoá `profile-capsule-*`, **defer** `capsule-capsule-*` với `errors: []`, marker đã ghi trước khi reclaim chạy; lần 2 `deletedPartitions: ["capsule-capsule-…"]`, `reclaimedBytes=105044`. `npm run smoke:dead-stores` 24 assert (gồm ca store bị tiến trình khác giữ ⇒ deferred, không error, và reclaim sau khi holder chết). `node --test .compiled/test/main/dead-store-cleaner.test.js .compiled/test/main/capsule-partition-migration.test.js` 8/8. Kiểm kê read-only trên data root thật: `npm run inventory:dead-stores` → 84 partition + 6 file, 5144.16 MB, `deferred: []`, `errors: []`. Chi tiết: `plans/reports/260919-store-reclaim-boot-race.md`.

### Sửa lỗi — Bridge terminal: quyền sở hữu session phân giải theo đúng plane gọi
- **Triệu chứng**: `antifan.terminalInput`/`terminalSendKey` chỉ kiểm tra ownership cho attachment; mobile grant không có `sessionId` rơi thẳng vào `tm.write(...)` — gõ được vào shell đang active của user. Cùng lớp lỗ đó ở `terminalCloseSession`, `terminalRenameSession`, `terminalResize`, `terminalRestart`: mobile grant đóng/đổi tên/resize/restart được session bất kỳ, kể cả shell đang dùng.
- **Nguyên nhân**: quy tắc "scoped caller chỉ chạm session của mình" mới áp cho attachment; nhánh mobile không tồn tại, và gate affinity ở dispatch resolve `p.sessionId || p.id` ngược với `p.id || p.sessionId` mà handler rename dùng.
- **Sửa**: mọi RPC terminal phân giải theo plane — master token không scope (địa chỉ session tường minh), mobile grant chỉ `grant.sessionId` + scope `terminal.input`, attachment giữ `terminalWriteForAttachment`; từ chối bằng `TERMINAL_FORBIDDEN`. Dispatch gate dùng đúng thứ tự `p.id || p.sessionId`.
- **Kèm theo**: mở rộng gate cho 4 handler vòng đời + resize/restart, gate nhánh input/sendKey khi thiếu `sessionId`, và một ca test ghi nhận hiện trạng fail-closed (grant mang control-plane id chưa nối terminal session nào).
- **Bằng chứng**: `test/main/bridge-server.test.js` 29/29 (3 ca plane: write resolution, lifecycle/resize/restart, fail-closed cho grant chưa bind); soak thật: `bursts` 0 trên 71 mẫu trước fix → 180 bursts + 784 `terminalEvents` sau fix.

### Xác minh runtime — Soak 2h post-fix: `FAILED` ở renderer slope và switch latency max
- **Cấu hình**: `SOAK_DURATION_MINUTES=120` (warmup 30 / workload 60 / recovery 30), profile `.antifan-soak-8h`, 6 tab + terminal thật, 122 mẫu, 0 phút máy sleep.
- **Kết quả**: `FAILED` — `slopeOk: false` (renderer 0.208 MB/min > SLO 0.15, total 0.124 ≤ 0.35), `latencyOk: false` (switch max 42.112 ms > 35 ms, p50 4.97 / p95 6.82); `memoryOk` (peak active 1549.71 ≤ 1600), `processOk` (orphan 0), `executionOk`, `teardownOk` (6/6 tab + terminal đóng sạch) đều true.
- **Phân tích**: creep nằm trọn ở renderer (browser −0.110, gpu +0.019, utility −0.001; renderer 717.16 → 727.28 MB) và không phẳng lại khi bỏ 20 phút đầu (last-40 0.235 / last-20 0.194) ⇒ tăng liên tục chứ không phải nhiễu mở cửa sổ; partial 4h trước fix cũng trượt gate renderer (0.263) ⇒ tái lập được, không phải hệ quả của fix bridge.
- **Bằng chứng**: `plans/reports/runtime-verification/real-soak-2h-verdict-20260919.md`, `real-soak-2h.json`, `real-soak-2h-checkpoint.json`; hai đường dẫn `real-soak-8h*` được khôi phục nguyên trạng bản Sep-4.

### Sửa lỗi — Soak thật không điều khiển được app: Bridge đóng socket vì token rỗng, cả run chỉ đo một app đứng yên
- **Triệu chứng**: `benchmark-real-soak-8h.cjs` dừng sau 44 giây với `RPC timeout for antifan.openTab` rồi `WebSocket is not open`, verdict `FAILED — Execution Error: WebSocket is not open`; không tab, terminal hay workload nào được tạo, nên mọi số RAM/latency đều `null`.
- **Nguyên nhân**: `BridgeServer` mint master token bằng `randomUUID()` và `setupWssEvents` đóng socket `4001` khi `clientToken !== this.token`, trong khi harness gửi `Authorization: Bearer ${process.env.ANTIFAN_BRIDGE_TOKEN || ''}` — token rỗng. Không có kênh nào lấy được token thật: `persistBridgeInfo()` cố ý chỉ ghi metadata không bí mật, còn harness tự khai `TODO(phase4): requires token injection`.
- **Sửa**: `BridgeServer.resolveMasterToken()` chỉ nhận `ANTIFAN_BRIDGE_TOKEN` khi `ANTIFAN_BENCHMARK=1` và token dài ≥ 16 ký tự; dùng cho cả khởi tạo lẫn `rotateToken()`. Harness mint token một lần, truyền vào env tiến trình con và dùng đúng token đó cho client WS — không ghi ra đĩa. Run production không set cờ benchmark nên không bao giờ nhận token inject.
- **Bằng chứng**: `test/main/bridge-server.test.js` 26/26, thêm ca "honors an injected master token only in benchmark mode" (benchmark nhận token inject, production thì không); soak preflight 2.5 phút `PASSED` với 6 tab + terminal thật (RAM p50 `1534.77 MB`, tab switch p95 `15.486 ms`, orphan `0`), trong khi trước khi sửa cùng cấu hình chỉ chạy được 44 giây rồi `FAILED`.

### Tính năng — Terminal daemon P0: host tách rời giữ session sống qua lần restart GUI
- **Mục tiêu P0**: sau khi restart/recompile GUI, mọi terminal session còn nguyên shell process, cwd, scrollback và tiến trình con đang chạy.
- **Nền tảng đã kiểm chứng**: `src/main/browser/terminal-manager.ts` không import Electron (duyệt closure: 8 file, 0 import) nên daemon chạy lại đúng `TerminalManager` trong runtime headless thay vì viết lại tầng PTY — `src/main/terminal-daemon/` gồm `protocol.ts`, `daemon-entry.ts`, `daemon-spawner.ts` (các chế độ `attached | detached | wmi | in-process`) và `daemon-client.ts` + `DaemonTerminalProxy`.
- **Stage + đo**: `scripts/stage-daemon-host.mjs` stage bundle theo phiên bản; 5 probe P0 chạy qua một wrapper pin data root tạm (`scripts/run-daemon-probes.mjs`, lane `test:probes`) để không đo nhầm thứ đang staged trên máy.
- **Bất biến mới (phát hiện bằng probe)**: không được coi "đã tạo PTY" hay "đã có output" là sẵn sàng nhận input — ConPTY nuốt input ghi trong cửa sổ chưa đọc stdin và banner PowerShell phát ra **trước** prompt, nên host gate theo prompt quan sát được.
- **Bằng chứng**: host survival (relay đã chết, host + shell còn sống, `ticks 22→28`), reattach 21/21, staged-host RPC authenticate + prompt-gated input, coverage 33/33 method · 38 live call. Báo cáo tách hai làn: làn shipping vẫn `mode=in-process` (daemon không boot) — nhánh daemon mới chỉ được chứng minh ở làn probe pin, không gộp thành một câu "e2e passed".

### Sửa lỗi — Đóng pane/split khi đang hydrate để lại unhandled rejection từ xterm
- **Triệu chứng**: `Uncaught (in promise) TypeError: Cannot read properties of undefined (reading 'dimensions')` — đỏ tiêu chí "0 renderer error" của `terminal-renderer-smoke` (Test 5).
- **Nguyên nhân**: `atomicHydrateSplitPane` kiểm epoch sau `await`, nhưng nhánh đóng split (`splitSessionState.id=''` … `splitTerm = null`) không tăng epoch; đuôi continuation không kiểm lại rồi gọi `splitTerm.reset()` (ngoài `try/catch`) và `viewportAtBottom(null)` trả `true` → `splitTerm.scrollToBottom()` trên `null`.
- **Sửa**: nhánh đóng tăng `splitSessionState.hydrationEpoch` (cùng hợp đồng với `releaseTerminalPane`), đuôi continuation thoát khi `!splitTerm || epoch lệch`; đường item đã có `item.released` + epoch từ trước.
- **Bằng chứng**: `npm test` lane `smoke:terminal` xanh (Test 5 pass), split-hydration probe `CONFIRMED_ALL_FIXES_VERIFIED`; dòng còn lại trong log là `Uncaught TypeError` **đồng bộ** trong `_sync` của xterm — khác lớp lỗi, không có frame repo.

### Bảo mật — Bootstrap secret của MCP proxy đi qua pipe thay vì environment block
- **Vấn đề**: payload bootstrap (mang bridge secret) truyền qua biến môi trường; khối env đọc được bởi mọi tiến trình cùng user.
- **Sửa**: `antifan-omp-mcp.cjs` đọc payload từ dòng đầu của stdin **trước khi** transport chạm stdin (đọc byte-by-byte; chỉ nhận payload thật sự trỏ tới bridge endpoint, frame JSON-RPC hợp lệ được `unshift` trả lại nguyên vẹn); client nội bộ ghi payload qua stdin và xoá các biến bootstrap kế thừa từ shell để không retarget nhầm session; kênh env vẫn giữ cho client ngoài repo (Codex/harness) vì tiến trình con của họ nằm ngoài tầm với stdin.
- **Kèm theo**: timeout mặc định 30 s cho mỗi request của client (có override từng call), reject toàn bộ pending khi stdout JSON hỏng hoặc tiến trình lỗi.
- **Bằng chứng**: `test:fast` xanh (`antifan-mcp-client`, `mcp-dispatch-payload-gate`, `omp-mcp-adapter`); lane e2e xanh với `mcp-industrial-overhaul`.

### Hạ tầng test — `npm test` chạy 14 làn: gate tĩnh vào bộ mặc định, graduate `test:e2e:strict`, thêm làn terminal
- **Sửa**: `run-test-pipeline.mjs` chạy cả gate tĩnh (`audit`, `plans:check`) trong bộ mặc định, tự chèn lane `compile` trước làn cần build (`--no-compile` để tắt, `--help` để in danh sách làn), đưa `smoke:terminal` và `test:probes` vào `KNOWN_LANES`; `test:unit` bó hẹp về unit (tách khỏi integration/benchmark) và `test:e2e` chạy `--test-concurrency=1`.
- **Sửa**: `check-plans.mjs` trả mã lỗi khi không tìm thấy plan nào thay vì báo xanh.
- **Bằng chứng**: chính lần chạy ship này — `npm test` → **14/14 làn xanh**, `PIPELINE_EXIT=0`.

### Hạ tầng test — Mỗi làn có ngân sách thời gian: làn treo báo đỏ thay vì chặn cả pipeline
- **Vấn đề**: `runLane` gọi `spawnSync` **không có timeout** — một làn giữ handle rò rỉ (`test:e2e:strict` chạy không `--test-force-exit`, hoặc smoke Electron) treo vô hạn; người gọi không phân biệt được "chậm" với "treo", và `npm test` không bao giờ trả về.
- **Sửa**: ngân sách theo làn (mặc định 8 phút; `smoke:terminal` 15, `test:e2e:strict` 10, `test:probes` 20; `--lane-timeout-ms` ghi đè toàn bộ), hết hạn thì kill **cả cây tiến trình** — `taskkill /PID <pid> /T /F` trên Windows vì tiến trình trực tiếp là `cmd.exe` (kill mỗi nó sẽ bỏ rơi npm/node/vitest), nhóm tiến trình `detached` + `kill(-pid)` trên POSIX — rồi summary ghi `timeout: true` kèm note `killed process tree (lane-timeout)` và exit code vẫn 1.
- **Bằng chứng**: `node --check` OK; `test:main --lane-timeout-ms 4000` → `test:main  failed  9.1s  killed process tree (lane-timeout)`, `PIPELINE_EXIT=1`, và `Get-CimInstance Win32_Process` không còn tiến trình `test/main` nào sót lại; đường bình thường `test:canary test:integration` → `PIPELINE_EXIT=0`; bộ mặc định trên runner mới: `npm test` → **14/14 làn xanh**, `PIPELINE_EXIT=0`, wall time 235.7 s (`audit 2.9s · plans:check 0.9s · compile 4.6s · test:canary 22.5s · test:fast 37.0s · test:site-clone 10.7s · test:integration 1.3s · test:main 73.8s · test:e2e:strict 62.0s · smoke:terminal 10.5s · test:terminal-transport 2.5s · test:terminal-rename 1.6s · test:mcp-dispatch-hub 2.6s · test:toolbar-qa-hub 2.7s`).

### Sửa lỗi — Ghi record của goal runner sống sót qua lock tạm thời trên Windows
- **Vấn đề**: `writeRecordAtomic` đổi tên file tạm lên file đích ngay lập tức; antivirus/indexer giữ handle trong vài chục ms và Windows trả `EPERM`/`EACCES`/`EBUSY`, làm lần chạy goal đứt ở bước ghi record dù không có lỗi logic nào.
- **Sửa**: `scripts/lib/atomic-record.mjs` retry đúng nhóm lỗi đó theo backoff 10→500 ms (ngân sách ~1.1 s) rồi mới ném lỗi thật.
- **Bằng chứng**: lane `test:unit` (unit của goal runner đi qua cùng đường ghi record) xanh trong `npm test` 14/14 làn.

### Sửa test — Lane e2e/terminal chứng minh teardown, watchdog và cái chết của tiến trình con
- **Sửa**: `terminal-rename-space` dựng `TerminalManager`/`SessionRecord` thật (có kiểm chứng persistence trên đĩa) + watchdog 60 s thay vì trạng thái giả; `test/unit/goal-runner.test.mjs` theo dõi tiến trình con (`isChildDead`) và khẳng định **stderr được giữ lại** thay vì bị nuốt, để lỗi con hiện ra trong báo cáo thay vì đổ cho timeout; `test/main/nested-cancellation.test.ts` nới ngân sách 150/120 ms → 1_500/1_200 ms vì bản cũ đo lịch chạy song song của lane chứ không đo hành vi hủy; các smoke/probe terminal chặn throttle cửa sổ Chromium và siết biên thời gian.
- **Bằng chứng**: `npm test` — `test:e2e:strict`, `test:terminal-rename`, `smoke:terminal` xanh; audit đối kháng (5 ứng viên + 1 verifier) ghi tại `plans/260919-0130-terminal-daemon-and-throughput/reports/260919-0405-ultra-verifier-audit.md`.

### Sửa lỗi — Context pack chứa bản sao của cùng một câu claim
- **Triệu chứng**: pack Core bơm vào mỗi prompt lặp claim. Đo trên 5 pack gần nhất: 75 slot chỉ chứa 46 câu khác nhau (29 slot là bản sao); pack của phiên `ak:debug` là 15 slot / **2 câu**.
- **Nguyên nhân**: `claimId = sha1(unitId + statement + anchor đầu tiên)` (`plans/260914-1248-work-root-sequential-evidence-scout/tools/deep-analyze-unit.mjs:44`) nên cùng một câu trích từ nhiều unit — một rule boilerplate nằm trong N skill, thêm bản copy trong worktree — sinh ra N claim khác nhau; `contextPack()` lấy top-N theo điểm và không gộp câu trùng.
- **Sửa**: `contextPack()` lấy pool rộng gấp 4 rồi gộp theo `(namespace, câu chuẩn hoá)`, giữ dòng điểm cao nhất và ghi lại `duplicateCount` + `alsoInUnitIds` + `contextPlatforms` (số bản sao trở thành tín hiệu, không bị ẩn); `query()` vẫn trả đủ mọi dòng để bằng chứng truy vết được.
- **Bằng chứng**: 5 pack gần nhất — 46/75 → 69/72 slot độc nhất; cùng task debug 2 → 14 câu; `packages/super-core` 32/32 (thêm ca: 3 bản sao gộp còn 1 dòng còn `query` trả đủ 3).

### Storefront Decoupling — Core bridge intent gating, theme-qa-gate micro lane (contract 3.5.0-lean)
- **Core bridge intent gating**: Phân loại intent task trước khi seed Core pack (`classifyTaskIntent`). Các prompt sửa storefront (`storefront-edit`) không còn nhận Core pack để tránh ô nhiễm context; hỗ trợ biến môi trường `ANTIFAN_CORE_BRIDGE_GATE=off` làm kill-switch tắt gating; ghi nhận session event `BRIDGE_CONTEXT_SKIPPED` với intent, taskHash và lý do bỏ qua một lần cho mỗi prompt riêng biệt.
- **Theme QA Gate micro lane (`QA_MICRO_STATIC`)**: Mở làn duyệt tĩnh cho các thay đổi pure-CSS vi mô. Áp dụng khi edit đúng 1 file CSS leaf trong `assets/` (`*.css`, `*.scss` — các biến thể `.liquid` tuyệt đối không hợp lệ vì file có thể mang Liquid không bao giờ an toàn cho làn tĩnh), tổng số dòng thay đổi (tính cả dòng thêm và dòng xoá/thay thế) ≤ 10 và không chứa tag Liquid (`{{` hoặc `{%`). Cho phép agent bypass gate với token `qaStatus: QA_MICRO_STATIC` kèm khai báo đường dẫn, selector và số dòng thay đổi; hook `theme-qa-gate` đối soát trực tiếp với edit thực tế mà nó quan sát được, ép quay về full browser QA (bước 1-8) nếu vi phạm.
- **Bản hợp đồng Agent Contract 3.5.0-lean**: Nâng `AGENT_CONTRACT_VERSION` từ `3.4.0-lean` lên `3.5.0-lean`, đồng bộ `STANDALONE_AGENT_CONTRACT` và `SELF_QA_DIRECTIVE` bổ sung bước 9 (làn micro-edit) cùng token `QA_MICRO_STATIC (lane 9)` vào danh mục `qaStatus` hợp lệ.
- **Gia cố sau review đối kháng**: Làn micro-edit và token bypass nay đọc **chỉ** nội dung assistant (không còn nhìn thấy `tool_calls.arguments`, nên grep/read chuỗi token không thể tự mở gate); call sửa nhiều file trong một lần bị **fail-closed**; bộ đếm dòng thay đổi tính đúng dòng xoá (`CUT a.=b:` theo span `b - a + 1`) và trả `Infinity` cho cú pháp block AST (`CUT N*`, `PUT N*:`) cùng lệnh xoá file `REM` vì span không quan sát được từ patch text; `session_start` reset toàn bộ state treo; regex phân loại intent mở rộng (`xóa/delete/optimize/giấu/căn…` cho storefront-edit; `giống mẫu`, `theo web đối thủ`, `tham khảo` cho clone). Test: gate 22/22, bridge 26/26.

### Sửa lỗi — Cờ `--minutes` của soak runner bị nuốt, mọi soak 120 phút chạy 30 phút
- **Triệu chứng**: nhánh `if (arg === '--minutes' && argv[i + 1])` trong `parseArgs` là một **khối rỗng** — không gán và cũng không `++i`, nên con số bị bỏ qua và `parsed.minutes` giữ mặc định 30. Help text (và lệnh chứng nhận phase 10 `--minutes 120`) vì thế không bao giờ tạo được cửa sổ 120 phút, mà cũng không có cảnh báo nào.
- **Sửa**: khôi phục phép gán `parsed.minutes = parseFloat(argv[++i]) || 30` đúng như các cờ anh em trong cùng hàm.
- **Bằng chứng**: chạy `parseArgs` trên chính văn bản trong file — trước fix `['--minutes','120'] -> 30`, sau fix `-> 120`; đồng thời quét toàn bộ `scripts/*` để chắc đây là nhánh rỗng duy nhất (1/1).

### Sửa lỗi — `antifan-core audit` ghi vào store mỗi lần gọi, ngược hợp đồng opt-in
- **Triệu chứng**: lệnh CLI `audit` gọi `core.corpusAudit({ record: true })` vô điều kiện, trong khi store đã đổi sang "chỉ ghi khi opt-in" và lệnh `gate` bên cạnh đã được sửa đúng — nên mỗi lần *thăm dò* bằng `antifan-core audit` lại mọc thêm một dòng `corpus_audit` trong store mà thư mục làm việc chỉ tới.
- **Sửa**: `audit` nay đọc-only mặc định, in một dòng nhắc ra stderr, và chỉ ghi khi gọi `audit '{"record":true}'` — cùng hình dạng với `gate`, nên chú thích cạnh lệnh `health` ("`audit` và `gate` cần `{"record":true}`") trở thành đúng.
- **Bằng chứng**: trên một DB tạm — `corpusAudit()` không ghi, `{record:false}` không ghi, `{record:true}` ghi 1 dòng; qua CLI thật: `audit` giữ nguyên số dòng (kèm note stderr), `audit '{"record":true}'` tăng 1.

### Sửa lỗi — Kết thúc session không trả lại slot tab cho pool đang giữ nó
- **Triệu chứng**: `releaseSessionTabPool` chỉ xoá theo **khoá** (affinity `${terminalId}` / `${terminalId}@gen` và pool cùng tên). Caller duy nhất — `attachment-registry.notifyDispose` — lại truyền **tabId** của tab đã bind, mà tab do một terminal nhận nuôi nằm trong pool khoá bằng terminalId: lời gọi trả `false` và slot vẫn bị tính. Slot ấy chỉ được dọn khi tab chết, nên một tab do người dùng đang xem (dispose listener cố ý không đóng) tiếp tục chiếm quota của terminal sau khi session kết thúc.
- **Sửa**: `native-tab-host.ts` tách `releaseTabFromEveryPool(tabId)` — phép giải theo giá trị mà `adoptChildTab` và cả hai reader quota đang dùng — và cho `releaseSessionTabPool` chạy nó bên cạnh phép xoá theo khoá; `releaseSessionTab` dùng chung đúng hàm đó nên chỉ còn một quy tắc "pool nào đang giữ tab này".
- **Bằng chứng**: `test/unit/browser/session-tab-slot-release.test.ts` thêm 3 ca (giải phóng theo tab khỏi pool khoá-terminal; không đuổi các thành viên khác của pool ad-hoc; báo `false` khi không giữ gì) — 7/7 xanh, và khi vá ngược cây đã biên dịch về hình dạng trước fix thì đúng 2 ca mới đỏ (5/7).

### Sửa test — Hai ca restore đọc `listSessions()` theo hợp đồng cũ (bỏ sót pane)
- **Triệu chứng**: `test/main/terminal-switching-regression.test.ts` khẳng định `listSessions()` trả **3** và **4** entry, nhưng từ `e3c8a5c0` (split-pane sidebar) mỗi pane được chiếu thành entry riêng — 4 base + 1 pane trả **5**, nên hai ca đỏ từ đó tới nay dù hành vi mới là chủ ý (tab strip khoá pane theo id).
- **Sửa**: hai ca nay khẳng định danh sách đầy đủ theo thứ tự `[base, pane của nó, ...]` và giữ nguyên các bất biến thật (thứ tự/tên/buffer của tab base, pane có `splitOf` đúng và buffer riêng).
- **Bằng chứng**: `node --test .compiled/test/main/terminal-switching-regression.test.js` — 18/18 pass (trước đó 2 đỏ).

### Sửa lỗi — Popup Annotation: danh sách "Gửi tới" chứa cả terminal đang sleep, đã thoát và pane split
- **Triệu chứng**: dropdown `Gửi tới` của Popup Annotation đổ nguyên `listSessions()` — vốn chiếu **mỗi pane split thành một entry riêng** và giữ cả session `sleeping`/`exited`. Trên app đang chạy, danh sách vì thế có những dòng như `Terminal split-1 (…)` cạnh tab cha của chính nó, và những session không còn shell nào để nhận prompt: chọn vào là gửi tới một đích không hành động được, còn nhìn thì rối.
- **Sửa**: `terminal-manager.ts` có thêm `selectAnnotationTargets(sessions)` — giữ `state === 'running' && !splitOf`. Cả hai nơi bơm context vào trang (`native-tab-host.ts` nhánh `did-finish-load` và `tab-devtools-host.ts` của `startInspect`) đều dùng hàm này, nên select chỉ còn `Tự động (theo site URL)` + các terminal đang chạy; pane về theo tab cha nó thuộc về. Payload bơm vào trang cũng nhẹ đi vì không còn kèm transcript của những entry bị loại.
- **Bằng chứng**: `test/main/per-tab-terminal-session.test.ts` ca "offers only running base sessions as annotation targets" chạy **chính script được bơm** trong jsdom, mở modal bằng một cú click thật rồi đọc `<option>` — trước fix `["auto","terminal-A","terminal-nap","terminal-dead","terminal-split"]`, sau fix `["auto","terminal-A"]`; 11/11 ca của file xanh.

### Sửa test — Ca "Task Run surface" chọn row theo vị trí nên không bao giờ chạy đường trace
- **Triệu chứng**: ca này click `items[0]` trên tab Task Runs, nhưng `coreListItems()` đặt **row bề mặt `__task_runs__` ở đầu** (cùng convention với `__bridge__` và `__regressions__`), nên cú click chỉ chọn lại chính row bề mặt: `coreDetailCategory` là `TASK_RUNS_PRESENT` thay vì `TRACE_FOUND`, còn đường IPC `getCoreTaskRunTrace` không hề được gọi — ca test đỏ vì chọn sai row, không phải vì sản phẩm hỏng.
- **Sửa**: `renderer/toolbar.ts` gắn `data-item-id` (khoá chọn đã có sẵn trong `CoreListItem.id`) cho mỗi row hub; ca test chọn `[data-item-id="pack-abc"]` thay vì theo chỉ số, nên row thêm/bớt sau này không thể trỏ nhầm row một cách im lặng.
- **Bằng chứng**: `node --test --test-force-exit .compiled/test/renderer/core-health-hub.test.js` — 6/6 pass (trước đó 1 đỏ).

### Sửa lỗi — Kéo tab cha ở sidebar bỏ lại pane chia đôi phía sau (pane không theo cha, nhóm đọc sai)
- **Triệu chứng**: pane (`splitOf`) là tab con của tab cha, nhưng mọi đường phân nhóm lại suy nhóm của pane từ **chính nó**: pane không có `category` riêng nên rơi vào nhóm "Chưa phân loại" và tách khỏi cha. Kéo/pick nhóm cho tab cha vì thế chỉ chở cha đi, còn pane nằm lại; và vì hàng pane nằm dưới cha trong cùng nhóm, người dùng thấy một pane "mồ côi" ngay dưới nhóm khác.
- **Sửa**: `src/renderer/standalone.js` dùng chung một phép quy chiếu — `groupKeyOf(session, keyBySessionId)` trả khoá nhóm của **tab sở hữu** (cha sống, đã tính cả bản tự chọn nhóm mới nhất trong phiên) — cho cả ba nơi: nhóm của hàng, thứ tự dựng lại khi kéo, và mục tiêu của popup chọn nhóm. Hàng pane **không còn là nguồn drag** (`wrap.draggable = !s.splitOf`) và popup trên pane mở từ nhóm của cha, nên thao tác luôn tác động lên tab sở hữu.
- **Bằng chứng**: 4 ca mới trong `test/renderer/terminal-tab-categories-sleep.test.ts` (pane nằm dưới cha; kéo cha mang pane sang nhóm mới; hàng pane không drag; popup trên pane bắt đầu từ nhóm của cha). Đối chứng trước/sau trên **đúng file bundle mà harness nạp**: hoàn nguyên 3 điểm sửa trong `.compiled/src/renderer/standalone.js` làm **đúng 4 ca đó đỏ** (40/44), khôi phục thì xanh lại 44/44 — nên assertion thật sự phân biệt được hành vi cũ.

### Sửa lỗi — Xếp một tab sang terminal thứ hai vẫn để lại quyền điều khiển ở terminal cũ
- **Triệu chứng**: phép "seat" một tab về một chủ sở hữu chỉ rút tab khỏi **pool** của phiên cũ (`evictTabFromOtherPools`), không đụng tới **affinity entry** — trong khi entry là nguồn thứ ba đọc cùng một sự thật: `isTabAllowedForPrimary` (cổng quyền của tool layer: `browser-control-port`, `capability-catalogue`) và `isTerminalAllowedForTab` (cổng `assertTerminalAccess`), cùng trần 10 tab của `adoptChildTab`. Tab đã thuộc terminal mới nhưng agent ở terminal cũ vẫn qua được cổng và vẫn bị tính một slot.
- **Sửa**: `native-tab-host.ts` đổi `evictTabFromOtherPools` thành `evictTabFromOtherOwners` — rút tab khỏi mọi pool **và** mọi affinity entry khác qua `detachTabFromAffinityEntry` (kèm `lastUrls`/`lineage`), xoá entry khi nó không còn giữ tab nào, đúng quy tắc đường unbind đang dùng; pool khoá bằng chính tab và entry bind vào tab đó là cùng một chủ sở hữu nên được giữ nguyên.
- **Bằng chứng**: ca 31 mới trong `test/unit/browser/terminal-tab-affinity.test.ts` (chủ cũ mất `isTerminalAllowedForTab` và `isTabAllowedForPrimary` với tab đã chuyển, người nhận có, entry cũ chỉ còn tab nó thật sự giữ) — 39/39 xanh; vá ngược cây đã biên dịch về hình dạng chỉ-rút-pool thì **đúng ca 31 đỏ** (47/48), khôi phục 48/48.

### Sửa lỗi — Pane chia đôi chiếm ô "terminal của tab" rồi biến mất khỏi danh sách gửi tới
- **Triệu chứng**: `claimTabTerminalSession` coi pane (`splitOf`) là đích hợp lệ như mọi session base, nên một tab chưa có lựa chọn nào bị gán `terminalSessionId` = id pane. Ô đó chỉ được đọc bởi dropdown "Gửi tới" của Popup Annotation — vốn đã lọc pane ra, chỉ còn session base đang chạy — nên người dùng thấy "Tự động" trong khi prompt thực tế đi tới pane đang bị ẩn khỏi danh sách.
- **Sửa**: `claimTabTerminalSession` bỏ qua session là pane (`target.splitOf`); pane vẫn sở hữu browser tab qua affinity map như trước, chỉ không ghi vào ô lựa chọn của người dùng.
- **Bằng chứng**: Flow 15b (`test/main/chromium-terminal-comprehensive-tab-interaction.test.ts`) — tab do pane bind giữ `terminalSessionId === undefined` trước khi đóng và `tab-main` vẫn giữ `'terminal-parent'` sau fix; bỏ đúng guard `|| target.splitOf` trong cây đã biên dịch làm **đúng Flow 15b đỏ** (31/32), khôi phục 32/32. Cả file: `node --test .compiled/test/main/chromium-terminal-comprehensive-tab-interaction.test.js` 32/32.

### Sửa lỗi — Rebind đi khỏi tab không trả slot về pool đang giữ nó (session tự chặn chính mình)
- **Nguyên nhân**: `releaseSessionTab` tra pool theo **id mà caller truyền vào như session**, nhưng registry chỉ biết **tab đã được nhả**: tab vào chỗ qua `createTab({ terminalSessionId })` hoặc bị terminal nhận làm con thì nằm trong pool khoá bằng **id terminal**. Lệnh xoá theo khoá trả `false` và slot vẫn bị đếm, nên sau khi rebind đi khỏi tab, `openTab` kế tiếp của chính session đó có thể từ chối `POLICY_DENIED` với một tab nó đã bỏ.
- **Sửa**: `native-tab-host.ts` duyệt các pool và xoá tab **ở nơi nó đang được giữ** — đúng cách `adoptChildTab` và `getManagedTabIds` vốn tra pool — và bỏ pool đã rỗng. Caller truyền id terminal vẫn nhả toàn bộ affinity entry qua `removeManagedTab`; caller truyền id tab thì không đụng entry đó, vì nó ghi quyền sở hữu terminal (badge + access check), không phải hạn mức của session này. Kèm theo: `listTerminalSessionIds` khử trùng — một pane split được `listSessions` chiếu thành entry riêng nên entry cha nêu lại nó bị liệt kê hai lần.
- **Bằng chứng**: `test/unit/browser/session-tab-slot-release.test.ts` khoá hợp đồng tại `getManagedTabIds` — nguồn prune duy nhất mà cả hai cổng hạn mức đọc: **đỏ trước fix** ở đúng các ca khoá bằng terminal, xanh sau.

### Sửa lỗi — Terminal đọc lệnh chuyển alternate-screen vắt qua ranh giới chunk
- **Nguyên nhân**: `appendData` khớp `\x1b[?1049h` / `\x1b[?1049l` **trong một chunk**, nhưng chunk không phải một message: ConPTY cắt chuỗi này, nên một lệnh *enter* bị vắt làm đôi để lại cờ `false` (chương trình full-screen bị xoá transcript) và một lệnh *leave* bị vắt để lại cờ **kẹt `true`** (`cls`/Ctrl+L của người dùng bị nuốt im lặng).
- **Sửa**: `terminal-manager.ts` quét trên **đuôi chunk trước + chunk này**, giữ lại đuôi (`ALT_SCREEN_SEQ_LENGTH - 1`) cho lần sau, và lấy lệnh **cuối cùng** khi một chunk mang cả hai.
- **Bằng chứng**: `test/unit/browser/terminal-alt-screen-scan.test.ts` chạy đúng handler `child.onData` của production qua ranh giới `FakePty` và khẳng định cờ cùng phần repaint xoá màn hình mà nó giữ lại cho tới khi chương trình rời screen.

### Cập nhật — Nhóm tab ở sidebar: đánh dấu `*`, màu, thứ tự; pane chia đôi nằm dưới tab cha
- **Việc**: header nhóm bỏ **badge đếm tab** (một con số không ai hành động được) và nhận menu `⋮` gom ba thao tác nhóm — đánh dấu `*`, chọn màu (8 màu + "Tự động"), chuyển nhóm lên/xuống. Mục không áp dụng được thì **vô hiệu hoá** (nhóm đầu không "chuyển lên" được) thay vì im lặng không làm gì. Tên nhóm lấy phần bề rộng còn lại và cắt bằng ellipsis nên nhóm dài không đẩy nút của chính nó ra ngoài cột 220px.
- **Màu nhóm nhìn thấy được ở cả hai layout**: ở sidebar, tên nhóm tô đúng màu nhóm (layout ngang đã có chip màu từ trước, nhưng sidebar thì màu chỉ nằm trong menu — một lựa chọn vô hình với người dùng sidebar). Màu suy diễn sẵn cho mọi nhóm thật, nên lựa chọn của người dùng chỉ là ghi đè.
- **Pane chia đôi không còn đọc như tab thứ hai**: hàng của pane (`splitOf`) render **ngay dưới tab cha** trong cả hai layout (trước đây rơi vào đúng vị trí mà main trả về), thụt lề 14px, viền trái xanh, glyph `⤷` **chỉ** hiện trên hàng pane, tooltip nêu tên cha sống (đổi tên cha thì tooltip con đổi theo). Glyph ẩn là trạng thái mặc định trong CSS nên một hàng từng là pane không thể giữ lại dấu khi đã thành tab thường.
- **Bằng chứng**: probe Electron thật (nạp đúng `.compiled/src/renderer/standalone.html` + preload, mock IPC) — 33/33 kiểm tra PASS: 0 badge đếm; bấm menu → đánh dấu `*`, chọn `#f472b6`, chuyển xuống, và cả ba đều tới `SET_TAB_PREFS` (`starredCategories: ['SHOPIFY']`, `categoryColors: {SHOPIFY: '#f472b6'}`, `categories: ['SAPO','SHOPIFY']`); sau khi reload renderer thì `*` và thứ tự vẫn còn; header sidebar `rgb(244,114,182)` và chip layout ngang cũng `rgb(244,114,182)`; hàng pane nằm ngay sau cha (`["s-shopify","s-shopify-split", …]`), `display` của glyph là `inline-flex` trên pane và `none` trên tab thường. Kèm theo: `npm run compile` exit 0, `npm run test:unit` 1139/1144 pass (5 skip, 0 fail), `npm run test:e2e` 6/6 xanh.

### Sửa lỗi — Gia hạn attachment ghi một frame mỗi nhịp tim (throttle đo sai đại lượng)
- **Nguyên nhân**: guard chống ghi dày so **độ trôi `expiresAt`**, nhưng mỗi lần gia hạn lại cộng thêm `extensionMs` (1h) — nên độ trôi đo được luôn bằng đúng phần vừa cộng và ngưỡng 60s **không bao giờ** chặn: nhịp tim 1Hz ghi một frame revisions mỗi tick, serialize lại toàn bộ candidate, đẩy tới ngưỡng compact 500 frame và làm nghẽn dispatch song song (đo trên bridge thật: lần append đầu 2.3s, các cú stall 200–2500ms).
- **Sửa**: `src/main/run/attachment-registry.ts` ghi **mốc wall-clock của frame bền vững gần nhất** rồi so **thời gian đã trôi**, thay vì so độ trôi của `expiresAt`.
- **Bằng chứng**: `test/main/attachment-runtime-scoping.test.ts` ca "throttles heartbeat renewals instead of appending a durable frame per tick" — trước fix 6 frame, sau fix 1 frame; đường rebind khi pid đổi vẫn ghi thẳng.

### Sửa lỗi — MCP proxy giữ mặc định `tabId` trỏ vào tab đã đóng (session trông như hỏng)
- **Nguyên nhân**: proxy stdio tự điền `tabId` cho mọi call thiếu nó từ **cache bound tab của chính nó**, nhưng chỉ làm mới cache ở switch/rebind/set-automation/open. Một cú close để mặc định trỏ vào tab vừa đóng: bridge đã xoay attachment authority sang tab failover, còn call kế tiếp tới như một yêu cầu **tường minh** cho id đã chết và bị từ chối `TARGET_MISMATCH` — phải pair lại mới dùng được; cú close do UI phát mà proxy không quan sát được cũng dính đúng bức tường đó.
- **Sửa**: `scripts/antifan-omp-mcp.cjs` đưa mặc định về **một chủ duy nhất** (`resolveBoundTabId`/`recordBoundTab`) và ghi lại đúng vòng xoay mà authority thực hiện: tab thay thế do close nêu (`failoverTabId`), và **xoá tường minh** khi close không có tab thay thế, để id cũ không thể được suy lại từ bootstrap file.
- **Bằng chứng**: `test/main/mcp-proxy-bound-tab-rotation.test.ts` — open xoay, close có thay thế xoay, close không thay thế thì xoá, close không liên quan không đụng mặc định; ca 2 và 3 fail khi tắt nhánh close. Live: e2e Milestone 7 (đóng tab session đang bind, call kế tiếp trả về DOM storefront đang sống).

### Sửa lỗi — Probe render-surface bị nuốt lỗi, nguyên nhân thật không tới được caller
- **Nguyên nhân**: `getViewport` bỏ qua exception của probe render-surface, nên "tab không đo được" và "tab không có surface" cho ra **cùng một payload** (`probe-unavailable`) — timeout CDP, pool bận hay target biến mất đều không bao giờ tới caller.
- **Sửa**: `src/main/tools/browser-control-port.ts` trả `probeError { code, message }` bên cạnh nguyên nhân đã phân loại. Fixture `tab-devtools-host` case 22 vốn ghim `CAPTURE_TIMEOUT` trong khi capture rework trả `NO_RENDER_SURFACE` khi tầng native không trả lời; hợp đồng production là **cố ý** (view detached thì viewport capture timeout), nên fixture nay trả raster lệch và **giữ nhánh timeout reachable** thay vì nới assertion.
- **Bằng chứng**: `npm run test:main` — 1315 pass, 0 fail.

### Kiểm chứng — Lane e2e chạy một sóng và tự chứng minh surface, cảnh báo, biên
- **Việc**: 5 file của lane e2e vốn chạy `--test-concurrency=1`, tốn 40.1s wall cho 29.6s test body; nay chạy **một sóng** (`--test-concurrency=5`) sau khi các lane live tự ghim register/data root vào profile tạm (U8) nên không còn chia sẻ state. `test:e2e:strict` vẫn tuần tự để chẩn đoán treo.
- **Siết assertion thay vì nới**: benchmark ghim **p95 thật (nearest-rank, 120ms, tối đa 1/20 mẫu)**, `maxMs` riêng phải dưới một nhịp tim, và **0 mẫu >500ms**; mẫu phải là DOM sống. Milestone 7 chứng minh failover khi đóng tab từ đầu tới cuối; golden lane mở cùng cảnh báo trên pha dispatch — cả hai **đều đổ** khi tháo `resolveTabId`.
- **Bằng chứng**: 3 lần chạy liên tiếp ở concurrency 2 — exit 0, 5/5 xanh, wall 20/21/21s (trước 40.1s); trong pipeline đầy đủ lane e2e còn **14.6s**.

### Sửa test — Waits của goal-runner đo máy thay vì đo tiến độ runner
- **Triệu chứng**: ca "loses at most one unit of work when killed mid-phase" fail **ba lần** ở trần 15s (15.03s, 15.08s) trong khi cùng ca đó trên máy rảnh chạy 3.4s; nâng trần lên 60s thì fail lại đúng **60.08s** với 1 unit có verdict — trần thời gian không phải bất biến cần đo.
- **Sửa**: `test/unit/goal-runner.test.mjs` thêm `waitForProgress` — ngân sách là **stall** (không có entry worklog mới trong 16s = 40× thời lượng unit của fixture): máy chậm chỉ kéo dài wall-clock, còn runner đứng yên vẫn fail **nhanh hơn** (16s thay vì 60s) kèm bằng chứng `describeRun()` (entry cuối, tuổi nhịp tim, runner còn sống hay không). Wait vô hạn trên `exit` của runner resume cũng được chặn ở 120s.
- **Bằng chứng**: đối chứng âm — bản sao throwaway của chính test trỏ vào fixture unit không bao giờ resolve (đã xoá): fail ở **16.4s** với `no worklog entry for 16021ms … heartbeatAgeMs=16037 runnerAlive=true last=[run-start@…, unit-start:u1@…]`; sau đó `test:fast` xanh 2 lần liên tiếp (1261/0, ca này 4.47s và 3.84s).

### Sửa lỗi — App biến mất khi split mode: gọi native emulation trên view không có platform widget
- **Nguyên nhân**: `enableDeviceEmulation`/`disableDeviceEmulation` là API native của `WebContentsView` và cần **platform widget** của chính view đó. Code cũ gọi thẳng, không hỏi view có đang nằm trong `window.contentView.children` hay không. View chưa attach (hoặc đã bị gỡ) ⇒ không có render widget host view ⇒ native đọc NULL. Sổ issue ghi đúng dạng đó: 3 record P0 `NATIVE_CRASH` từ `runtime.process`, tất cả `STATUS_ACCESS_VIOLATION` (`0xC0000005`), `faultingAccess: read`, `faultingAddress: 0x0`, `processType: browser` — `ISS-1789564111316-tmg4y` (16-09 13:08), `ISS-1789579340686-mkk3c` (16-09 17:22), `ISS-1789612090295-mp0wd` (17-09 02:28), dump `de686712-…` / `c2bbb433-…`. Chết ở **browser process** nên cả cửa sổ biến mất, không có log JS nào, và `try/catch` không cứu được fault native. Đường sinh lỗi: tab split mode → `applyTabDeviceEmulation` → nhánh mobile emulate pane không có surface.
- **Sửa**: `native-tab-host.ts` từ chối **trước** khi gọi native — `safeEnableDeviceEmulation`/`safeDisableDeviceEmulation` nhận thêm `view` và thoát sớm khi `view && !isTabViewAttached(view)` (`isTabViewAttached` = view có trong `window.contentView.children`), mọi call site truyền view của chính nó. Nửa `disable` trả về **trước** khi sửa sổ sách, nên `emulatedWebContents` vẫn trung thực (emulation vẫn đang bật) và lần gọi sau — khi view đã attach — sẽ retry. View đã attach giữ nguyên hành vi cũ: không pane nào đang hiển thị bị tắt emulation.
- **Bằng chứng**: đối chứng trước/sau trên cùng một host (script throwaway, đã xoá) — *detached enable*: sau fix `[]` / `emulated=false`, trước fix `[enable:desktop]` / `emulated=true`; *split tab, mobile detached*: sau fix `[enable:desktop]`, trước fix `[enable:desktop, enable:mobile]` (đúng lời gọi gây fault). Ca thứ tư là **contrast pair** trong một test: attach đủ 2 pane thì `enable:mobile` **có** xảy ra, nên khẳng định phủ định ở nhánh detached không rỗng. `test/main/native-tab-host-agent-lifecycle.test.ts` 13/13 (4 ca mới), 4 suite split 68/68, `npm run test:main` 1269/1272 — fail duy nhất `does NOT block quiescence on never-ending third-party analytics beacons` là assertion wall-clock, file đó không import `NativeTabHost` và pass khi chạy riêng; `tsc -p .` sạch.
- **Harness nói thật thay vì nới assertion**: `test/main/split-review-tabhost.test.ts` trước đây dựng `contentView` bằng hai hàm rỗng, **không có** mảng `children`, nên host không thể phân biệt view đã attach với view mồ côi — 2 ca cũ fail vì đúng lý do đó. Harness nay có child list thật và attach pane của tab active; không assertion nào bị nới.

### Sửa lỗi — Crash loop bị đọc thành một cái chết (register gộp row theo `errorMessage`)
- **Nguyên nhân**: `IssueRegister.record()` dedupe theo `toolName + errorMessage + status`, còn intake ghi `errorMessage = summary.headline` — mà headline chỉ gồm exception + offset + địa chỉ fault. Nhiều dump cùng signature (đúng dạng crash loop) ⇒ cùng headline ⇒ gộp thành **một** row: `affected` dồn cả danh sách dump và `timestamp` bị ghi đè thành lần cuối. Trong sổ thật, `ISS-1789564111316-tmg4y` và `ISS-1789579340686-mkk3c` cùng một dump nhưng **không** gộp, chỉ vì headline của chúng tình cờ khác nhau (một cái kèm full path `E:\Work\...\electron.exe+0x…`, một cái chỉ `electron.exe+0x…`) — may mắn, không phải thiết kế.
- **Sửa**: `crash-report-intake.ts` ghi `errorMessage` kèm tên dump (`${headline} (dump ${filename})`) — theo đúng convention sẵn có của chính file (`CRASH_DUMP_UNREADABLE` cũng nêu tên dump trong message). Mỗi cái chết còn một row, `affected` vẫn giữ bằng chứng, và việc chống ghi trùng cùng-dump vẫn do `accountedFor` lo nên không sinh row thừa.
- **Bằng chứng**: `test/unit/crash-report-intake.test.ts` 8/8 — ca "caps reports per launch and discards the rest so a crash loop cannot flood the register" trước đó fail `1 == 2`, nay xanh **mà không sửa một assertion nào**. Probe trước fix: register chỉ có 1 row `affected: [crash-a.dmp, capped-2.dmp, capped-1.dmp]`; sau fix: intake báo 2 và register giữ 2 row. Đây cũng là điều kiện để `runtime.crash` đếm đúng số cái chết.

### Cập nhật — Core Health UI nói thẳng về native crash (trước đây là một P0 lẫn trong 90 issue)
- **Vấn đề**: crash có vào sổ, nhưng Core Health chỉ gộp thành một dòng `DEGRADED / OPEN_HIGH_SEVERITY_ISSUES` với detail `19 P0 + 67 P1 open issues` — không phân biệt được "browser process chết" với một finding QA, cũng không thấy dump / pid / lúc nào.
- **Sửa**: `diagnostics/core-health.ts` thêm check hạng nhất `runtime.crash` (luôn có mặt, kể cả khi sạch) và `snapshot.crashes` (`total`, `latestAt`, `latestId`, `records`), đọc từ chính sổ issue (`errorCode === 'NATIVE_CRASH'`, đang OPEN, mới nhất trước); `notes` của crash reporter được parse phòng thủ nên record vẫn báo id/time/reasonCode khi metadata hỏng. UI (`renderer/toolbar.ts`, `renderer/toolbar.css`): badge tab Core Health hiện `CRASH ×N` (đỏ, giữ nguyên khi tab đang active, tooltip kèm thời điểm + reasonCode + dump) thay cho status; pane chi tiết của `runtime.crash` mở đầu bằng records (process, dump, pid) thay vì blob JSON chung. Check mới **không** tự làm xấu trạng thái — phiên sạch vẫn `HEALTHY / ALL_GATES_PASSED`.
- **Bằng chứng**: probe trên sổ issue thật → check `runtime.crash` = `DEGRADED / BROWSER_PROCESS_CRASHED`, `affected` = 3 id crash, detail `3 browser-process crash(es) recorded; latest 2026-09-17T02:28:10.295Z in the browser process · c2bbb433-925f-403a-adf2-9a1ae635509e.dmp`; `crashes.records` có đủ `processType: browser`, `pid: 32284`, dump. `test/unit/core-health-service.test.ts` 17/17 (3 ca mới: crash kèm evidence, notes hỏng, phiên sạch vẫn HEALTHY); `tsc -p .` sạch.
- **Chưa đóng**: 3 record P0 trong sổ vẫn để `OPEN` — crash native chỉ xác nhận được bằng một phiên chạy thật, nên phải khởi động lại app rồi thấy `runtime.crash` chuyển `HEALTHY / NO_CRASH_RECORDED` mới là bằng chứng fix ăn.

### Sửa lỗi — Dải tab của Hub nghiền nát nhãn (báo trực tiếp từ app đang chạy)
- **Nguyên nhân**: `.hub-nav-strip` là flex row 7 tab, còn `.hub-nav-tab` không có `flex-shrink: 0` lẫn `nowrap` — nên khi nội dung vượt bề rộng thì flexbox **co từng tab cho vừa** (bề rộng tab = 1009-1180px, tổng bề rộng tự nhiên của 7 tab là 1629px), nhãn tiếng Việt rơi xuống 2-3 dòng và badge bị đẩy sang dòng riêng. Badge `CRASH ×3` / `DEGRADED` dài hơn các badge số cũ nên càng siết thêm.
- **Sửa**: `.hub-nav-tab`, `.hub-tab-icon` và `.hub-tab-badge` giữ nguyên bề rộng (`flex-shrink: 0`) và không ngắt dòng (`white-space: nowrap`); `.hub-nav-strip` cuộn ngang (thanh cuộn mảnh) nên nhãn luôn nguyên vẹn ở mọi bề rộng, kể cả khi badge dài thêm (`CRASH ×12`).
- **Bằng chứng** (đo trong chính Electron 43, chạy đúng tệp HTML/CSS của Hub, cửa sổ 1317×819 như ảnh báo lỗi): CSS ở `HEAD` → 7 tab bị co còn tổng 1098px, **chiều cao tab 86px** (nhãn 2 dòng), strip không cuộn; sau fix → tab giữ đủ bề rộng tự nhiên (1629px), **chiều cao 41px** đồng nhất (1 dòng), strip cuộn ngang. Ở cửa sổ 1000px: `HEAD` 102px (3 dòng) / sau fix 41px.

### Sửa lỗi — App biến mất khi chụp tab agent offscreen (`Page.captureScreenshot fromSurface:false`)
- **Nguyên nhân**: tab agent Dual-Plane chạy OSR (`prefs.offscreen = true`, `paintWhenInitiallyHidden = false`) nên **không có native window**; `fromSurface:false` là đường native-window snapshot của Chromium → `WindowSnapshotReachedScreen` → `GrabNativeWindowSnapshot(GetNativeView() = null)` → **deref NULL, chết cả browser process** (app biến mất, không có log JS nào). Dump trong `runtime/crashDumps/reports/` là `0xC0000005` tại địa chỉ `0x0`.
- **Sửa**: `tab-devtools-host.ts` không bao giờ gửi `fromSurface:false` nữa — Tier 2 (CDP fallback) và `captureVerificationScreenshot` (mọi mode, kể cả `viewport`) đều dùng `fromSurface:true`; **xoá hẳn Tier 3 "Offscreen Native View Paint Fallback"** (trùng Tier 2, chỉ khác đúng đường native gây chết). Đích không có compositor surface giờ trả về lỗi capture có kiểu (`CAPTURE_*`) thay vì giết process.
- **Guard vòng đời**: `onNavigate` và `onMessage` (`CSS.styleSheetAdded`) của CDP thoát sớm khi WebContents đã bị huỷ, không đọc state của target đã chết.
- **Giữ dump có biên**: `diagnostics/crash-dump-retention.ts` giữ 3 dump mới nhất theo mtime (đệ quy trong `reports/`, không đụng tệp không phải `.dmp`, không ném lỗi), chạy sau `crashReporter.start` và sau intake (prune trước intake sẽ xoá mất dump chưa được ghi sổ); 4 ca trong `test/main/crash-dump-retention.test.ts`.
- **Bằng chứng**: `npm run test:main` → 1242/1244 pass, 0 fail (2 skip), gồm `tab-devtools-host.test.ts` (assert `fromSurface:true` cho desktop viewport và background attached tab) và ca 6 `playwright-parity-kernel.test.ts` (CDP fallback rasterize từ compositor surface, không bao giờ xin native-window snapshot). `npm run test:unit` lúc đó → 939/947; 2 fail còn lại (`bridge-receipt-coverage`, `core-health-service`) đã được vá trong cùng release — xem mục "Sửa test — CLI coverage + core-health hermetic" bên dưới; suite hiện 958/963 pass, 0 fail.
- **Gỡ test lỗi thời**: `workflow-schema` / `frame-queue` / `semantic-ref-occlusion` / `touch-lifecycle` — API mà chúng kiểm đã bị xoá hoặc đổi tên trên nhánh này (`LegacyWorkflow*Schema`, `normalizeStepParams`, hằng coalescing của bridge, script executor viết lại), nên giữ lại chỉ là ghim chữ trong script sinh ra.

### Sửa lỗi — Full-page capture treo 60s khi cửa sổ ẩn/minimized (NO_RENDER_SURFACE)
- **Nguyên nhân**: `captureVerificationScreenshot` mode `fullPage`/`clip` gọi `Page.captureScreenshot` với `captureBeyondViewport:true`; cửa sổ `hide()`/minimized không sinh compositor frame nên lệnh không bao giờ settle → treo đúng deadline 60s (`CAPTURE_TIMEOUT`). `document.hidden` **không** quan sát được `win.hide()` trong app này (renderer vẫn báo visible), nên gate settle không chặn được. Port path đã có `NO_RENDER_SURFACE` từ trước; verify path thiếu kiểm tra tương đương.
- **Sửa**: `tab-devtools-host.ts` thêm refusal trong `captureAction` (sau attach + zero-geometry check): `mode !== 'viewport'` && target không offscreen && cửa sổ không presented → `CaptureError('NO_RENDER_SURFACE', cause 'window-not-presented' | classifyRenderSurfaceCause(surface))`. Seam mới `ctx.isWindowRenderable` (`native-tab-host.ts`: `!isDestroyed() && isVisible() && !isMinimized()`). Viewport-hidden vẫn chụp được (không cần beyond-viewport surface); offscreen target giữ path riêng; `fromSurface` giữ `true` mọi mode — không chạm lại đường native-window-snapshot gây crash 0xC0000005.
- **Bằng chứng** (`.tmp-af-hub-audit/cap-parity.cjs`, control-plane thật gắn vào host, cả hai GPU mode): `hidden/verify-fullpage` `NO_RENDER_SURFACE` trong **5-6 ms** (trước: 60.286/60.297 ms `CAPTURE_TIMEOUT`); `hidden/verify-viewport` OK ~316-356 ms; `hidden/port-fullpage` `NO_RENDER_SURFACE` 4-7 ms; `recovered/verify-viewport` OK ~333-358 ms sau `show()`.

### Đính chính phép đo — MCP/port surface hoạt động đúng; `disable-gpu` không phải nguyên nhân treo
- Probe `.tmp-cap-deep*` trước đây đo port/MCP bằng target tự mint (`browserEpoch: 1`, lease tự phát) → `isCurrentTarget` (`native-tab-host.ts:7609`) luôn false vì host không có control plane → **mọi** lệnh port/MCP trả `TARGET_STALE` trong ~1 ms. Đó là artifact của harness, không phải defect sản phẩm. Khi gắn `ControlPlaneRuntime` thật (như `index.ts:371`) và mint target qua `getAutomationTarget()`: `port-screenshot` 385-418 ms, `port-fullpage` ~1,0-1,2 s, `mcp-viewport` ~0,34-0,36 s, `mcp-full_page` 1,35-2,25 s trên trang 14.604 px — không lệnh nào gần bound 25/30 s.
- `disable-gpu` bị minh oan cho ca treo capture: software raster chụp full-page 14.604 px trong 1,39-4,14 s, ngang GPU-on (1,36-5,06 s); payload software còn nhỏ hơn (185.431 B vs 200.349 B). Biến quyết định là **cửa sổ ẩn**, đã vá ở mục trên.
- Trang vượt trần 16.384 px (18.900 px thử nghiệm) bị từ chối có kiểu `FULLPAGE_CAPTURE_UNSUPPORTED_GEOMETRY` trong 4-11 ms trên cả verify lẫn port — không treo, không dark window.
- Drain state sau timeout có biên: `sendCdpCommand` (`tab-devtools-host.ts:652-660`) force-detach + xoá queue khi drain > 5.000 ms; không thể poison target vĩnh viễn.

### Sửa test — CLI coverage + core-health hermetic
- `scripts/antifan-core.cjs`: thêm `case 'replay'` → `core.replayRegression` và `case 'observe'` → `core.recordObservation` (tên bị pin bởi `RECEIPT_REQUIRED_CLI_COMMANDS` trong `.omp/hooks/pre/antifan-core-bridge.ts`). `bridge-receipt-coverage.test.mjs` xanh — hai dispatch mutating của commit `efa14a6` giờ có CLI subcommand.
- `test/unit/core-health-service.test.ts`: hai ca trước đây phụ thuộc môi trường (dist `.super-core` tồn tại sẵn trên máy dev → test "UNAVAILABLE" fail; temp tsconfig thiếu `types`/`typeRoots` → test real-CLI skip). Nay test 1 spawn **bản copy** của `antifan-core.cjs` trong temp dir (require `../packages/super-core/dist` miss deterministically → exit 2 → UNAVAILABLE thật), test 2 thêm `types:['node']` + `typeRoots` trỏ repo `node_modules/@types` để temp build compile được. 14/14 pass, 0 skip.

### Sửa lỗi — Hub không nói được vì sao app biến mất (native crash không có record)
- **Nguyên nhân**: một fault native không chạy handler JS nào, nên dump trong `runtime/crashDumps/` là **nhân chứng duy nhất** — nhưng không có gì đọc nó. `index.ts` chỉ bật crash reporter rồi để dump nằm im; sổ issue (`issues/issue-register.jsonl`) không có record nào về cái chết. Ca thật 16:38 ngày 16-09: `0xC0000005` (`STATUS_ACCESS_VIOLATION`), pid 30392, **browser process**, `electron.exe+0x684D7A8`, đọc địa chỉ `0x0`, 58 thread, có `pty.node` nạp sẵn — và Hub hoàn toàn im lặng.
- **Sửa**: `diagnostics/crash-dump-forensics.ts` đọc minidump bằng các `read()` có biên trên descriptor (không nạp bản sao 68 MB thứ hai): header + directory, exception record **64-bit** (`+8` code, `+24` address, `+32` số tham số, `+40` tham số — bản 32-bit làm address bị cắt và biến fault trong image thành fault "ngoài mọi image"), module list, dictionary annotation đọc **theo descriptor** trong CrashpadInfo, và crash-key pool của Chromium (`pid`/`ptype`) nằm trong khoảng trống không stream nào mô tả. `diagnostics/crash-report-intake.ts` mỗi lần khởi động đọc các dump **chưa có trên sổ**: ghi `crashReport.detected` vào lifecycle journal và issue P0 `NATIVE_CRASH` cho mỗi dump mới (issueClass `runtime`, gộp nhóm Root Causes của Hub), dedupe theo tên dump trong `affected`, tối đa 5 issue/lần khởi động (phần dư ghi `crashReport.deferred` để vòng crash không nhấn chìm sổ), dump không đọc được → P1 `CRASH_DUMP_UNREADABLE` (có dump vẫn là bằng chứng có cái chết). Nối vào `index.ts` ngay sau `crashReporter.start`, trước `pruneOldCrashDumps` (prune trước sẽ xoá dump chưa được ghi sổ), không chờ và không chặn khởi động. `IssueClass` thêm `runtime` (`NATIVE_CRASH`, `CRASH_DUMP_UNREADABLE`, tiền tố `runtime.`).
- **Bằng chứng**: parse dump thật (68.626.272 B) trong **22 ms** → `browser died with STATUS_ACCESS_VIOLATION (0xc0000005) at ...\electron.exe+0x000000000684d7a8, read of 0x0000000000000000` (pid 30392, `ver` 43.4.0, 58 thread, native addon `node-pty\prebuilds\win32-x64\pty.node`); sau intake, `IssueRegister.summarizeOpen()` có `P0 NATIVE_CRASH ×1 [runtime]`; `test/unit/crash-report-intake.test.ts` 7/7 (dump dựng theo đúng layout byte, gồm ca write-fault/địa chỉ ngoài mọi image, dump rác → null, dedupe lần khởi động thứ hai, cap 5).

### Sửa lỗi — Full-page capture từ chối oan trên trang có carousel/banner động
- **Nguyên nhân**: `imageIdentityStable` so **cả vị trí** từng ảnh, nên một carousel chạy bằng `transform` (tiki.vn: hộp ảnh `1000,4680,156,156` dịch chuyển trong khi `docHeight` đứng yên 7576) và một slot quảng cáo đổi nguồn ảnh (vnexpress.net: beacon 1×1 → creative) đều bị trả `IMAGE_IDENTITY_UNSTABLE` dù tài liệu đã đứng yên để raster.
- **Sửa**: structural identity chỉ còn **kích thước hộp ảnh** (bỏ vị trí, bỏ `naturalWidth/Height`): ảnh thêm/bớt/đổi cỡ → vẫn từ chối (tài liệu còn đang dựng); ảnh **dịch chuyển** (carousel transform, reflow trong viewport) → chấp nhận. Lý do và witness đổi theo đúng cái đã đo (`first differing box size`), không suy diễn tên lớp CSS của một trang cụ thể.
- **Bằng chứng** (full-page thật): tiki.vn `OK` 2.479.777 B raster 1424×7576 (trước: từ chối), vnexpress.net `OK` 3.614.737 B raster 1424×11359 (trước: từ chối), wikipedia từ chối **có lý do** (`1424x60913` vượt trần 16384) trong 8 ms; `test/unit/capture-settle.test.ts` 38/38, gồm ca mới "admits a capture while a transform carousel moves images without reshaping them".

### Haravan — Đổi đích theme, dọn đích cũ, siết script pull
- **Chọn đích theo tham số và từ chối theme đang chạy**: `scripts/lib/theme-target.mjs` (`resolveThemeId()`) là nguồn duy nhất cho 4 script kiểm chứng storefront — mặc định theme `1001514194`, đọc `HARAVAN_THEME_ID`, từ chối `1001510509` (theme `main`) và mọi giá trị không phải số (`test/unit/theme-target.test.mjs`). Chạy thật: `HARAVAN_THEME_ID=1001510509` → exit 1 kèm `[SAFETY] refusing to target the protected live theme`; mặc định → 15/15 route HTTP 200.
- **Pull là mirror chứ không phải sync, và biết remote đã đổi**: quyết định lấy/không lấy tách thành `scripts/lib/pull-decision.mjs` (12 ca trong `test/unit/pull-decision.test.mjs`) — tệp bị sửa sau pull **không bao giờ** bị ghi đè kể cả khi `HARAVAN_FORCE_REFRESH=1`; tệp văn bản so độ dài byte; **ảnh nhị phân so `updated_at`** và thêm tín hiệu thứ hai: độ dài lệch `size` API thì tải lại một lần, nếu vẫn lệch thì ghi `variantRepresentation: true` để tín hiệu yếu đó không thành vòng tải-lại-mỗi-lần-chạy (cờ sống qua cả nhánh `skip` và `edited`). Trước đây ảnh chỉ được tải lại khi bấm force nên drift thật không bao giờ bị thấy: `hera_index_hero_2_mobile.jpg` `17:02:03Z → 17:35:29Z`, `shop_social_sidebar_item_image_{1,5}.png` → `17:35:42Z`/`17:35:47Z` trong lúc làm việc. Script nay in danh sách khoá được tải lại; hai lần chạy liên tiếp cho `fetched 0, refreshed 0, skipped 241`.
- **Ghi nhận giới hạn của phép đo byte**: CDN trả byte khác nhau cho cùng một `public_url` (`hera_index_hero_1_mobile.jpg`: 45.507 B rồi 55.681 B) và theo `Accept` (`Vary: accept, accept-encoding`; webp 76.840 B vs jpeg 98.904 B) nên tài liệu **không** còn lấy "khớp byte với CDN" làm tiêu chí kiểm chứng. `size` là trường **không diễn giải được** — nó không khớp ổn định với `attachment` lẫn byte CDN (hero_1 `size` 55.681 vs `attachment` 45.507; coupon_2 2.140 vs 1.653; social_1 1.542 vs 1.288; chỉ hero_2 khớp) nên không dùng làm tiêu chí đủ/thiếu. Nội dung uỷ nhiệm là `value` (văn bản) / `attachment` base64 (nhị phân). Các ghi chú cấp khoá (`variantRepresentation`, `attachmentBytes`, `cdnSourced`) nay mang qua `skip`/`edited` bằng **một danh sách duy nhất** (`CARRIED_ENTRY_NOTES`) thay vì từng câu `if` viết tay — chính kiểu viết tay đó đã làm rơi `attachmentBytes` rồi `cdnSourced`. Khi API không trả `attachment` cho ảnh, byte lấy từ CDN được ghi dấu `cdnSourced: true` vô điều kiện (trước đây nguồn chỉ được suy ra từ độ dài ≠ `size`, bỏ sót đúng ca dài bằng `size`); đối chiếu nội dung 18:18:40Z–18:19:24Z: 237/237 khoá so sánh được khớp, 4 khoá remote không có nội dung nên không kiểm được. Đo tiếp trên 2 ảnh đang lệch cho thấy CDN trả **đúng định dạng** theo đuôi tệp (`image/jpeg`, `image/png`) và gửi `Accept` không đổi câu trả lời (cùng sha256) ⇒ không có byte sai định dạng, và không có cách nào "sửa" phía client để đạt parity với bản gốc; hai khoá đó được đánh dấu `variantRepresentation` và mỗi lần chạy in ra INFO.
- **Ảnh nhị phân lấy từ `attachment` của API, và có chế độ kiểm tra parity**: nội dung uỷ nhiệm của một asset là `value` (văn bản) hoặc `attachment` base64 (nhị phân); `public_url` có thể trả bản đã chuyển đổi/cache nên chỉ dùng làm đường dự phòng, và `public_url` không đúng host `cdn.hstatic.net` thì bị từ chối kèm lý do thay vì lỗi DNS. Thêm `HARAVAN_VERIFY_PARITY=1`: đọc lại nội dung uỷ nhiệm của API cho mọi khoá, so sha256 với mirror, in `[FAIL]` + exit 1 khi lệch (asset không có nội dung phía remote được báo `[WARN]` riêng, không tính là lệch). Đo được: 240/241 khoá khớp tại thời điểm kiểm.
- **Dọn đích cũ**: xoá 617 tệp tracked gắn theme `1001512581` (mirror `themes/phukienmaymoc-copy` 53 MB, plan `260912-1731`, report cấp store/phiên cũ, 3 script gắn mirror); hạ cấp claim `VERIFIED` mất nguồn trong `docs/haravan` (và repoint quan sát corpus sang `customizes/*` — nguồn còn sống); xoá rác `.hrv_tmp_pull-*`; giữ `reports/haravan-store-inventory.json` vì `specs/base-theme-contract.json` cite.

### Annotation — Self-QA gate buộc mở tab xem thật + QA binding (contract 3.3.0-lean)
- `SELF_QA_DIRECTIVE` viết lại thành pipeline bắt buộc: (1) mở/reload tab tới Page URL của annotation và `anti.inspect.dom`/`anti.inspect.styles` đúng Element Selector để chứng minh DOM live phản ánh edit (chưa phản ánh → báo "verification pending theme sync", không báo pass/fail), (2) `anti.screenshot.viewport`/`full_page` làm bằng chứng, (3) `theme.qa_validate` với `tabId`/`workspaceRoot`/`expectedUrl` từ dòng "QA Binding" mới trong annotation markdown. Bổ sung nhánh lỗi còn thiếu: `CAPABILITY_NOT_FOUND` (không retry trong task, nhưng task mới phải tự probe lại), `SETTLE_INCOMPLETE`/`CAPTURE_NOT_READY` (retry đúng 1 lần rồi báo `INCONCLUSIVE` kèm gate detail), `TARGET_*`/`URL_*_MISMATCH` (`anti.browser.rebind_target` → navigate tab đang bind tới Page URL). `AGENT_CONTRACT_VERSION` 3.2.0-lean → 3.3.0-lean; test literal cập nhật có chủ đích.
- Annotation payload mang `tabId` của tab được annotate (`AnnotationPayload.tabId`, `AntiFanPickedElement.tabId`, inject tại `tab-devtools-host.ts` từ `targetTabId` phía host — không tin payload phía page); markdown sinh ra có dòng `QA Binding (AntiFan MCP)` gồm `tabId` + `workspaceRoot` + `expectedUrl`.
- `theme.qa_validate` nhận thêm `expectedUrl` (optional): khi khai báo, route gate `checkRouteIdentity` (host/themeid/path) chạy trước khi validate — sai trang hoặc sai `themeid` ném `URL_*_MISMATCH` thay vì QA lặng lẽ trên tab cũ. `BrowserControlPort.getLiveTabUrl`/`getTabRedirectChain` public hoá để capability dùng lại đúng route gate của visual compare.

### Annotation — QA receipt + terminal `qaStatus` token (contract 3.4.0-lean)
- `theme.qa_validate` nhận thêm `annotationId` và sau mỗi lần validate ghi **receipt file** `<workspaceRoot>/.antifan/qa-receipts/<ts>-<runId>.json` (receiptVersion, runId/attemptId, annotationId, tabId, documentGeneration, browserEpoch, expectedUrl, observedUrl, verdict `QA_PASSED`/`QA_FAILED`, passed, criticalCount, createdAt). Receipt là bằng chứng gate cho runtime phía consumer — ghi receipt lỗi không bao giờ làm fail call QA.
- `SELF_QA_DIRECTIVE` bắt buộc mọi đường kết thúc khai báo đúng một token `qaStatus`: `QA_PASSED` | `QA_FAILED` | `QA_INCONCLUSIVE` | `QA_UNAVAILABLE`; báo "done" không token là vi phạm contract. `CAPABILITY_NOT_FOUND` đổi từ "không retry" sang **re-probe đúng 1 lần** (capability có thể vừa đăng ký sau restart app) trước khi terminal `QA_UNAVAILABLE`.
- Enforcement phía consumer: `~/.omp/agent/hooks/post/theme-qa-gate.ts` (user-scope OMP hook) theo dõi write/edit vào workspace có `.antifan/`; khi chưa có receipt mới hơn lần edit, mọi `tool_result` bị nối reminder gate; bypass audited qua token `QA_UNAVAILABLE`/`QA_INCONCLUSIVE` trong message assistant. Gate trên edit quan sát được, không trên intent khai báo.


### Toolbar — Sửa lỗi popup bị clip, bookmark bar, prompt modal và các nút hỏng
- **Cơ chế overlay token (chống clip popup)**: `toolbar.ts` thay thế `setOverlay(true/false)` đơn lẻ bằng `overlayTokens: Map<string, number | undefined>` refcounted. Trước đây đóng 1 popup gọi `setOverlay(false)` vô điều kiện làm co toolbar view (74px) và clip mọi popup còn mở (find bar, app menu, context menu). Nay mỗi popup giữ token riêng (`theme-qa`, `workflow-hub`, `phone-status`, `profile-dropdown`, `app-menu`, `shortcuts`, `mobile-remote`, `tab-context`, `suggest`, `find-bar`, `toast`, `prompt`); view chỉ co khi token cuối cùng được giải phóng; chiều cao overlay là max của các token đang giữ.
- **Bookmark bar hiển thị thật**: `native-tab-host.ts` thêm `isBookmarkBarVisible` vào payload `GET_INITIAL_STATE` và `STATE_UPDATED`; `renderBookmarks()` bỏ dòng `style.display = 'none'` hardcode, hiển thị bar (`display: flex`) khi bật và có bookmark, đồng thời render danh sách bookmark items có click-to-navigate. Tự động set CSS var `--bookmark-bar-offset: 28px` (0px khi tắt).
- **Find bar dynamic top**: `toolbar.css` đổi `.find-bar` từ `top: 76px` cố định sang `top: calc(76px + var(--bookmark-bar-offset, 0px))` — không còn bị đè lên bookmark bar khi bar đang mở.
- **Toolbar toast**: chuyển từ `bottom: 8px` (bị vẽ đè lên nút toolbar trong strip 74px) sang `top: 76px; right: 14px` (hiển thị dưới strip, trên vùng trang) và tự acquire token overlay `toast` (chiều cao 40px) trong suốt thời gian hiển thị.
- **Prompt modal thay thế `window.prompt()`**: Electron renderer ném lỗi khi gọi `window.prompt()`, làm chết 2 tính năng (`btnHubNewWorkflow` tạo workflow mới trong Hub và `menuItemSetAlias` đặt alias tab trong context menu). Bổ sung `#promptOverlay` modal (HTML + CSS + `showPromptDialog()` bằng `Promise.withResolvers`) xử lý Enter/OK và Esc/Cancel/backdrop.
- **Opener cho Mobile Remote**: `#mobileRemoteOverlay` vốn mồ côi (không có nút mở vì `btnMobileRemote` không tồn tại trong HTML). Bổ sung mục `menuItemMobileRemote` (📱 Mobile Remote (QR)) vào `#appDropdownMenu` và wire sự kiện mở modal.
- **Phím Escape toàn cục**: nhấn Esc nay lần lượt đóng popup/modal đang mở (`phoneStatus`, `tabContextMenu`, `appDropdownMenu`, `profileDropdownMenu`, `shortcutsOverlay`, `workflowHubOverlay`, `mobileRemoteOverlay`, `themeQaOverlay`).
- **Screenshot ghi Clipboard**: `menuItemScreenshot` trước đây gọi `captureViewport()` rồi bỏ qua kết quả; nay ghi base64 vào `navigator.clipboard` và hiển thị toast xác nhận.
- **Theme QA hiển thị lỗi rõ ràng**: `runThemeQa()` thất bại nay mở `themeQaOverlay` hiển thị chi tiết lỗi thay vì chỉ hiện một toast thoáng rồi mất dấu.
- **Dọn dẹp mã chết**: gỡ bỏ các listener và biến thừa gắn với các ID không tồn tại trong `toolbar.html` (`btnRuler`, `btnCaptureFullPage`, `btnDevTools`, `codexMainMenu`, và 6 ID menu cũ `menuFind`, `menuQuickAnnotate`, `menuFontFinder`, `menuLens`, `menuOpenBrowser`, `menuShortcuts`). Sửa độ rộng tối thiểu của toolbar overlay khi mở sidebar từ `Math.max(200, ...)` thành `Math.max(400, ...)` cho nhất quán với `updateLayout`.
### Sửa lỗi — Terminal mất handle PTY (phát hiện khi làm test "cắn")
- `TerminalManager.spawn` tạo record với `pty: null` nhưng **không gán** handle `node-pty` vừa tạo vào record, nên mọi đường dùng handle đều là no-op: `writeTo`/`write` bỏ im lặng mọi phím gõ (`ensureSessionPty` coi record không PTY là "chờ khôi phục" và spawn lại), `resize`/`resizeTo` chỉ ghi `pendingCols/pendingRows`, teardown không kill được shell (nguy cơ tiến trình mồ côi), và `getDiagnostics` luôn báo `runningPtyCount = 0`. Nay `spawn` gán `s.pty = child`; hai test `INVARIANT 4 (Input Routing)` và `INVARIANT 5 (Geometry Routing)` trong `test/main/terminal-stream-invariants.test.ts` khẳng định input và geometry tới đúng PTY của session — đỏ riêng từng ca khi bỏ dòng gán, xanh khi có.

### Kiểm thử — Audit suite (ultra: 5 ứng viên + 1 verifier)
- Gate `npm test` từng dừng ngay sau `test:canary` đỏ (`&&`), nên ~1.957 test phía sau chưa từng chạy trong gate mặc định. Nay `scripts/run-test-pipeline.mjs` chạy đủ 7 làn test ở `npm test` (thêm 2 cổng tĩnh `audit` + `plans:check` ở `npm run verify`), ghi nhận từng làn, đánh dấu `skipped` cho các làn phụ thuộc khi `compile` hỏng, và thoát 1 nếu có làn đỏ; `npm test`/`npm run verify` trỏ vào runner này.
- 12 test `test:main` đỏ vì fixture thiếu kỳ vọng URL (`URL_EXPECTATION_MISSING` → `INCONCLUSIVE`) và `diffPixels` đã bị bỏ khỏi payload so sánh: khôi phục `diffPixels` ở `src/main/tools/browser-control-port.ts` (tầng báo cáo, canary writer và QA matrix đều đọc trường này) và bổ sung `expectedTargetUrl`/`expectedBaselineUrl` trong fixture. `test:main` nay 1091 pass / 0 fail / 1 skip có tiền đề.
- `callTool` của MCP client nuốt `isError` và trả chuỗi lỗi như kết quả thành công ⇒ nay ném lỗi; test client chạy trên double stdio hermetic mới (`test/fixtures/mcp/fake-omp-mcp.cjs`, 52 tool) thay vì bridge sống.
- Ba test replay canary "pass" dù artifact không tồn tại: nay có tiền đề máy kiểm được (`test/fixtures/canary-run/replay-precondition.mjs` liệt kê 9 artifact thiếu kèm `sha256`/`byteLength`), skip phải nêu tên tiền đề thiếu thay vì im lặng.
- Test soak e2e khẳng định literal thay vì số đo: mọi trường báo cáo nay suy ra từ `samples` đã đo (`REQUIRED_SAMPLES = 10`, `SOAK_SLOPE_LIMIT_MB_PER_MIN = 5`).
- Bốn test "nói dối" được chuyển sang chạy code thật: cookie partition đi qua biên Electron `session` (bỏ `Map` RFC 6265 tự dựng); terminal invariants chỉ stub biên `node-pty` (bỏ ghi đè `privates.spawn`); gap state machine nạp `src/renderer/standalone.js` thật trong `vm` (harness dùng chung `test/renderer/standalone-harness.ts`); Round 1/4/6/8 của split (regex trên nguồn + số học sao chép) thay bằng 8 ca hành vi trong `test/renderer/terminal-split-behaviour.test.ts` — gồm hình học split đo từ `clientHeight`, kẹp `paneMin`, debounce nút, chord bàn phím, nhãn context menu, và định tuyến hyperlink (`createTab` → fallback `openExternal` khi thiếu **và** khi reject).
- Test "low-spec latency" đo vòng lặp `setImmediate` của Node (không chạm production) nay kiểm hai bất biến thật của `AsyncThemeQaQueue`: supersede abort đồng bộ, và job cũ settle muộn không xoá generation mới.
- `scripts/check-plans.mjs` bỏ qua plan **không có frontmatter** (11 plan không bao giờ bị gate) ⇒ nay nêu tên và thoát 1; đã ghi `status` có bằng chứng cho cả 11. `test/unit/check-plans.test.mjs` cập nhật theo hợp đồng mới (3 → 6 ca), gồm ca plan thiếu `status:` và ca cây plan của repo.
- Bất biến "zero remote hotlink" của site-clone không thấy `srcset`/`imagesrcset`/`poster`/`formaction`/CSS `url()`: nay có oracle độc lập `remoteResourceUrls()` tách từng candidate, fixture mang `srcset` remote thật, và ca chứng minh oracle bắt đủ 6 ngữ cảnh.
- `test/main/omp-mcp-adapter.test.ts`: 8 chỗ `await responsePromise` không có hạn (child im lặng ⇒ treo cả làn) nay bọc `withDeadline`; giữ `child.kill()` trong `finally`.
- Sửa nhỏ: tiêu đề test JPEG trùng với gate PNG trong `test/unit/visual-capture.test.ts`; chú thích sai "endpoint `/api/cookies/import` đã bị gỡ" (endpoint vẫn tồn tại, chỉ handshake extension bị gỡ); `.gitignore` thêm `.antifan-data/`.

### Haravan A–Z — Đợt rà soát chéo (ultra: 5 ứng viên + 1 verifier)
- Newsletter ở footer dùng đúng quy ước form của theme: khối thành công là `div.alert.alert-success[role="alert"]`, khối lỗi là `div.form-errors.alert.alert-danger[role="alert"]` với `{{ form.errors | default_errors }}`; trước đó là `div.form-error` cùng vòng lặp `form.errors.messages[field]` — không có CSS trong `assets/theme.css` và lệch khỏi `§states.formErrors` của hợp đồng cùng 7 form còn lại (article, activate_account, addresses, login, register, reset password, page.contact).
- Hợp đồng hết mâu thuẫn với code đã ship: `§states.variantUnavailable` và `SCENARIO_03` không còn mô tả nhãn "Không khả dụng" qua `selected_variant == nil` (theme không có chuỗi đó; `product.liquid:1` luôn có `current_variant`, và nút chỉ có "Thêm vào giỏ hàng"/"Hết hàng"). Trạng thái nay mô tả đúng kiến trúc swatch zero-JS: mọi chip trỏ tới biến thể thật, combo không tồn tại thì rơi về biến thể còn hàng mang giá trị đó.
- `ROUTE_LIST_COLLECTIONS` khai `paginate` trong `requiredLiquidObjects`, khớp quy ước của các route phân trang khác (`ROUTE_COLLECTION`, `ROUTE_SEARCH`, `ROUTE_BLOG`) và khớp bảng route trong tài liệu.
- Mục 9 tài liệu hợp đồng cập nhật 12 → 13 kịch bản; mục 10 bổ sung dòng `UNRESOLVED_06_NEWSLETTER_FORM_MECHANISM` (trước đó chỉ có 5 dòng dù JSON đã định nghĩa 6).
- Harness render `plans/reports/_render-base-theme.mjs` được nâng từ 19 lên **37 kiểm tra**: stub `paginate` nay cắt mảng theo `per_page`, dựng `parts`/`previous`/`next` nên nhánh `paginate.pages > 1` của `pagination-default.liquid` thực sự chạy (12/12/2 tile cho 26 danh mục, ẩn nav khi chỉ 1 trang); render thật `snippets/footer.liquid` (mặc định, thành công, lỗi, và tắt newsletter → không gọi form tag) cùng `templates/404.liquid` (không prefill `search.terms`).
- Kiểm tra pass 3 của swatch được sửa cho đúng đích: fixture cũ còn biến thể 4 nên pass 1 khớp trước và pass 3 không bao giờ chạy; nay `removeVariants: [4]` + `availability: { 2: false }` buộc pass 3 chạy, và assertion kiểm tra `href` trỏ đúng `?variant=2` thay vì chỉ tìm chuỗi "(Hết hàng)". Mỗi kiểm tra mới đều được xác nhận bằng đột biến: đổi `default_errors`, đổi `form 'customer'`, giới hạn `(1..2)`, bỏ include pagination, thêm prefill 404 — cả năm đều làm harness đỏ đúng chỗ.
- Gỡ hàm chết `hrefs()` trong harness.
- Vòng soát advisory sau đó: gỡ cờ chết `is_color`/lớp `color-swatch` trong `swatch.liquid` (không có rule CSS nào trong theme, chỉ là affordance rỗng); `pagination-default.liquid` ép kiểu `part.title | times: 1` trước khi so với `paginate.current_page` (corpus dùng đúng idiom này; nếu `title` về dạng chuỗi thì so sánh trực tiếp sẽ trượt); stub harness đặt `current_page` trong drop `paginate` và phát `title` dạng chuỗi, nên `aria-current="page"` được render thật và kiểm tra mới bắt được lỗi nếu ai bỏ ép kiểu (đột biến xác nhận). Siết lại hai câu quá mạnh: `SCENARIO_03` nay nói rõ thứ tự pass (hết hàng thì trỏ biến thể đã hết hàng và trang render "Hết hàng"), `SCENARIO_04` nói rõ số cột lấy từ setting `collection_grid_columns` ở mọi viewport vì theme không có breakpoint đổi số cột. Harness: **37 → 38 kiểm tra**. Mục 4.2 của tài liệu hợp đồng còn mô tả swatch theo kiểu "ưu tiên biến thể còn hàng" (tức là chọn theo tồn kho) — đã viết lại đúng 3 lượt như spec, lượt 1 cố ý không xét tồn kho.

### Haravan A–Z — Lint truthfulness, Base Theme, QA fail-closed
- Sửa bộ lint Haravan theo bằng chứng 45 theme: biến vòng lặp `cart.items` được tự do (chỉ chặn alias sai `cart_item`/`item_cart`/`cart_line_item`); `settings['x']` được phân giải theo cả `config/settings_schema.json` lẫn tên control trong `config/settings.html` (control nằm trong HTML comment không khai báo gì); `product.media` buộc có fallback `product.images` cùng file; `media_tag` bị từ chối; `blog.articles.size` chỉ bị chặn khi dùng như tổng số (không chặn phép kiểm rỗng); `settings.html` chỉ bị cấm khi schema đi kèm còn sống. Trên theme dự án: 27 → 5 vi phạm, cả 5 đều là include trỏ tới snippet không tồn tại.
- `dod-validator` chỉ chấp nhận `?themeid=<số>` khi audit preview Haravan; `theme_id=`/`preview_theme_id=` bị từ chối (chuỗi này từng xác nhận sai 45/45 case).
- QA fail-closed: thiếu bằng chứng ⇒ `INCONCLUSIVE`, điểm chưa đo để `null`, không tự cấp 100 cho một lần quét tĩnh.
- Bổ sung `themes/universal-haravan-base/` (base theme trung tính), wiki `docs/haravan/` (10 tài liệu), hợp đồng `specs/base-theme-contract.json`, cùng kế hoạch triển khai Haravan A–Z và bằng chứng scout/capture.

### Haravan A–Z — Vòng soát bàn giao (ShipReview) và Base Theme
- Lint: `settingsSchemaIsLive`/`settingsFormIsLive` từng là tham chiếu hàm trần (luôn truthy) nên chế độ `f1genz` từ chối mọi theme có `settings.html` và luật "thiếu khai báo settings" không bao giờ chạy; `SETTINGS_READ_PATTERN` cắt cụt id có dấu gạch nối (`settings.footer-top-check-1` → `footer`). Cả hai đã sửa và có fixture riêng.
- Site-clone/compiler: `atomicSwap` dọn `sections/`/`locales/` cũ khi hoán đổi thư mục; bộ sinh snippet không còn ghi đè header/footer do IR sinh ra; nhánh import `.ts` thứ ba của `compile-haravan-theme.mjs` nằm trong `try/catch`.
- `.gitignore`: `*.png` từng loại 123 ảnh PNG (18.82 MB) đang được Liquid trong theme dự án tham chiếu; negation `!themes/*/assets/*.png` khôi phục khả năng theo dõi.
- QA viewport chỉ-giảm: `passed` của caller là bắt buộc và được AND với ngưỡng `VISUAL_MISMATCH_PASS_THRESHOLD_PERCENT = 10`, nên diff 0.99% không thể nâng một FAIL cấu trúc thành PASS.
- Base theme: swatch là liên kết `/products/{handle}?variant={id}` (server render đúng biến thể, không cần JS, không lệch giá/ảnh); quickview nạp fragment từ `?view=quickview` → `templates/product.quickview.liquid` (`{% layout none %}`) và báo lỗi rõ khi tải hỏng; 404 có form tìm kiếm; gỡ 3 asset chết (`base-theme.css`, `base-theme.js`, `theme-tokens.css`).
- Hợp đồng base theme và `docs/haravan/base-theme-contract.md` khớp lại thực tế: input của product-loop, mô tả mini-cart/cart-table/quickview, số liệu census đo trên 45 root, và form newsletter được ghi nhận `UNVERIFIED`.

### Haravan A–Z — Đợt sửa sau rà soát (swatch đa thuộc tính, route /collections, newsletter)
- Swatch: chọn một thuộc tính không còn lật ngược thuộc tính khác. Mỗi giá trị chọn biến thể theo 3 lượt — (1) biến thể khớp mọi thuộc tính còn lại với biến thể đang render, **không xét tồn kho**, nên combo hết hàng vẫn được trỏ đúng và gắn nhãn (Hết hàng) thay vì lặng lẽ đổi sang giá trị khác; (2) biến thể còn hàng mang giá trị đó, chỉ dùng khi combo không tồn tại; (3) biến thể bất kỳ. Đang ở Xanh/S bấm M sẽ sang Xanh/M (kể cả khi Xanh/M hết hàng) thay vì Đỏ/M. Đã render thật qua LiquidJS: 19/19 kiểm tra.
- Bổ sung `templates/list-collections.liquid` cho route `/collections`: base theme trước đó không có template này trong khi route vẫn tồn tại — storefront thật trả HTTP 200 với class `template-list-collections`, và 41/45 root corpus phục vụ route bằng chính file đó (contract trước đây map sai sang `templates/collection.liquid`).
- Newsletter trở lại `{% form 'customer' %}` kèm `form.posted_successfully?`/`form.errors` — đây là cơ chế chuẩn của corpus (79 file thuộc 40/45 root đo được), thay cho POST thô tới `/account/contact` (chỉ 2 file). Đích lưu dữ liệu vẫn `UNVERIFIED` (`UNRESOLVED_06`).
- Gỡ snippet chết `product-grid.liquid` cùng setting `product_grid_columns` chỉ nó đọc (3 mặt cấu hình còn 38/38/38 khớp nhau); `collection-card.liquid` nay có nơi dùng thật là `list-collections`; bỏ prefill `search.terms` không có nguồn trên trang 404.

### Kiểm thử — Live PTY smoke trên shell thật
- `test/e2e/terminal-live-pty.test.ts` mở `powershell.exe` thật qua `node-pty` thật và chứng minh đường ống của sản phẩm, không phải stub: gõ dấu mốc rồi đọc lại từ chính shell, resize 100×40 được xác nhận **từ trong shell** (`[Console]::WindowWidth` = 100) lẫn trên handle (`pty.cols/rows`), `runningPtyCount` về 0 và teardown xong dưới 20 s với **shell pid cùng toàn bộ tiến trình con đều thoát**. Test theo dõi pid của chính shell (không quét cả máy) nên không đỏ giả khi các làn chạy song song; bỏ `safelyKillSession` làm test đỏ đúng chỗ (`timed out … waiting for the shell process`), khôi phục thì xanh. Làn `test:e2e` 7/7.

### Rendering & Device Preset Background
- Khắc phục triệt để lỗi tab Chromium hiển thị toàn màu đen (hoặc trắng) sau khi chuyển từ preset bo góc (iPhone/iPad/Galaxy) sang preset phẳng kích thước cố định (MacBook 13/14, Full HD, Surface Pro, iPhone SE…): `applyTabDeviceEmulation` giờ luôn đồng bộ màu nền của `WebContentsView` theo bán kính bo góc của preset (`#00000000` khi bo góc, `#ffffff` khi phẳng), thay vì chỉ đặt trong nhánh bo góc và bỏ quên nhánh còn lại. Trước đây view giữ nguyên trạng thái trong suốt, khiến mọi khoảnh khắc chưa được vẽ của tab lộ nền cửa sổ `#080c14` ra ngoài (tab đen) cho tới khi F5 vẽ lại toàn bộ viewport.
- Bổ sung unit test khoá hợp đồng "màu nền view luôn khớp bán kính bo góc" cho toàn bộ danh mục `DEVICE_PRESETS`, chặn tái phát khi thêm preset mới.

---
## [v1.3.5] - 2026-09-01 (Core Runtime Hardening, Lineage Safety & Dual-Tier MCP Parity)

### Core Runtime & Lineage Security
- Bổ sung canonical control-plane ID validation (`validateControlPlaneId`) và containment validation (`assertWorkspaceContained`, `assertNoReparseTraversal`) trong `WorkspaceFilePort.stageAttachment`, ngăn chặn triệt để path traversal qua `runId` và symlink/junction reparse points.
- Kiểm tra `assertNoReparseTraversal` trước khi `mkdirSync` đệ quy thư mục run, ngăn chặn side-effect tạo thư mục ngoài workspace khi `.antifan` hoặc `.antifan/artifacts` là symlink trỏ ra ngoài.
- Hỗ trợ idempotent duplicate staging: phát hiện file trùng qua `flag: 'wx'`, kiểm tra `isFile()` và so khớp hash `sha256` nguyên vẹn; ném `INVALID_ARGUMENT` khi phát hiện file bị sửa đổi / sai lệch hash.

### MCP & Capability Tool Parity
- Khớp nối và chuẩn hóa danh mục tool alias: expose đầy đủ `anti.theme.assert_cart` qua `mcpServer.listTools()` và duy trì contract đồng nhất cho `anti.agent.cursor.move`.
- Khắc phục triệt để các trường hợp edge case trong bootstrap parsing và heartbeat renewal.

## [v1.3.3] - 2026-08-31 (Runtime Performance, Token Guarding & Modular TabHost)

### Runtime Performance & Terminal Buffer Optimization
- Tối ưu hoá $O(1)$ memory cho `safeSliceTailJsonBounded` trong `TerminalManager`: duyệt ngược 1 lần (single-pass reverse scan) tính toán chính xác byte size UTF-8, escape sequences (`\x1b[0m`), ký tự điều khiển và lone surrogates ES2019 ($0xD800..0xDFFF \rightarrow 6\text{ bytes}$), xử lý buffer 512KB+ dưới 4ms mà không cấp phát mảng lớn.
- Bổ sung bảo vệ biên UTF-16 surrogate pairs cho `safeSliceTail`.

### Token Guard & Semantic Ref Error Protection
- Khắc phục triệt để nguy cơ rò rỉ toàn bộ DOM/`outerHTML` vào LLM context khi ref lookup thất bại: `SemanticRefRegistry` và `TabAutomationHost` trả về error payload cấu trúc ngắn gọn kèm gợi ý CSS selector.

### Phân tách Modular Sub-Controllers cho NativeTabHost
- Tách `TabAutomationHost` quản lý con trỏ AI trực quan (Visual Cursor), quỹ đạo Bézier (Bézier Trajectory), và thực thi World 1004 có bảo vệ hàng đợi FIFO.
- Tách `TabDevToolsHost` quản lý Font Finder, Lens, Ruler, Picker, In-Page DOM/screenshot, và View Page Source.
- Duy trì 100% public API parity và tương thích ngược hoàn hảo trên `NativeTabHost`.


## [v1.4.2] - 2026-08-28 (Annotation Popup Compact & Queue Prefix)

### Popup Annotation — /queue Prefix & Gọn Lại
- Popup Annotation mặc định bắt đầu bằng tiền tố `/queue `: mọi annotation được đẩy vào hàng đợi agent (`/queue`) ngay khi mở, chỉ cần gõ yêu cầu; nếu xoá tiền tố, nó được tự chèn lại lúc gửi.
- Xoá nút đính kèm ảnh và dòng gợi ý phím tắt trên footer — popup gọn chỉ còn nút Gửi; dán ảnh (Ctrl+V) và kéo-thả ảnh vẫn hoạt động như cũ.
- Popup đáp ứng kích thước màn hình: rộng `min(92vw, 400px)` (to hơn trên màn lớn, co gọn trên màn nhỏ), định vị dựa trên kích thước đo thực tế nên không tràn viewport dù textarea tự nở rộng.

## [v1.4.1] - 2026-08-28 (Per-Tab Terminal Memory Fix)

### Popup Annotation — Ghi nhớ Terminal theo từng tab
- Sửa lỗi Popup Annotation dùng nhầm Terminal của tab khác: trước đây lựa chọn Terminal được lưu ở một slot toàn cục (`lastAnnotationSessionId`) + `localStorage` chia sẻ theo origin, nên chọn Terminal B ở tab 2 sẽ ghi đè lựa chọn Terminal A ở tab 1.
- Lựa chọn Terminal giờ được lưu **theo từng tab** (`tab.state.terminalSessionId`, field đã có sẵn trong contract `AntiFanTab`); `startInspect`, `did-finish-load` và poll 200ms chỉ đọc/ghi đúng tab đang inspect.
- Xoá toàn bộ đọc/ghi `localStorage['antifan_last_annotation_session_id']` khỏi element-picker — hết rò rỉ giữa các tab cùng origin.
- Poll inspect được khoá chặt vào `inspectedTabId`: chuyển tab giữa lúc inspect không còn đọc nhầm context của tab khác.
- Nối dây kênh IPC chết `SET_TAB_TERMINAL_SESSION` (`antifan:toolbar:set-tab-terminal-session`) — bổ sung handler tại `NativeTabHost`.
- Giữ backward-compat: `getLastAnnotationSessionId()` / `setLastAnnotationSessionId()` vẫn hoạt động (mặc định theo tab đang active, hỗ trợ tham số `tabId`).
- Tab mới chưa chọn Terminal vẫn mặc định `auto`; session bị đóng/kill → tự động quay về `auto`, không crash. Thêm regression test `test/main/per-tab-terminal-session.test.ts`.

### Diagnostics Trust Gate
- `TabDiagnosticsManager` thêm `clear()`: buffer diagnostics bị xoá ĐỒNG BỘ tại `did-start-navigation` (main-frame, không in-place, pane có quyền điều hướng) — dữ liệu QA không còn nhiễm từ navigation trước.
- Console/failure entries gắn `origin` + `isFirstParty` tại thời điểm record (tính theo URL tab); eval/blob/data/javascript fallback page-owned, không bao giờ throw.

### Shared Diagnostics Filter
- Module `src/main/qa/diagnostics-filter.ts` là nguồn duy nhất: `computeOrigin`, `classifyDiagnostics`, `sanitizeDiagnosticText`, `stripUrlQuery`, `confineWorkspaceRoot`.
- Verdict: console level ≥ 3 và network failure (Chromium NetError âm, trừ `-3` aborted) từ first-party/theme-asset CDN (`hstatic.net`, `shopifycdn.com`, `cdn.shopify.com`, `cdn.sapo.vn`) → critical; third-party noise (GTM, FB Pixel, chat widget) → warning; main-frame failure luôn critical.
- Cả full path `ThemeQaWorkflow.validate` lẫn fallback path đều dùng chung filter → cùng verdict; fallback giờ luôn trả `summary` đầy đủ (`passed`/`totalIssues`/`criticalCount`).
- Sanitize đầu ra: bỏ control chars/backticks/role markers, cắt query string (token/email không lộ vào prompt); workspaceRoot confine chống traversal.

### Self-QA Directive trong Annotation Prompt
- `buildAgentTaskHeader` chèn directive Self-QA: sau khi sửa file gọi `theme.qa_validate`, chỉ báo hoàn tất khi `summary.passed === true && criticalCount === 0`, tối đa 2 vòng tự sửa; nhánh fallback khi tool thiếu/lỗi auth (`ATTACHMENT_REQUIRED`/`ATTACHMENT_INVALID`/`MCP_CONTEXT_REQUIRED`) → báo dev xác nhận visual, CẤM bịa kết quả QA.
- Intents read-only (review/research/security/documentation/testing/extract-component) dùng variant bằng chứng không bắt buộc.
- `AGENT_CONTRACT_VERSION` 3.0.0 → 3.1.0 (đồng bộ test literal + evidence envelope).

## [v1.3.0] - 2026-08-27 (Theme QA & Verification Gate Release)

### E-Commerce Platform Detection (Haravan, Sapo, Shopify)
- Tích hợp `PlatformDetector` nhận diện tự động nền tảng Theme qua cấu trúc thư mục (`settings_schema.json`, `.bwt` vs `.liquid`, `package.json`), domain runtime (`haravan.com`, `mysapo.net`, `myshopify.com`) và script CDN (`hstatic.net`, `bizweb.dktcdn.net`, `cdn.shopify.com`).

### Zero-Liquid Error Scanner (RT-01, RT-05)
- Tự động phát hiện lỗi biên dịch Liquid runtime (`Liquid error:`, `missing_include`, `filter_error`, `translation_missing`, `syntax_error`).
- Áp dụng bộ lọc loại trừ thông minh (RTE / Rich Text Content Exclusion) không báo lỗi giả khi nội dung văn bản trong bài viết hoặc mô tả sản phẩm chứa từ khoá "Liquid error".

### Responsive Layout Overflow Engine & Culprit Attribution (RT-04, RT-06)
- Quét tự động tràn ngang (Horizontal Layout Overflow) trên 3 breakpoints chuẩn e-commerce (Mobile 375px, Tablet 768px, Desktop 1440px).
- Bổ sung ngưỡng deadband sub-pixel 0.5px loại trừ sai số làm tròn floating-point của trình duyệt.
- Thuật toán Culprit Attribution xác định chính xác thẻ DOM và selector gây tràn ngang cùng toạ độ bounding box.

- Tích hợp bộ quy tắc kiểm định tương thích Haravan / Sapo / Shopify (đã triển khai HS-01 đến HS-06, mở rộng theo lỗi thực tế từ pilot):
  - **HS-01**: Kiểm tra form Add to Cart (`name="variantId"` cho Sapo vs `name="id"` cho Haravan/Shopify).
  - **HS-02**: Kiểm tra endpoint form liên hệ (`action="/postcontact"` cho Sapo vs `action="/contact"` cho Haravan/Shopify) và sự hiện diện trường `contact[email]`.
  - **HS-03**: Casing trường blog comment trên Sapo (`Author/Email/Body` chuẩn hoa chữ đầu vs dạng thường).
  - **HS-04**: Kiểm tra handler xoá địa chỉ khách hàng (`deleteAddress`) — engine runtime chứng minh sự vắng mặt qua `typeof`, fallback static chỉ cảnh báo (không thể chứng minh handler thiếu vì có thể nạp từ script ngoài).
  - **HS-05**: Ảnh featured phải dùng URL CDN tuyệt đối của đúng nền tảng (`hstatic.net` / `dktcdn.net` / `cdn.shopify.com`).
  - **HS-06**: Script analytics/nặng phải được bảo vệ bởi guard noPS/StartOptimize trước khi tải.

### MCP Stdio Capabilities & Renderer QA Badge
- Expose 2 MCP Tools mới cho AI Coding Agents (Antigravity, Claude Code, Cursor): `theme.qa_validate` và `theme.debug_bundle` (hỗ trợ alias `antifan_theme_qa_validate`, `antifan_theme_debug_bundle`).
- Tích hợp Theme QA Badge trên Toolbar Renderer hiển thị trạng thái và kích hoạt quét kiểm thử storefront nhanh.
- Tự động lọc thông tin nhạy cảm (PII Sanitization - email, số điện thoại, token) trên toàn bộ báo cáo và artifact lưu trữ.

### Trusted Diagnostics Gate & Annotation Self-QA Prompt
- Diagnostics buffer được xoá đồng bộ tại `did-start-navigation` (main-frame, đúng authority pane) — hết lỗi ma khi navigate A→B; mọi console/network entry mang theo `origin`/`isFirstParty` để phân loại theo nguồn.
- Shared filter `diagnostics-filter` dùng chung cho full path + fallback quick path: verdict nhất quán, `summary` object đầy đủ trên cả hai path (3rd-party noise chỉ là warning, không fail gate), workspace root bị confine chống path traversal.
- Prompt annotation giờ yêu cầu agent tự gọi `theme.qa_validate` sau khi sửa (tối đa 2 vòng tự sửa) với fallback không tool / lỗi auth / hết vòng — CẤM bịa kết quả QA; `AGENT_CONTRACT_VERSION` bump `3.0.0` → `3.1.0`.

---

## [v1.2.4] - 2026-08-19 (AntiFan Unified Release)

### Desktop Viewport Auto-Fit Zoom & Presets
- Tự động tính toán tỷ lệ `fitScale = min(1.0, availableWidth / presetWidth)` và áp dụng `webContents.setZoomFactor(fitScale)` khi chọn kích thước thiết bị Desktop (FHD 1920×1080, 1728×1117, 1440×900).
- Khắc phục triệt để lỗi tràn ngang (horizontal viewport clipping) trên màn hình nhỏ hoặc khi mở đồng thời Sidebar Chat, đảm bảo website luôn hiển thị vừa khít 100% không bị che khuất nội dung.

### Google Chrome Parity: Ctrl + Mouse Wheel Zoom
- Bổ sung phím tắt chuẩn Google Chrome: giữ `Ctrl` (hoặc `Cmd`) kết hợp lăn chuột (Mouse Wheel) để phóng to / thu nhỏ nội dung trang web mượt mà từ 25% đến 500% theo bước nhảy 10%.

### Single-Bridge IPC Command Dispatch & Multi-Chat Loop Fix
- Thay thế toàn bộ cơ chế ghi file shotgun đa thư mục bằng việc xác định đúng Workspace đích và ghi duy nhất vào `.antigravity/mcp-bridge/${cmdId}.json` kèm `conversationId`.
- Triệt tiêu 100% hiện tượng tin nhắn bị loop vô hạn hoặc phát tán sang tất cả các phiên chat đang hoạt động trong IDE.

### Rich Markdown Table Rendering
- Tích hợp bộ tiền xử lý Markdown Tables với container bo góc hiện đại, tự động trích xuất bảng biểu thành placeholder `ANTIFANTABLEBLOCK` trước khi xử lý đoạn văn, loại bỏ lỗi bảng vỡ do thẻ `<p>`.

---

## [v1.2.3] - 2026-08-19

### Performance & Continuous Live Streaming Sync
- Kích hoạt phần cứng GPU toàn diện trong Electron/Chromium: bỏ qua danh sách đen GPU (`ignore-gpu-blocklist`), bật GPU rasterization, zero-copy memory transfer và smooth-scrolling giúp cuộn trang và hiệu ứng mượt mà 60fps.
- Tắt tính năng `CalculateNativeWinOcclusion` trên Windows giúp triệt tiêu hiện tượng giật lag/drop frame khi sử dụng đa màn hình hoặc nhiều cửa sổ.
- Giữ toggle Work luôn mở (`open = true`) và cập nhật liên tục theo thời gian thực trong suốt quá trình Agent đang suy nghĩ hoặc gọi tool calls. Chỉ tự động đóng lại khi cả turn hoàn tất 100%.
- Nâng cấp cơ chế đồng bộ Live Transcript: theo dõi trực tiếp thư mục logs và stat poll 500ms. Bảo đảm không bị mất hoặc nghẽn sự kiện trên Windows.
- Bộ nhớ đệm Markdown Render Cache (LRU Map) trong Sidebar Chat giúp tái sử dụng HTML đã render của các tin nhắn trước đó, triệt tiêu CPU spike khi streaming.

### GPU Lens Zoom & Color Loupe (DPI & Aspect Ratio Fix)
- Khắc phục triệt để lỗi vỡ hình và méo tỷ lệ ảnh trong Lens Zoom do sai lệch toạ độ `devicePixelRatio` giữa ảnh chụp `capturePage` và CSS viewport trên màn hình Windows High-DPI.
- Tính toán tỷ lệ co giãn thực tế (`scaleX`, `scaleY`) theo pixel bitmap gốc của viewport, bảo đảm hình ảnh trong Lens luôn sắc nét 100%, đúng vị trí và không bị biến dạng.
- Hỗ trợ render Canvas High-DPI (`220px * dpr`) với đường cắt tròn (circular clip mask) chống răng cưa.
- Bổ sung lưới pixel chuyên dụng (Pixel Grid) khi phóng to >= 4x giúp dò tìm pixel và mã màu HEX chính xác như trong Figma/Photoshop.

### Thinking Markdown Formatting & Section Headers
- Định dạng toàn diện Markdown bên trong khung Thinking: tự động chuyển đổi tiêu đề in đậm `**...**` thành các khối section header tinh tế với biểu tượng ✦, tô màu inline code `` `...` ``, danh sách gạch đầu dòng và khoảng cách đoạn văn chuẩn typography.

### Continuous Message Queue Auto-Dispatch
- Tự động đẩy tin nhắn tiếp theo trong Hàng chờ (Message Queue) ngay khi Turn của Agent kết thúc (`isRunning: false` trong `onSessionChanged`).
- Bổ sung cơ chế Watchdog giám sát trạng thái idle định kỳ, bảo đảm không bao giờ bị kẹt hàng chờ.

### Agent Browser (Visual AI Cursor & Chromium Web Automation)
- Bổ sung chế độ Agent Browser 100% tương đồng Antigravity IDE:
  - Con trỏ chuột AI Agent phát sáng (`🤖 Agent`) di chuyển mượt mà trên trang web theo thời gian thực.
  - Hiệu ứng gợn sóng (ripple wave pulse) khi click và spotlight quang bao quanh element mục tiêu.
  - Bong bóng thông báo hành động (Action Banner) và mô phỏng gõ phím từng ký tự (Typing simulation).
  - Tích hợp đầy đủ bộ công cụ Agent Automation qua MCP và WebSocket Bridge: `antifan_agent_click`, `antifan_agent_type`, `antifan_agent_scroll`, `antifan_agent_hover`, `antifan_agent_highlight`, `antifan_agent_clear`.

### Strict Workspace & Session Prompt Routing Isolation
- Khắc phục triệt để lỗi prompt bị gửi đồng loạt tới tất cả các cửa sổ Antigravity IDE đang mở.
- Xây dựng thuật toán `findWorkspaceRoot(path)` phân tích transcript và trích xuất chính xác thư mục workspace gốc của từng phiên trò chuyện.
- Cơ chế phân luồng đơn (Single-Targeted Bridge Dispatch): Chỉ ghi lệnh MCP bridge duy nhất vào thư mục `.antigravity/mcp-bridge` của đúng Workspace được chọn.

### Windows Desktop Installation & Application Icon
- Tạo icon ứng dụng độ phân giải cao (`assets/icon.png` và `assets/icon.ico`) với phong cách neon cyan / orbital particle sang trọng.
- Gắn icon chính thức vào cửa sổ BrowserWindow và thanh Taskbar trên Windows.
- Tạo bộ cài đặt Shortcut tự động trên Windows (`npm run install:shortcut`): tạo Shortcut "AntiFan Browser" trực tiếp trên Desktop và Start Menu.

---

## [v1.2.2] - 2026-08-19

### Assistant Turn Aggregation & Single Work Drawer
- Gộp tất cả các bước trung gian (Thinking và các Tool Calls liên tiếp) trong một phiên trả lời của Assistant vào một turn duy nhất.
- Thay vì tạo hàng chục dòng "Worked for a few seconds" rời rạc, giờ đây toàn bộ các bước thực thi được gom gọn trong 1 toggle tổng thể.
- Trong khi đang phản hồi trực tiếp (streaming): khối được mở để người dùng theo dõi các bước xử lý live.
- Khi phản hồi hoàn tất: khối tự động đóng lại thành một dòng duy nhất.

### Annotation Comment Validation
- Bắt buộc phải có comment người dùng mới được gửi annotation theo đúng chuẩn `E:\Work\apps\antigravity-browser`.
- Hiển thị cảnh báo màu đỏ "Add a comment before sending to Chat." bên dưới textarea và tự động focus con trỏ nếu bấm Send/Enter khi chưa nhập nội dung.

---

## [v1.2.1] - 2026-08-19

### Message Queue Edit & Session Scoping
- Bổ sung nút "Sửa" (Edit) cho từng tin nhắn trong hàng chờ: đưa toàn bộ nội dung text, phần tử đính kèm (Element Chip) và ảnh chụp trở lại khung soạn thảo.
- Hàng chờ tin nhắn được phân tách độc lập theo từng phiên chat (session ID).

### IDE Pause Sync
- Đồng bộ nút Tạm dừng (Pause) trực tiếp với Antigravity IDE: phát lệnh abortTurn qua file command bridge và kênh WebSocket abortPrompt.

### Dynamic Skill & Agent Catalog
- Tích hợp SkillScanner tự động quét và đồng bộ toàn bộ hơn 100+ skill được cài đặt trong thư mục `~/.gemini/config/skills/`, plugins và workspace `.agents/skills/` vào menu autocomplete.

---

## [v1.2.0] - 2026-08-19

### Annotation & Element Picker Auto-Submit
- Tự động dispatch lệnh sendToAgentPanel và phát tới đúng phiên làm việc (active session/tab) ngay khi người dùng nhấn Gửi hoặc Enter trên Modal Annotation.
- Lưu trữ toàn bộ file task Markdown vào `.antigravity/annotations/` và ảnh chụp DOM vào `.antigravity/snapshots/` trong thư mục workspace active.
- Đồng bộ đầy đủ preview chip phần tử trên Chat Composer với thông số tag, class, kích thước, font chữ, màu sắc và ảnh thumbnail.

### Window State & Multi-Monitor Persistence
- Triển khai WindowStateManager tự động ghi nhớ toạ độ (x, y), kích thước (width, height) và trạng thái phóng to (isMaximized) vào `window-state.json`.
- Hỗ trợ tự động nhận diện hệ thống đa màn hình qua `screen.getAllDisplays()`, đảm bảo cửa sổ mở lại đúng vị trí trên màn hình phụ.

---

## [v1.0.0] - 2026-08-15 (Production Release)

### Multi-Tab Chromium Architecture & Developer Tools
- Quản lý nhiều tab độc lập bằng WebContentsView với bảo mật webPreferences nghiêm ngặt.
- Quick Inspect, Font Finder, GPU Lens Zoom, Pixel Ruler & Layout Grid.
- Tích hợp docked Developer Tools gắn cố định ở cạnh dưới.
